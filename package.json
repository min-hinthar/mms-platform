{
  "name": "mms-platform",
  "private": true,
  "packageManager": "pnpm@9.12.0",
  "engines": { "node": ">=20" },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "db:types": "supabase gen types typescript --linked > packages/db/src/types.gen.ts"
  },
  "devDependencies": {
    "turbo": "^2.3.0",
    "typescript": "^5.6.3",
    "@types/node": "^22.9.0"
  },
  "pnpm": {
    "overrides": {
      "next": "16.1.2",
      "react": "19.2.3",
      "react-dom": "19.2.3"
    }
  }
}
