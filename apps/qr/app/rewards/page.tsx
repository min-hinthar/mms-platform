// STUB — rewards. Reuse the delivery app's Burmese-gem loyalty ledger (shared Supabase).
// Render the tier ring + gem balance + perks from the existing loyalty tables.
export default function Rewards() {
  return (
    <main style={{ padding: 24, maxWidth: 440, margin: "0 auto" }}>
      <h1>Rewards</h1>
      <p style={{ color: "var(--t2)" }}>Tier ring, gem balance, perks — read from the shared loyalty ledger in M5.</p>
    </main>
  );
}
