// STUB — account. Profile, order history (reorder keeps modifiers), settings (theme/lang),
// feedback + ungated review triage. Auth via the delivery app's existing Supabase auth.
export default function Account() {
  return (
    <main style={{ padding: 24, maxWidth: 440, margin: "0 auto" }}>
      <h1>Account</h1>
      <p style={{ color: "var(--t2)" }}>
        Profile, order history + reorder, settings, feedback — M5.
      </p>
    </main>
  );
}
