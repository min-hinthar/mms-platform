## entry-home-boundaries

- **[HIGH]** `apps/qr/components/HomeResumeCard.tsx (home '/' session-resume card, W5a-adjacent order card)`
  - dep: useActiveOrderStatus(true) → useOrderStatus: PostgREST read of qr_orders (.maybeSingle poll ×10/3s) + realtime channel `order-status:<key>` + resolveSplitOrderId server action (bounded 4×2s retry)
  - behavior: Card RENDERS (order key comes from localStorage, TTL 4h) with statusText = 'Confirming your order' — and stays there FOREVER. Two traced paths: (a) no cached anon token (auth down, fresh/expired session) → useOrderStatus's effect never runs (`if (!anon) return`), so `exhausted` never sets and timedOut stays false; (b) cached valid token → REST reads fail, errs>=3 fail-fast sets timedOut=true after
  - gap: DISHONEST: the entry screen claims we are actively confirming a payment during a total outage, indefinitely. No failure state, no retry affordance, no 'status unavailable' truth. The W9c honesty fix stopped one consumer short.
  - fix: Consume timedOut/statusWord in HomeResumeCard: on timedOut show 'Placed — live status unavailable right now' (we DO hold a client record of a placed order; 'Placed' is the floor we can stand on), keep the /track link, and mirror the pill's quiet-not-gone rule. Also cover path (a): with no anon sessi
- **[HIGH]** `apps/qr/lib/authz.ts:80 + apps/qr/app/api/session/route.ts:47-51 + apps/qr/app/api/session/peek/route.ts:24-28 (every bearer/cookie-verified endpoint)`
  - dep: sessionClient(token).auth.getUser(token) / serverClient(cookies).auth.getUser() — a network round-trip to Supabase AUTH, which is down too
  - behavior: supabase-js wraps the network failure into an auth error (AuthRetryableFetchError → user:null), so: POST /api/session returns 401 {error:'Invalid session'}; GET /api/session/peek returns 401 'Invalid session'; and assertCartMember throws AuthzError('Not signed in', 401) — which funnels to every mutation surface: cart adds, /api/stripe/create-intent (returns e.message/e.status straight to the clien
  - gap: DISHONEST + misdirecting: blames the diner's session for OUR outage, and downstream client copy will push them into futile re-scan/re-join loops. A 401 also tempts clients to sign out / re-mint, churning state.
  - fix: In authz.ts and both session routes, branch on the auth error class (AuthRetryableFetchError / fetch failure) → 503 with honest copy ('We're having trouble on our end — hang tight and try again in a moment') + Retry-After; add an AuthzError kind='unavailable' so route handlers map it to 503 not 401.
- **[HIGH]** `apps/qr/components/AnonAuthGate.tsx:54-69 (root layout — runs before ANY session exists)`
  - dep: browserClient().auth.getSession (local) then supa.auth.signInAnonymously() — Supabase AUTH endpoint, down
  - behavior: On a fresh device: signInAnonymously fails, one retry, then console.error ONLY — no UI anywhere. `useAnonSession` returns null forever, so every bearer-gated consumer (useSessionPeek, useOrderStatus, realtime setAuth) sits in a permanent 'not loaded yet' state indistinguishable from loading. The gate re-runs only on pathname change. On a device with a cached non-anon (upgraded) session, getSession
  - gap: Total-auth-outage = silent limbo. No user-visible signal, no retry affordance on the entry screen, and the null-forever anon session converts every downstream loading state into an eternal skeleton/blank.
  - fix: Have AnonAuthGate publish a session status (ok | establishing | failed) via context/store; home + menu surfaces render an honest degraded-service note on 'failed' with a manual retry; pair with the global outage banner below.
- **[MED]** `apps/qr/app/page.tsx (entry '/') — the surface as a whole`
  - dep: Zero server reads (pure static JSX — confirmed by live 200s). Client children: HomeResumeCard (above), HomeSessionCard→useSessionPeek→GET /api/session/peek, AppHeader→getRewardsBadge server action
  - behavior: The home page renders 100% NORMAL: hero, all three door cards (Dine-in / To-go / Grocery), Join-a-table. useSessionPeek's catch resolves to [] (initial failure) so HomeSessionCard renders nothing — a diner with a live open table gets no resume card, silently. getRewardsBadge returns null (auth getUser fails) → header shows the plain 'Rewards' label (claims nothing — good). Net effect: the entry af
  - gap: No surface in the app can say 'ordering is temporarily down'. During a confirmed outage the honest move is to say so at the door, not to invite diners into a broken funnel. Also: HomeSessionCard hiding is fine, but a diner mid-meal loses their only visible way back to their table with zero explanati
  - fix: A lightweight health signal (e.g. the peek/badge failures + a ping) feeding one root-level degraded-service banner component (brand-voice, aria-live polite, token-styled): 'Ordering is having trouble right now — please see our staff, your table and order are safe.' Doors stay tappable (deep surfaces
- **[MED]** `apps/qr/app — not-found.tsx inventory: NONE anywhere (repo-wide)`
  - dep: Any unmatched URL — live evidence: /track/<uuid> 404s (track is a flat route; only /track?params exists)
  - behavior: Default Next.js 404: stark unbranded 'This page could not be found', system font, no home link, ignores tokens/dark Night theme, no a11y focus handling. A diner following a mangled/shortened tracking link hits a dead end with no way back.
  - gap: Off-brand dead end (outage-independent, but the outage exercise surfaced it). Violates the repo bar: tokens, brand voice, recovery affordance.
  - fix: Root app/not-found.tsx: branded 🫖 card matching error.tsx's geometry — 'That page isn't on our menu' + links home and (when an active order exists in the wayfinding store) to /track.
- **[MED]** `apps/qr/app/error.tsx + global-error.tsx — error-boundary inventory: root-only (NO segment-level error.tsx under any of menu/cart/track/account/grocery/dine-in/staff/rewards/board)`
  - dep: Any THROWN RSC read or client render error below the root layout
  - behavior: A thrown RSC read anywhere renders the BRANDED root boundary (never Next's ugly default 'Application error' page — good): 🫖 'This screen didn't load' / 'Something went wrong on our end. Your order is safe — try again.' + 48px Try again, focus moved to heading, posthog capture, chunk-reload guard; AppHeader + layout stay mounted. BUT (a) 'Try again' during a paused DB re-throws immediately with zer
  - gap: Reset-loop with no state: repeated failures look identical; no offline/health detection; the boundary can't distinguish 'transient' from 'we are down'. Segment-level boundaries are absent, so any future throwing read replaces the WHOLE page rather than the failed region.
  - fix: Track reset attempts in the boundary; after N failed resets swap copy to the outage register ('We're having trouble on our end — your order is safe; please see staff or try again shortly') and offer the home link. Consider segment error.tsx for the money surfaces (cart, track) so chrome+context surv
- **[MED]** `apps/qr/app/layout.tsx:61 + apps/qr/app/(order)/menu/page.tsx:16,110 + apps/qr/lib/menu/mostLoved.ts:33-80 — caching that masks (or fails to mask) the outage`
  - dep: Root `export const dynamic = 'force-dynamic'` (CSP-nonce requirement) vs menu `export const revalidate = 300` vs getMostLoved `unstable_cache({revalidate: 3600})`
  - behavior: Answer to 'why does /menu still stream content': NOT ISR — force-dynamic in the root layout makes every route render per-request (revalidate=300 on the menu page is inert under a force-dynamic parent), so no cached page can mask the outage. The menu streams because its catalog read swallows the error (`const { data } = await db.from('menu_items')…` — error destructured away, line 110) → items=[] →
  - gap: The outage is masked as empty content on the highest-traffic surface (menu agent's fix to own), and mostLoved caches its error-state [] for an hour post-recovery. From this surface's brief: know that NO page in the app is served from cache — every fallback state you design WILL be exercised on every
  - fix: For mostLoved: throw inside the unstable_cache callback on FETCH failure (or bypass caching the [] error result) so an error never becomes a cached fact; keep [] only for genuinely-empty aggregates. Menu empty-state: hand off to the menu-surface task.
- **[LOW]** `apps/qr/lib/useOrderStatus.ts:147-162 (+ every realtime consumer: home resume card, header pill via useActiveOrderStatus, group-cart channels)`
  - dep: supabase realtime websocket to the paused project (`.channel().subscribe()` — no status callback passed)
  - behavior: The websocket connect fails; realtime-js retries indefinitely on its backoff schedule — a silent, endless reconnect loop per mounted channel (battery/network churn on a diner's phone sitting on the entry screen with a resumable order). CHANNEL_ERROR/TIMED_OUT statuses are unobserved (subscribe() takes no callback), so no UI ever learns the live channel is dead — the REST poll's errs>=3 fail-fast (
  - gap: No cap/circuit-breaker on reconnect attempts during a sustained outage, and channel status is never surfaced, so 'live' UI cannot distinguish subscribed-and-quiet from never-connected.
  - fix: Pass a status callback to .subscribe(); on repeated CHANNEL_ERROR flag a shared 'realtime degraded' signal (feeds the same outage banner) and back the reconnect off (removeChannel + scheduled re-add with a long timer) while keeping the visibility-wake refetch as the recovery path.
- **[LOW]** `apps/qr/app — loading.tsx inventory gaps: /rewards, /board, /staff/{feedback,lock,login,profile,team}, /staff/table/[id]/add (present: account, cart, grocery, track, staff, staff/{approvals,expo,kitchen,orders}, staff/table/[id], (order)/{dine-in,menu}; '/' needs none — zero awaits)`
  - dep: All routes are force-dynamic → every navigation server-renders on demand; a segment without loading.tsx shows NO pending UI (client-side nav freezes on the previous screen) until SSR settles
  - behavior: For the missing-loading segments, navigation blocks with zero feedback for however long the server render takes to fail/settle. A paused Supabase mostly fails FAST (connection refused / 5xx — live evidence: 401 in 1.5s), so the freeze is short today — but a timeout-flavored failure (the other paused-project mode) would freeze these routes for the full fetch timeout with the diner staring at the pr
  - gap: Uneven coverage; /rewards is a diner-facing surface with a DB-read page and no skeleton. Staff/login+lock are near-static (low risk).
  - fix: Add loading.tsx to /rewards and /board at minimum; audit each missing staff segment for awaits.
- **[MED]** `apps/qr/app/api/* error-return sweep (board, stripe, resend, release-lock)`
  - dep: serviceClient() PostgREST reads/writes + assertCartMember auth verify
  - behavior: GET /api/board → 500 {error:'Board read failed'} (the wall-TV poll; client handling is the staff surface's trace). POST /api/stripe/create-intent → the auth check fails FIRST → 401 'Not signed in' (see the authz finding); past auth it would catch-all to 500 'Payment service error' (honest-ish). create-share-intent identical. POST /api/cart/release-lock → assertCartMember throws non-AuthzError on n
  - gap: All diner-facing API failure copy is either blame-shifting ('Invalid session', 'Not signed in') or generic; nothing distinguishes 'we are down' for clients to render honest outage copy.
  - fix: One shared 503 + machine-readable {error, code:'service_unavailable'} shape for detected infra failures across routes; client fetch helpers map it to the outage register.

## auth-session-mint

- **[HIGH]** `apps/qr/components/AnonAuthGate.tsx (lines 54-69) → lib/useAnonSession.ts → lib/useTableSession.ts:130,184`
  - dep: supabase.auth.signInAnonymously() / getSession() refresh against Supabase AUTH (GoTrue) — the root of the entire session chain; every door needs this before /api/session can even be POSTed
  - behavior: THE dominant outage state (fresh visitor, or returning diner whose ~1h anon access token expired — refresh also fails). signInAnonymously fails, retries ONCE, then console.error and gives up — no state is set anywhere. useAnonSession returns null forever; useTableSession's mint effect never runs (guard `if (!anon …) return`), so session=null AND error=null → `loading` is true FOREVER. Menu: every
  - gap: An eternal skeleton that looks like loading — the exact anti-pattern CLAUDE.md's Error/recovery bullet bans ("every async UI has a loading AND a failure/recovery state"). The failure state machine starts one layer too late: auth failure has no channel into the session UI. Also dishonest: "setting up
  - fix: Give useAnonSession a status: expose {session, status: 'pending'|'ready'|'failed', retry} (AnonAuthGate writes 'failed' after its retries via a module store or context). useTableSession maps status==='failed' to error='auth_unreachable' so ALL the existing recovery surfaces fire (solo alert card, Gu
- **[HIGH]** `apps/qr/app/grocery/page.tsx (lines 305-314, 525-538, 757-770)`
  - dep: Same root: useTableSession("scango") never mints because anon auth is down (State A above)
  - behavior: sessionError stays null and cartId stays null forever → the page shows "Starting your basket…" permanently (line 536). The Scan tab shows "Starting your basket… scanning opens in a moment." — it never opens. Every add tap (scan/search/browse) flashes "Still starting your basket — try again in a moment." — trying again in a moment will never work. Meanwhile GroceryBrowse's catalog read (a server re
  - gap: Three simultaneous lies on one money door: an eternal "Starting…" that is not starting, a "try again in a moment" that is false, and a "check your connection" that misdiagnoses a server outage. No path to a retry for the basket at all.
  - fix: Rides the useAnonSession status fix: auth-failed → sessionError set → the existing banner renders (with corrected copy, see next finding). Time-box the "Starting your basket…" hint: after N seconds of pending, swap to the failure banner. Catalog alert copy: distinguish fetch-rejected-offline (naviga
- **[HIGH]** `apps/qr/app/api/session/route.ts (lines 104-121, findActive) → components/GuestList.tsx (lines 80-125)`
  - dep: serviceClient() PostgREST read of table_sessions in findActive — `(await …maybeSingle()).data ?? null` swallows the transport error entirely
  - behavior: Partial-outage shape (PostgREST/DB down while GoTrue answers — also any post-auth DB blip): a joinOnly mint (`?j=` invite code) gets `sess=null` from a FAILED read and returns 404 "No table found for that code". GuestList shows that string VERBATIM (it pattern-matches 'No table found') and additionally renders the JoinTable escape — actively inviting the guest to re-type their perfectly correct co
  - gap: A DB read failure is indistinguishable from 'code not found' at the API boundary, so the most misdirecting copy in the app fires during an outage. The code comment KNOWS about the conflation and patches around it client-side instead of fixing the discrimination server-side.
  - fix: Stop swallowing: destructure `{ data, error }` in findActive; on error return 503 { error: 'service_unavailable' } (never the 404). Client: map 503/'service_unavailable' to outage copy ("We're having trouble on our end — your code is fine, try again in a moment") and suppress the JoinTable escape on
- **[MED]** `apps/qr/app/api/session/route.ts (lines 76-100, dine-in picker path)`
  - dep: serviceClient() read of qr_tables to resolve tableNumber → qr_code; `{ data: tbl }` destructure drops the error
  - behavior: A dine-in picker claim (`?table=N`) whose qr_tables read fails at transport gets 400 "That table isn't available — scan its sticker or pick another." The diner walks the room scanning stickers and picking other tables; every attempt fails with the same instruction. (Sticker-path table-number resolution at line 92-98 swallows the same failure benignly — sessionTable just nulls.)
  - gap: DB transport failure misclassified as a client mistake (400 + blame-the-table copy) during an outage.
  - fix: Destructure `{ data, error }`; error → 503 'service_unavailable' (only a genuine null row keeps the 400 copy). Same client 503 mapping as above.
- **[MED]** `apps/qr/app/api/session/route.ts (lines 44-51) — bearer verification`
  - dep: sessionClient(token).auth.getUser(token) — a live network round-trip to Supabase AUTH per mint
  - behavior: Full pause, diner still holds a VALID unexpired anon token (outage began mid-visit): getUser's fetch fails; supabase-js wraps it as a retryable fetch error → authErr truthy → 401 "Invalid session". The diner's session is NOT invalid — we couldn't verify it. If the fetch hangs instead of refusing, the route holds until the platform timeout → 504 with a non-JSON body → client error becomes "HTTP 504
  - gap: Wrong status class (401 for an upstream outage) and no client-visible distinction between 'your session really is invalid' (rare, should re-auth) and 'we are down' (should wait/retry). No timeout/AbortController on either the server's getUser or the client's mint fetch — the hang variant leaves the
  - fix: Inspect the getUser error: network/retryable kind → 503 'service_unavailable' (+ Retry-After); only a real verification rejection keeps 401. Add an AbortController (~8s) to the client mint fetch; map abort to the same outage error. Client copy for 503: "This is on us — we're having trouble reaching
- **[MED]** `apps/qr/app/grocery/page.tsx (lines 525-533, sessionError banner)`
  - dep: useTableSession error from a failed mint (States B/C — a token existed, the POST failed)
  - behavior: Banner: "Couldn't start your grocery basket — you can browse, but adding needs a connection." Two problems during a DB outage: (1) 'you can browse' is a promise the page can't keep — the catalog read fails too, so the aisles show their own failure alert; (2) 'needs a connection' implies the SHOPPER's connectivity when the server answered (401/500/503) — we are down, not them. Retry is `window.loca
  - gap: Connection-blaming copy for a server-side outage; a promise (browse) the sibling component breaks; reload-retry instead of the established in-place re-mint.
  - fix: Retry → revalidate() (matches W9d's own recovery pattern). Copy: branch on the failure class — server 5xx/503 → "We're having trouble on our end — we can't start baskets right now. You can still look around; try again in a moment."; fetch rejected + !navigator.onLine → keep the connection framing. D
- **[LOW]** `apps/qr/components/TableCartProvider.tsx (lines 559-615 solo card) + components/GuestList.tsx (line 96)`
  - dep: useTableSession error (any mint failure)
  - behavior: Solo modes: fixed alert "Couldn't reach your order. Try again" — neutral, retries in place, doesn't spin forever (error kills `loading`, no auto-retry loop). Dine-in: "Couldn't join this table." + "Try again"/"Reconnecting to your table…" narration. Both are serviceable but generic: neither says the problem is ours, neither distinguishes outage from a genuinely dead session, and there's no auto-re
  - gap: Copy stops short of the honesty bar for an outage ('Couldn't join this table' reads as a table/code problem), and a sustained outage degrades to manual tap-retry with no backoff or 'we're on it' framing.
  - fix: When error maps to the new 503/'auth_unreachable' class: copy → "We're having trouble on our end — hang tight, we'll keep trying." + a gentle capped auto-retry (e.g. 5s/15s/30s) with the manual button retained; keep current copy for genuine 4xx arms.
- **[LOW]** `apps/qr/app/api/session/route.ts (lines 152-166 sweep, 217-222 expiry slide, 269-276 cart find)`
  - dep: serviceClient() UPDATE/SELECT calls whose { error } is discarded (`await db…update()` result unused; `let { data: cart }` drops error)
  - behavior: Benign in a FULL outage (the subsequent insert fails → honest 500 'Could not create session'/'Could not create cart'), but during a flapping/partial outage a failed cart FIND is treated as 'no open cart' → an INSERT is attempted against a session that may already have one; the partial unique index makes this converge correctly, so the net behavior is a 500, not corruption. The swallowed sweep/expi
  - gap: Unannotated error swallows on a money/auth route (the Pre-PR sweep requires each to be a commented, deliberate swallow); all outage-shaped failures exit as generic 500s with no 503/Retry-After for the client to key honest copy off.
  - fix: Annotate or propagate: sweep + expiry-slide get a deliberate-swallow comment (they're best-effort); cart find should check `error` → 503. Standardize a `{ error: 'service_unavailable' }` 503 body across the route so one client mapping covers every arm.
- **[LOW]** `apps/qr/lib/useSessionPeek.ts + app/api/session/peek (home 'Your order' resume card)`
  - dep: GET /api/session/peek with the anon bearer (needs both auth AND DB)
  - behavior: No anon session → effect never runs → sessions stays null → resume card simply never renders. With a token, the peek 401s/500s → deliberate catch resolves to [] → card doesn't render. The home door remains fully usable-looking (shells are static) with no hint anything is down.
  - gap: Honest-by-omission, but it contributes to the masked outage: home looks perfect, every door works until the diner is deep in a flow. Worth one quiet, global 'we're having trouble' signal (e.g. in AppHeader) once the auth-status store knows we're down, so the FIRST honest signal isn't three taps deep
  - fix: No change to the peek itself. Once useAnonSession exposes 'failed', consider a single dismissible status ribbon at the shell level (one per view, role=status) — "Ordering is temporarily down — the menu's still browsable."

## menu-cart-provider

- **[HIGH]** `apps/qr/app/(order)/menu/page.tsx:110-135 → components/menu/MenuBrowser.tsx:606-626`
  - dep: RSC catalog read: publicClient().from('menu_items').select(...) — anon-key PostgREST GET, awaited during server render
  - behavior: PostgREST failures do NOT throw (supabase-js resolves { data: null, error }), and the page destructures `const { data }` — the error object is discarded, unlogged. items = [] → the page streams a fully-styled menu SHELL (header, search pill, category rail all absent since cats derive from items; toolbar + arrival beat render) with the MenuBrowser empty state: 'Nothing matches' + 'The menu catalog
  - gap: DISHONEST + DEAD-END: a full outage renders as 'The menu catalog is empty.' — blames the restaurant's menu, offers no retry, and swallows the error without even a console.error. The one surface where an outage page ('We're having trouble reaching the kitchen — try again in a moment' + retry) is most
  - fix: Destructure { data, error }; on error render a dedicated menu-outage state (honest 'we can't reach our menu right now' copy, brand voice, retry via router.refresh(), role=alert, tokens) or throw to error.tsx; log the error. Never reuse the filter empty state for a failed read (discriminate like W9d'
- **[HIGH]** `components/AnonAuthGate.tsx:54-69 + lib/useAnonSession.ts + lib/useTableSession.ts:184 + components/AddButton.tsx:100-110`
  - dep: Supabase AUTH (GoTrue) — signInAnonymously() for first-time visitors; token refresh for returning visitors with an expired cookie. Everything downstream (session mint, cart, realtime) needs the anon t
  - behavior: First-time visitor (or expired cookie): signInAnonymously fails, one retry, then console.error ONLY — AnonAuthGate renders nothing and never surfaces anything; it re-attempts only on a route change. useAnonSession stays null forever → useTableSession's effect NEVER RUNS → { session: null, error: null, loading: true } is an ETERNAL state. TableCartProvider: error is null → the 'Couldn't reach your
  - gap: The pre-session outage is INVISIBLE: loading:true/error:null limbo means every 'no session yet' guard reads as still-loading. 'setting up your table…' is an eternal skeleton-that-looks-like-loading — dishonest after ~10s — and it is audible-only; sighted diners see dim dead pills with no words. Anon
  - fix: Give the anon-auth failure a channel: e.g. AnonAuthGate exposes a failed flag (context/store) that useTableSession folds into error, so the existing solo banner + GuestList retry mount with outage-honest copy ('We're having trouble connecting — your menu browsing still works'); add a mint timeout th
- **[MED]** `lib/useTableSession.ts:137-182 + app/api/session/route.ts:47-51 + components/TableCartProvider.tsx:563-615 + components/GuestList.tsx:80-125`
  - dep: POST /api/session (mint/join) — server-side sessionClient(token).auth.getUser(token) network round-trip to the auth server, then ~8 service-role PostgREST queries
  - behavior: auth.getUser returns an error (auth-js wraps network failure, doesn't throw) → the route 401s '{ error: "Invalid session" }' in ~1-2s (matches the live 1.5s evidence for the no-bearer 401). Client: error='Invalid session', loading=false. SOLO modes: the fixed 'Couldn't reach your order. Try again' role=alert banner mounts (44px retry, tokens, pinned under header) — retry re-mints in place, safe, a
  - gap: COPY DISHONESTY: 'Couldn't join this table' implies the table/code is the problem when WE are down; the server's 'Invalid session' (never shown) misclassifies an outage as an auth problem. Worse taxonomy risk one layer down: api/session's findActive swallows its PostgREST error, so a DB-down read on
  - fix: Discriminate at the route: map auth/PostgREST network failures to a distinct 503 { error: 'service_unavailable' }; client maps it to honest copy ('We're having trouble reaching our kitchen — nothing you did; try again in a moment') on both the solo banner and GuestList, keeping current copy for genu
- **[MED]** `components/TableCartProvider.tsx:385-486 + components/AddButton.tsx:173-241 + lib/authz.ts:107-146`
  - dep: Server Actions addItem/setQty/getCartView → assertCartMember (SSR cookie auth.getUser + service-role reads)
  - behavior: Mid-session pause (menu already rendered with items, cart live): tap Add → instant optimistic morph to stepper + flash 'Added to your order' (a LIE — the write is about to fail) + CartBar count bumps → the action throws (getCallerUid 'Not signed in' 401, or the maybeSingle-null conflations: 'No such cart' 404 / 'Session is no longer active' / 'Not a member' — all network failures misclassified as
  - gap: Announce-then-retract dishonesty at the app's highest-frequency moment: SR users hear 'Added to your order' then 'Reconnecting…' with no explicit 'that didn't go through'; the visual stepper→pill melt-back is the only signal the add failed. And authz.ts's !row-throws turn network failure into false
  - fix: On the catch path flash an explicit failure first ('That didn't go through — reconnecting…'); in authz.ts destructure error separately from data and throw a distinct retryable 'service_unavailable' AuthzError for error!=null so absence and outage never share a message.
- **[MED]** `lib/realtime.ts:48-171 + components/TableCartProvider.tsx:169-183,355-383 + components/GuestList.tsx:161-199 + components/menu/ArrivalBeat.tsx`
  - dep: Realtime websocket: private presence channel table:{sessionId} (guest list) + postgres_changes on qr_carts/qr_cart_items (peer cart sync); visibilitychange → getCartView freshness backstop
  - behavior: Socket drops; supabase-js reconnects forever on its capped backoff (~every 10s) — an infinite silent reconnect loop. Channel statuses CHANNEL_ERROR/TIMED_OUT → console.error ONLY, zero UI. The guest list FREEZES at the last presence sync: 'Table 7 · Party of 3' keeps asserting live presence that is no longer live; peers' adds/removes stop arriving with no 'sync paused' indication, so two phones si
  - gap: Live-ness is claimed while dead: the presence list and peer-sync are presented identically whether the socket is up or 10 minutes gone — the exact 'no promise the code doesn't keep' violation (live guest list = a promise). The visibility-refresh swallow conflates 'cart paid/closed' (correct to swall
  - fix: Surface channel health from useGroupCart/useCartRealtime (status out, not just console.error); after N failed rejoins mark the guest list/timeline stale ('Live sync paused — reconnecting') and clear it on SUBSCRIBED; discriminate the refresh() catch (AuthzError code vs network) so only the closed-ca
- **[MED]** `apps/qr/app/(order)/menu/page.tsx:15 + app/layout.tsx:61 + lib/menu/mostLoved.ts:33-80`
  - dep: Next caching posture — why /menu might (or might not) mask the outage
  - behavior: The route shell streams 200 because the render never throws — NOT because of caching. `export const revalidate = 300` on the menu page is effectively inert for the route: the ROOT layout exports `dynamic = "force-dynamic"` (CSP-nonce requirement), making every route per-request, and the page also reads cookies (getWelcomeBack/getFavoriteIds). Next 16 fetch defaults to no-store and supabase-js pass
  - gap: Two things: (a) the misleading `revalidate = 300` line + comment implies menu caching that doesn't exist — remove or wire it deliberately (an explicit unstable_cache/'use cache' on the catalog read would ALSO be the cheap outage shield: last-good menu served while the DB is down); (b) unstable_cache
  - fix: Decide intentionally: wrap the catalog read in unstable_cache(revalidate:300) with the error arm re-throwing (stale-if-error → the menu READS survive a pause), drop the dead segment export, and make mostLoved's catch throw inside unstable_cache (or cache with a short TTL on failure).
- **[LOW]** `components/menu/MenuBrowser.tsx:114-132 (toggleHeart) + 214-225 (reorder)`
  - dep: Server Actions toggleFavorite (RLS write) and reorderOrder
  - behavior: Heart tap: optimistic flip → toggleFavorite returns null (its catch) → heart silently flips BACK with no announcement, no toast — reads as a broken button. Reorder deep-link (?reorder=): the .catch shows + announces 'Couldn't reorder just now — the menu's all yours.' — honest, already good.
  - gap: The silent heart revert gives zero feedback (visual blink only, nothing through the live region) — minor, but it's the pattern gap: every optimistic revert on this surface should say why.
  - fix: Route a one-line announce through the provider's flash on revert ('Couldn't save that favorite just now').
- **[LOW]** `components/TableCartProvider.tsx:229-267 (refresh/first load) — dine-in cold load with a still-valid cookie`
  - dep: First getCartView after a hypothetical successful mint; not reachable in a FULL outage (the mint fails first) but reachable in a partial one (auth up, PostgREST down)
  - behavior: Initial getCartView catch leaves the view permanently empty with only the settle-baseline seeded — cart renders as zero items/no totals with no failure surface (the comment scopes the swallow to 'cart paid/closed', but a network failure lands in the same arm). CartBar simply doesn't render (count 0), so a diner whose table HAS items sees an empty order — a quieter cousin of the rewards 'zeroed hub
  - gap: Absence-vs-failure conflation on the first cart load: empty-because-failed is rendered as empty-because-new.
  - fix: Discriminate the catch (AuthzError cart_closed → silent; anything else → set the provider error so the existing banner mounts).

## grocery

- **[HIGH]** `apps/qr/app/grocery/page.tsx (mint wait) ← useTableSession ← useAnonSession ← AnonAuthGate`
  - dep: Supabase AUTH: AnonAuthGate.signInAnonymously() (network → GoTrue) must succeed before useAnonSession returns non-null; useTableSession's mint effect is gated on `anon` and never runs without it
  - behavior: First-time visitor (or one whose cached anon JWT is expired and can't refresh): signInAnonymously fails, retries ONCE, then only console.errors — AnonAuthGate renders nothing and exposes no error. `anon` stays null → the mint POST never fires → session=null AND error=null forever. The page shows the bare text 'Starting your basket…' indefinitely — a pseudo-loading state with nothing in flight. Any
  - gap: An unreachable failure state: the outage is invisible and the copy makes two promises the code can't keep ('in a moment', 'opens in a moment'). Violates the sweep's 'every async UI has a loading AND a failure/recovery state' rule — this async step has only a loading state.
  - fix: Surface anon-auth failure: have AnonAuthGate publish its error (context or a store) OR give useTableSession a deadline — if `anon` is still null after ~8s, set the same sessionError path so the existing banner renders, with outage-honest copy ('We're having trouble on our end — your basket can't sta
- **[MED]** `apps/qr/app/grocery/page.tsx:525-538 (sessionError banner) ← POST /api/session`
  - dep: Mint route verifies the bearer via sessionClient(token).auth.getUser(token) — a live round-trip to paused Supabase auth — then ~10 DB reads/writes; any failure returns 401 'Invalid session' / 500
  - behavior: Visitor with a still-valid cached anon JWT: getSession() resolves locally, the mint POSTs, the route's getUser fails at the network layer → 401 'Invalid session' → sessionError set → role="alert" card: 'Couldn’t start your grocery basket — you can browse, but adding needs a connection.' + Retry (window.location.reload()). If the paused endpoint TIMES OUT instead of refusing, the diner sits in 'Sta
  - gap: Copy is dishonest twice for an outage: (a) 'you can browse' is a broken promise — the catalog read fails against the same paused DB, so Browse shows its own failure card; (b) 'adding needs a connection' blames the shopper's radio when WE are down. Retry is a full reload, which can drop the diner int
  - fix: Outage-honest copy that makes no browse promise ('We couldn’t start your basket — something's wrong on our end. Browsing may be limited too.'); Retry should call revalidate() (re-POST the mint) instead of reloading; add AbortSignal.timeout to the mint fetch.
- **[MED]** `apps/qr/components/grocery/GroceryBrowse.tsx:106-124 ← lib/grocery.ts getGroceryCatalog`
  - dep: Server Action → publicClient().from('grocery_items') select — throws 'grocery catalog read failed' on any query error (deliberately never [])
  - behavior: Skeleton grid renders, the action executes on Vercel, the Supabase fetch fails → throw → client .catch → failed=true → role="alert" card replaces the skeleton: 'Couldn’t load the aisles. Check your connection and try again.' + working Retry (re-runs the fetch). No eternal skeleton, no fake empty market — unless the paused endpoint hangs, in which case the skeleton persists for the full server-side
  - gap: The copy blames the shopper's connection — their connection is fine; our DB is paused. Also breaks the sessionError banner's 'you can browse' promise, and when BOTH fail (the outage norm) two simultaneous role="alert" regions mount (sessionError card + this card), violating the repo's one-live-regio
  - fix: Neutral/honest copy ('Couldn’t load the aisles — please try again in a moment. If it keeps happening, it’s on our end.'), optionally gate connection-blame on navigator.onLine===false; demote one of the two alerts to role="status" or collapse both into one outage banner.
- **[MED]** `apps/qr/app/grocery/page.tsx:304-380 add() toast ← lib/grocery.ts scanAdd`
  - dep: Server Action: assertCartMember (auth.getUser + 3 DB reads) → grocery_items lookup → insertOrIncLine RPC; on authz throw it re-derives via whyCartUnavailable
  - behavior: Cart id already in state (session minted pre-pause, tab open): every scan/tap add round-trips, assertCartMember throws, whyCartUnavailable returns 'unreadable' → the final else flashes 'Couldn’t add that — check your connection and try again.' for 1.8s. The camera stays open on the Scan door, so a shopper scanning shelf items gets the same blame-the-radio toast per scan. A transport-level action f
  - gap: The highest-frequency interaction on the surface answers a server-side outage with 'check your connection' — dishonest, and it invites the shopper to debug their wifi indefinitely. No escalation after repeated failures (the toast never graduates to a persistent notice).
  - fix: Split the copy: navigator.onLine===false → connection copy; otherwise 'Couldn’t add that — we’re having trouble right now, try again in a moment.' After N consecutive unreadable/transport failures, raise a persistent page-level 'We’re having trouble reaching the market' strip instead of per-scan toa
- **[LOW]** `apps/qr/app/grocery/page.tsx:781-809 truth strip ← lib/grocery.ts getGroceryLines`
  - dep: Server Action: assertCartMember → readGroceryLines (2 error-checked reads); refusals return {ok:false, reason} — during an outage always 'unreadable'
  - behavior: Hydrate on mount/refocus fails → syncFailed. Pre-hydration: 'Couldn’t check your basket.' + Retry; post-hydration (lines already on screen from before the pause): 'Couldn’t refresh your basket — what’s shown may be out of date.' + Retry. role="status", not alert. CONFIRMED: 'unreadable' is not terminal, so the 'Start a fresh basket' recovery (a data-losing re-mint) is never offered during an outag
  - gap: Minor: it never names the outage — 'Couldn’t check your basket' is safe but a paused-DB shopper gets no signal it's us vs. them, and Retry will fail silently-identically forever with no escalation. The 'may be out of date' line also can't tell the shopper adds are refused too.
  - fix: After 2-3 failed retries, upgrade the strip's copy to own the outage ('We’re having trouble on our end — your basket is safe and will reload when we’re back') and consider auto-retry with backoff instead of manual-only.
- **[LOW]** `apps/qr/app/grocery/page.tsx:246-300 stepQty ← lib/cart.ts setQty`
  - dep: Server Action: assertCartItemMember (DB read, error discarded → 'No such cart item' throw during outage) → mms_cart_item_set_qty_if_open RPC; page reconciles via getGroceryLines
  - behavior: Optimistic qty flip renders, write throws → 'Couldn’t update that — try again.' toast; reconcile read also fails ('unreadable' or throw) → the pre-flip snapshot restores (wrote=false + appliedSeq unchanged). Money-safe: the optimistic view never outlives a refused write; UI snaps back to the last server truth.
  - gap: Cosmetic only: the flip-then-snap-back reads as a glitch with a generic toast — the shopper isn't told the basket is temporarily read-only. Note also assertCartItemMember discards its read error so an outage surfaces as the 404-shaped 'No such cart item' internally (contained on this page, but a lat
  - fix: When the reconcile also fails, reuse the truth strip's outage state (setSyncFailed) so the strip explains the snap-back; add error checks to assertCartMember/assertCartItemMember reads so DB failure is never shaped as no_cart/no_item.
- **[LOW]** `apps/qr/app/grocery/page.tsx:416-445, 654-724 search ← lib/grocery.ts searchGroceryItems`
  - dep: Server Action → serviceClient().rpc('mms_grocery_search') — throws on error (deliberately never [])
  - behavior: Typing ≥2 chars → 220ms debounce → 'Searching…' → catch → searchFailed → inline hint 'Search unavailable — please try again.' Correctly distinguished from the zero-results 'No matches — try fewer letters.' No spinner-forever; each new keystroke retries naturally.
  - gap: None material. 'Please try again' is the only soft promise; acceptable. No explicit Retry button, but retyping re-fires the search.
  - fix:
- **[MED]** `apps/qr/app/grocery/page.tsx + lib fetches (mint, catalog, lines, scanAdd)`
  - dep: All Supabase calls run server-side inside actions/route handlers with NO explicit timeout; the client fetches (mint POST, action POSTs) also have no AbortSignal deadline
  - behavior: Live evidence says failures are refused/5xx/timeouts. Refused → fast honest failure states (above). But if the paused endpoint TIMES OUT, every 'loading' state stretches to the platform fetch/function ceiling: catalog skeleton for minutes, 'Starting your basket…' for minutes, a scan toast that arrives half a minute after the scan — eternal-skeleton-adjacent states that look like loading while noth
  - gap: The failure/recovery UX is only as good as the transport's willingness to fail fast. Violates 'fail fast on unrecoverable errors instead of burning a full retry budget.'
  - fix: AbortSignal.timeout(~8s) on the mint fetch; a Promise.race deadline client-side around getGroceryCatalog/getGroceryLines/scanAdd invocations (or a fetch-timeout wrapper in the Supabase server clients) that lands in the existing catch paths.

## cart-checkout-track

- **[HIGH]** `apps/qr/app/cart/page.tsx (SSR entry)`
  - dep: getCartView → assertCartMember → getCallerUid → supabase auth.getUser() (network call to Supabase Auth), then service-role PostgREST reads (qr_carts, qr_cart_items, menu_items, totals). Fallback probe
  - behavior: auth.getUser() returns an AuthRetryableFetchError (supabase-js returns it, never throws) → getCallerUid throws AuthzError('Not signed in', 401) → getCartView throws → page catch sets view=null → didIPayForCart hits the same dead auth, returns false BEFORE its .catch (fail-closed, so `mine` is always false in an outage) → the diner sees headline 'Your order' and body 'This order isn’t available on
  - gap: DISHONESTY RATING: worst on the surface. Blames the diner's DEVICE for our outage, implies the order is gone (it's intact), offers NO retry (only exit links that abandon the cart), and the recovery route ('start from the menu') would mint a fresh session against a dead backend. Root cause: the catch
  - fix: Discriminate transport failure from authz at the source: in getCallerUid/assertCartMember, detect AuthRetryableFetchError / PostgREST error objects (error.message 'TypeError: fetch failed', status 0/undefined) and throw a distinct BackendUnavailableError. cart/page.tsx catches it separately and rend
- **[HIGH]** `apps/qr/components/Checkout.tsx — continueToPayment() → POST /api/stripe/create-intent (route.ts)`
  - dep: Route: assertCartMember (auth.getUser + qr_carts/table_sessions/session_members), withinMutationRate, acquireCartLock, pickup RPCs, getCartTotals — all Supabase.
  - behavior: Auth down: assertCartMember throws AuthzError('Not signed in',401) → route returns 401 {error:'Not signed in'} → client rule `res.status < 500 && data.error` shows the body VERBATIM → the diner taps 'Continue · $42.80' and the single live region reads 'Not signed in' — meaningless to an anonymous diner who never signed in. If auth is somehow cached/up but PostgREST is down: the qr_carts read yield
  - gap: The <500 pass-through of AuthzError.message turns an outage into 'Not signed in' / 'No such cart' — dishonest, self-blaming, and actively frightening one tap before payment. No outage-aware copy, no bounded-retry escalation, no reassurance that nothing was charged.
  - fix: Route: map BackendUnavailableError (see finding 1) to 503 with a server-authored honest string; client: allowlist which 4xx bodies are shown (the intentional ones: settling 409, lock 409, slot 409, kitchen closed/full) and route 401/404/unknown to honest outage copy: 'We’re having trouble on our end
- **[HIGH]** `apps/qr/components/Checkout.tsx — review-step cart edits (changeQty / toggleFulfillment / makeNow) + refresh()`
  - dep: Server actions setQty/setLineFulfillment/makeItNow (assertCartMember + RPCs) and getCartView on every reconcile; visibilitychange + realtime-triggered refresh().
  - behavior: For a diner ALREADY on /cart when the outage starts (mid-meal — the most common case): every stepper/For-here↔To-go/Make-it-now tap applies optimistically and reflects INSTANTLY, the server action throws (redacted) into a bare catch, refresh() throws into its bare catch (commented as the expected post-pay 403 swallow), the transition ends, the useOptimistic overlay clears — and the control SNAPS B
  - gap: Eternal silent snap-back = the UI actively lies that taps did nothing wrong while discarding them. The refresh() comment concedes it can't discriminate the failure — that discriminator is exactly what's missing.
  - fix: Have refresh() (and the mutation catches) detect BackendUnavailableError-shaped failures via a typed Server Action result (mirror W9d's discriminated scanAdd pattern — Next redacts thrown errors, so return {ok:false, reason} instead of throwing). On outage: set the existing single live region to 'We
- **[MED]** `apps/qr/components/Checkout.tsx — onPromo catch`
  - dep: applyPromoAction server action (assertCartMember + mms_apply_promo RPC).
  - behavior: Action throws (redacted) → catch sets status: 'Couldn’t apply that code — check your connection and try again.' The diner's connection is fine; WE are down. Retry loops the same message.
  - gap: Blames the diner's connection for our outage — the exact 'blaming the user's radio' anti-pattern W9d retired on /grocery.
  - fix: Reword the transport catch to neutral-honest: 'Couldn’t reach us just now — hold on to the code and try again in a moment.' (or discriminate outage via typed result and say 'on our end').
- **[MED]** `apps/qr/app/track/page.tsx + components/OrderTracker.tsx + lib/useOrderStatus.ts — post-Stripe redirect (?payment_intent=pi_…&redirect_status=succeeded), diner HAS a cached anon JWT`
  - dep: SSR: none (this branch reads only searchParams — streams fine). Client: browser PostgREST read of qr_orders (RLS is_member), Realtime postgres_changes channel, then getMyOrderFallback server action (a
  - behavior: The diner just PAID (mid-payment moment). track/loading.tsx skeleton → chip 'Confirming order' (or 'Confirming payment'), rail step 1 'Confirming…' → each browser read fails fast (supabase-js returns error object), 3 consecutive errors 3s apart → exhausted ≈6–9s → timedOut → getMyOrderFallback: auth.getUser fails server-side → user null → returns {ok:false, reason:'not_found'} (NOT 'error') → bann
  - gap: Three cracks: (1) 'Find it in your account' is a dead-end promise — /account is equally down, and the fallback couldn't even distinguish 'auth down' (returned not_found instead of error) so the UI can't know to suppress it; (2) copy never admits fault direction ('on our end') and 'taking longer than
  - fix: getMyOrderFallback: return reason:'error' when getUser fails with a retryable/network error (reserve 'not_found' for a real empty read); OrderTracker: on reason:'error', switch the banner to outage copy ('We’re having trouble on our end. Your payment is confirmed and safe — this screen will catch up
- **[HIGH]** `lib/useOrderStatus.ts + components/AnonAuthGate.tsx — /track with NO usable anon session (fresh device, cleared storage, or expired JWT that can't refresh — Supabase Auth is down)`
  - dep: AnonAuthGate signInAnonymously() (Supabase Auth), useAnonSession getSession/onAuthStateChange; useOrderStatus's entire effect is gated on `anon` being non-null.
  - behavior: signInAnonymously fails, one retry fails → console.error only, renders nothing → useAnonSession stays null → useOrderStatus's effect NEVER RUNS: no load, no error counting, `exhausted` never latches → timedOut stays false (the key is valid) → the tracker sits at chip 'Confirming order' + rail 'Confirming…' ETERNALLY. No banner, no Refresh, no timeout, no live-region update — a permanent fake-loadi
  - gap: The eternal-skeleton class of dishonesty: 'Confirming…' claims active work that is not happening and can never complete. The no-session path is the one path with no timeout at all.
  - fix: In useOrderStatus, start a ~10–12s deadline when key is valid but anon is still null; on expiry latch exhausted (timedOut → the existing banner + Refresh renders). Optionally have AnonAuthGate expose a mint-failed flag (context) so /track can show the honest outage variant immediately.
- **[MED]** `apps/qr/app/track/page.tsx — split-tender arm (?cart=…&paid=1) and HomeResumeCard/LiveOrderRow deep links`
  - dep: SSR: getSplitOrderId → getCallerUid().catch(null) + service reads of qr_cart_shares/qr_carts (assertSessionMember).
  - behavior: getCallerUid catches to null; the qr_carts read returns data:null → getSplitOrderId returns null (no throw) → SSR renders the finalizing card: '🫖 Payment received / Your share is in — we’re finalizing the table’s order. Check back in a moment.' + Refresh (replace). Every refresh re-runs the same failing reads and re-renders the identical card, indefinitely. A split payer who just paid their share
  - gap: Fabricated activity + broken time promise during an outage: 'we’re finalizing' (nothing is running) and 'Check back in a moment' (could be an hour). No escalation after N refreshes. Cannot easily discriminate outage here because the same generic card deliberately answers non-member probes (the W9c o
  - fix: Soften to promise-free truth that is valid for BOTH the race and an outage (keeps the oracle sealed): 'Your share is in. It can take us a little while to pull the table’s order together — this page will have it when it’s ready.' Consider a client-side visited-count (sessionStorage) that after ~3 ref
- **[LOW]** `/track/<uuid> (no app/track/[id]/ route exists; no not-found.tsx anywhere in apps/qr/app)`
  - dep: None — pure routing. Confirmed live: 404s.
  - behavior: Next.js default unbranded 404 ('404 | This page could not be found') — identical with the DB up or down. No brand voice, no tokens, no way back, no door-picker link. All real tracker links are query-param shaped (/track?payment_intent=…, /track?cart=…&paid=1 — HomeResumeCard, liveOrderTrackHref), so this only bites hand-edited/guessed URLs today — but it is the app's answer for EVERY unknown path.
  - gap: An unbranded dead end on a diner-visible surface; violates the brand/tokens/a11y bar and offers no recovery route.
  - fix: Add app/not-found.tsx: branded card ('We couldn’t find that page'), door-picker link via menuHref(null)/menuLinkText(null), tokens + focus-on-heading per QA §A. Optionally a track/[id]/page.tsx that redirects to /track.
- **[LOW]** `apps/qr/components/Checkout.tsx pay step (clientSecret already minted pre-outage) + PaymentSection.tsx`
  - dep: Stripe only for confirm (UP in this scenario); releasePayLock server action + refresh() on 'Edit order'.
  - behavior: The diner CAN still pay: confirmPayment talks to Stripe, succeeds, redirects to /track (see /track findings) — money captured, fulfillment deferred to webhook retries (safe: intent amount was server-derived pre-outage). 'Edit order' / 'Back to review': releasePayLock throws (caught, non-fatal comment), refresh() fails silently → back on review with pre-outage data and the silent-snap-back state of
  - gap: Minor: returning to review lands the diner in the silent-snap-back cart with no hint the backend is down; nothing tells them 'paying still works' (arguably the one action that DOES work).
  - fix: Covered by finding 3's outage discriminator: when review-step refresh detects outage, keep the pay CTA enabled with a caption that totals shown are as-of the last sync (they are server-derived at mint time anyway).
- **[LOW]** `apps/qr/components/SettlementBoard.tsx (split view inside Checkout) + cart page settling-no-split arm`
  - dep: getSettlement server action + useSettlementRealtime (qr_cart_shares postgres_changes); create-share-intent route for share payment.
  - behavior: First mount mid-outage: typed load failure → 'Couldn’t load the split.' + 'Try again' (loops but honest-neutral). Already-loaded board: refresh failures → 'Couldn’t refresh — showing the last update.' + Try again — an explicitly-labeled frozen snapshot (good). Realtime reconnect-loops silently. Paying a share hits create-share-intent → same AuthzError-verbatim risk class as create-intent. The /car
  - gap: Small: retry loops with no escalation; share-pay boundary shares finding 2's verbatim-4xx problem; the honest SSR settling arm is shadowed by finding 1's dishonest catch.
  - fix: Inherit findings 1–2's fixes (outage discriminator + create-share-intent copy allowlist); no board-specific work needed beyond an outage-worded variant of the existing failure line.
- **[LOW]** `Caching audit for this surface (/cart, /track)`
  - dep: None to audit out: both pages are fully dynamic (cookies() via auth + awaited searchParams force per-request render); no unstable_cache / 'use cache' / revalidate / force-static anywhere under app/car
  - behavior: Nothing on this surface is masked by caching. The shells still stream 200 because every failure is CAUGHT into fallback JSX (page-level try/catch, .catch defaults like getPrepMinutes→12), not because stale data is served. What LOOKS like masking is the client-side last-known state: a mounted Checkout keeps rendering pre-outage items/totals as if current while every refresh silently fails (finding
  - gap: None (informational) — the outage-masking on this surface is swallowed-error state-freezing, not Next caching.
  - fix: None for caching; the state-freeze is addressed in finding 3.

## staff

- **[HIGH]** `apps/qr/lib/staff.ts (getStaffAuth, lines 50-101) — the root cause behind every staff surface`
  - dep: serverClient(cookies).auth.getUser() — a network round-trip to Supabase Auth on EVERY staff request (pages via requireStaffPage, polls/actions via requireStaff)
  - behavior: VERIFIED in @supabase/auth-js 2.108.2 dist: a connection-refused/timeout/5xx from the auth endpoint becomes AuthRetryableFetchError, which \_getUser CATCHES and returns as { data: { user: null }, error } — it does not throw. getStaffAuth destructures only `user` and DISCARDS `error` (line 52-54), so every outage resolves to { kind: "anon" }. The entire staff console therefore experiences a DB pause
  - gap: An infrastructure outage is indistinguishable from a signed-out user at the single choke point every staff surface gates through. There is no fourth discriminant. Everything downstream (login redirect, "Staff sign-in required." action errors, board redirects) inherits the lie.
  - fix: Add { kind: "unavailable" } to StaffAuth when getUser returns an error whose name is AuthRetryableFetchError (or status 0/5xx), and thread it through: requireStaffPage → render a staff outage shell (not a login redirect); getKitchenQueue/getExpoQueue → { ok:false, reason:"outage" }; requireStaff → a
- **[HIGH]** `apps/qr/components/staff/KdsBoard.tsx (refresh, lines 155-200) + components/staff/ExpoBoard.tsx (refresh, lines 32-52)`
  - dep: 5s setInterval + realtime-debounced calls to the getKitchenQueue()/getExpoQueue() server actions
  - behavior: What a cook sees mid-service: within ≤5 seconds of the pause, the first poll returns { ok:false, reason:"signin" } (requireStaff→AuthzError caught in lib/kitchen.ts:74-79 / lib/expo.ts:32-37) and the client executes window.location.assign("/staff/login") (KdsBoard.tsx:163, ExpoBoard.tsx:38). The ENTIRE live ticket queue — every in-flight dish, elapsed timers, held cards, the recall rail — is torn
  - gap: The redirect destroys the one thing the outage UX needs most — the last-known ticket state on the always-on display. Honest behavior is the inverse: keep the frozen queue on screen, banner it as frozen-with-timestamp ("showing tickets as of 2:41 — new paid orders may not appear"), auto-retry, and on
  - fix: With the reason:"outage" discriminant from staff.ts: keep snap, set an outage banner distinct from stale ("We can't reach the ordering system — tickets frozen at <time>; orders paid during this window will appear when we're back"), keep polling, escalate copy after N minutes. Reserve the login redir
- **[HIGH]** `apps/qr/components/staff/FloorBoard.tsx (refresh catch, lines 85-92) — /staff floor home`
  - dep: 5s poll → getFloorView() server action (lib/floor.ts:63 — bare await requireStaff(), throws)
  - behavior: getFloorView throws (AuthzError → the action promise rejects on the client) and the catch arm only console.errors. The floor board freezes SILENTLY and PERMANENTLY: no banner, no redirect, no staleness signal of any kind — FloorBoard is the ONLY staff board with no stale state at all. Worse, it actively looks live: the role=status region keeps asserting "N active tables", and every card's Relative
  - gap: Missing the fails≥2 stale banner its three sibling boards have (KdsBoard/ExpoBoard/ApprovalsBoard/FloorDetailLive), and the ticking relative-time labels + confident live region make a frozen board indistinguishable from a quiet one. Also FloorBoard never receives the signin/locked discriminant at al
  - fix: Discriminate getFloorView like the queues ({ ok:false, reason }), add the fails counter + an outage/stale banner to FloorBoard, freeze or annotate RelativeTime while stale ("as of 2:41"), and dim/timestamp the frozen cards.
- **[HIGH]** `apps/qr/components/staff/StaffLogin.tsx (sendCode lines 78-121, google lines 64-76, verify lines 123-140)`
  - dep: browserClient().auth.signInWithOtp / signInWithOAuth / verifyOtp — Supabase Auth is down too, so no session can be established for the entire outage
  - behavior: This is where every staff member lands (SSR redirect or board self-redirect). (1) Typing their staff email → signInWithOtp fails with AuthRetryableFetchError (status 0/5xx ≠ 429) → copy: "We couldn't send a code to that email. Check it's your staff address and try again." — blames THEIR address for OUR outage; they will retype it and retry into the same wall. (2) "Continue with Google": PKCE signI
  - gap: The login form is the outage's dead-end surface and every one of its error strings is dishonest for it (violates the sweep's "honest microcopy" bullet: blaming the user's email/code/connection when WE are down). The Google path is worse than copy — it physically strands the shared tablet off-app.
  - fix: Branch on err.name === "AuthRetryableFetchError" / status 0 or ≥500 in all three handlers → one honest state: "Our ordering system can't be reached right now — this isn't your account. We'll keep trying." with auto-retry; guard the Google button behind the same signal (or preflight a cheap fetch to
- **[HIGH]** `apps/qr/app/staff/page.tsx · kitchen/page.tsx · expo/page.tsx · orders/page.tsx · approvals/page.tsx · feedback/page.tsx · team/page.tsx · profile/page.tsx · table/[id]/page.tsx · lock/page.tsx (every SSR entry)`
  - dep: requireStaffPage() → getStaffAuth() → auth.getUser() network call, before any page content
  - behavior: Every navigation to any staff route: loading.tsx skeleton flashes (~1-2s at observed failure latencies), then redirect("/staff/login") → the plain sign-in form (denied=false, since auth resolves anon not not_staff). No redirect LOOP occurs — /staff/login's own getStaffAuth also resolves anon so it renders the form rather than bouncing back (the loop question is settled: it's a one-way funnel, not
  - gap: No SSR-level outage state exists or is reachable. The infrastructure signal is destroyed at getStaffAuth before any page could branch on it. A staff-voiced outage shell ("Our system is unreachable — your tickets are safe in the kitchen's paper trail; this screen will recover itself") is unbuildable
  - fix: requireStaffPage: on kind "unavailable", render (not redirect to) a dedicated staff outage screen with auto-refresh — keeping the URL so recovery lands the person back on the board they were on. Add app/staff/error.tsx as the backstop with staff-voiced copy.
- **[MED]** `apps/qr/lib/kitchen.ts (getKitchenQueue lines 115-134) + lib/expo.ts (getExpoQueue lines 44-53) + lib/floor.ts (getFloorView lines 68-77) — the empty-queue-as-truth class`
  - dep: serviceClient() PostgREST reads; VERIFIED in postgrest-js 2.108.2 (PostgrestBuilder.ts:390-446): with shouldThrowOnError=false (no .throwOnError() anywhere in apps/qr) a fetch rejection RESOLVES to {
  - behavior: Reachable in the partial-outage shape (PostgREST down/5xx while an auth check passes — pause/restore staging, PostgREST crash, DB overload): getKitchenQueue's `if (!lines …) return { ok: true, queue: empty }` renders the KDS as "All clear" / "Nothing on the line" WITH ok:true — a kitchen showing zero tickets while paid orders exist. getExpoQueue → "No bags waiting" / "Nothing to bag". getFloorView
  - gap: Read failure and genuinely-empty are conflated at every queue read, converting outages into false "all done" states. Violates the sweep's own rule: every { error } handled or a commented deliberate swallow — these are uncommented and load-bearing.
  - fix: Check `error` on the first (anchor) read of each queue fn and return { ok:false, reason:"unavailable" } so boards keep their last-known snapshot + outage banner instead of painting a false empty state. Same for getFloorView's sessions read.
- **[MED]** `apps/qr/lib/floor.ts (getTableDetail lines 197-210) + components/staff/FloorDetailLive.tsx (refresh lines 76-96) + app/staff/table/[id]/page.tsx (lines 19-34)`
  - dep: table_sessions .maybeSingle() read; poll every 5s from the table drill-down
  - behavior: Full pause: requireStaff throws → FloorDetailLive catch → after 2 fails (~10s) shows "Reconnecting — showing the last known order" and keeps the frozen detail — the best current behavior on the surface. BUT the partial-outage arm fabricates a table clear: if the auth check passes and only the table_sessions read fails, `!session` → returns null → the client executes router.replace("/staff") (silen
  - gap: null conflates "session row absent/closed" with "read failed"; "Reconnecting…" implies a transient blip and never escalates, offers no manual retry, and never states what's actually wrong.
  - fix: Discriminate getTableDetail: { ok:true, detail } | { ok:true, closed:true } | { ok:false, reason:"unavailable" }. Only `closed` renders the closed-table page / triggers the floor bounce. Escalate the stale banner after ~60s to honest outage copy with a frozen-at timestamp.
- **[MED]** `apps/qr/components/staff/PinUnlock.tsx (submit lines 39-76, signOut lines 78-87) — the locked shared tablet`
  - dep: unlockConsole server action (requireStaff → auth.getUser) + browserClient().auth.signOut() (network POST /logout)
  - behavior: A locked tablet is a client-side trap for the whole outage: the correct PIN returns { ok:false, reason:"error" } (staff-pin-actions.ts:85 — requireStaff nulls before the PIN is ever checked) → "Couldn't check that PIN. Try again." — an invitation to retry forever against a wall (at least no lockout is burned: verifyStaffPin never runs). The escape hatch "Forgot PIN? Sign out" fails too — signOut r
  - gap: Both copy strings are dishonest for an outage ("Try again" / "check your connection") and the screen offers no truthful third state. A cook who locked the tablet before break returns to a device that gaslights them.
  - fix: Give unlockConsole an "unavailable" reason (from the staff.ts discriminant) → "We can't reach the system to check your PIN — this isn't you. Retrying automatically." with auto-retry; on signOut network failure say the system is unreachable, not their connection.
- **[MED]** `apps/qr/lib/kitchen.ts · expo.ts · floor.ts · staff-cart.ts · staff-pin-actions.ts · voids.ts · refunds.ts — every staff mutation's requireStaff().catch(() => null) arm (17 occurrences of "Staff sign-in required." across 6 files)`
  - dep: Server Actions: bumpLine/bumpTicket/recallTicket/fireTicketNow/staffFireCart, setTogoStatus, clearTable/mergeTables, staff cart add/edit, setPin/removePin/lockConsole, void/comp/refund + manager PIN s
  - behavior: Any tap that races the first failed poll (or happens on the frozen floor, which never redirects) returns { ok:false, error: "Staff sign-in required." } rendered inline — on a screen where the user is visibly signed in, mid-shift. LockButton shows it as a role=alert beside the Lock control (LockButton.tsx:37-41); FloorDetailLive shows it in the shared write-error region; KDS bump taps mostly never
  - gap: "Staff sign-in required." is the wrong explanation and implies a fix (re-login) that cannot work during the outage; "Try again." burns retries against a dead backend with no honest ceiling. Neither tells the cook the truth that matters operationally: your tap was NOT saved — track it on paper.
  - fix: Thread the outage discriminant into the shared catch arms: "We can't reach the ordering system — that change wasn't saved." One copy string, defined once, reused by all 17 sites.
- **[LOW]** `apps/qr/components/staff/ApprovalsBoard.tsx (lines 47-67, 102-106) + app/staff/page.tsx countPendingApprovals badge + lib/staff-pin.ts staffHasPin + app/staff/feedback/page.tsx + lib/staff.ts listStaff (team roster)`
  - dep: 5s poll → listPendingApprovals (throws on auth failure); count/hasPin/feedback/roster reads all `data ?? []` / error→false
  - behavior: ApprovalsBoard (full pause): throw → catch → "Reconnecting — showing the last known list" after ~10s — works, same never-escalating "Reconnecting" fiction. Partial-outage arms: countPendingApprovals error→0 silently drops the "Approvals (N)" badge off the floor nav; staffHasPin error→false makes /staff render "Set a tablet PIN →" to someone who has one (and hides LockButton); feedback renders "No
  - gap: Same read-error/empty conflation class as the queues, on lower-stakes surfaces; the "Reconnecting…" wording never admits a sustained outage.
  - fix: Cover under the same discriminated-read sweep; add a minutes-elapsed escalation to the shared stale banner copy.
- **[LOW]** `apps/qr/lib/useFloorRealtime.ts (lines 42-99) — the staff realtime channel (floor, KDS, expo, table detail)`
  - dep: supa.auth.getSession() (local, may attempt token refresh) → realtime.setAuth → websocket to the paused project + postgres_changes subscription
  - behavior: Token still locally valid (≤1h old): subscribe fires, the websocket to the dead host fails → CHANNEL_ERROR/TIMED_OUT is only console.error'd; realtime-js's Phoenix socket then retries reconnection on its backoff schedule INDEFINITELY — a silent background reconnect loop hammering the paused endpoint for the whole outage (network/battery churn on always-on tablets), invisible to the user. Token exp
  - gap: Channel state is invisible to the boards — it could be a cheap, honest early-warning input (socket down + polls failing = outage, not session expiry) but is currently only a console line. Minor: unbounded background reconnects during a known-long outage.
  - fix: Expose channel status from the hook (onStatus callback) so boards can fold it into the outage banner; optionally cap/pause reconnects while the poll-level outage state is active.
- **[LOW]** `apps/qr/components/staff/StaffSignOut.tsx (lines 13-17) + app/error.tsx (staff has no boundary of its own) + timeout posture`
  - dep: auth.signOut() network POST; nearest error boundary above /staff is the root app/error.tsx; no Supabase call anywhere on the staff path sets a fetch timeout/AbortSignal
  - behavior: (a) StaffSignOut ignores the signOut() result entirely and navigates to /staff/login regardless — during the outage the local cookie session SURVIVES (auth-js keeps it on retryable errors), so a "signed out" shared tablet is silently still authenticated after restore: the next person to visit /staff is the previous member, an attribution gap on a shared device. (b) If anything under /staff ever th
  - gap: Sign-out failure swallowed on a shared device; no staff-voiced error boundary; no fetch timeout/watchdog on the poll loop (a hang, unlike a refusal, currently has NO recovery path at all).
  - fix: Check signOut error (honest copy + keep the button); add app/staff/error.tsx; wrap board refreshes in an AbortSignal.timeout (~8s) so a hang degrades into the normal fails/stale path instead of a frozen inFlight lock.

## money-webhooks

- **[HIGH]** `apps/qr/lib/split-settle.ts (onShareAuthorized / onShareCaptured / onShareFailed / onShareCanceled / cartIdForPi) + app/api/stripe/webhook/route.ts split branches`
  - dep: Service-role writes/reads on qr_cart_shares + qr_carts inside the Stripe webhook's split-tender event handlers (amount_capturable_updated, succeeded, payment_failed, canceled)
  - behavior: Every DB call resolves to { data:null, error } — NEVER throws (verified in vendored postgrest-js 2.108.2: fetch rejections are caught and returned as error objects, status 0). onShareAuthorized: the status update's error is ignored (result not destructured), cartIdForPi swallows its error → returns null → the helper returns void → the webhook 200-ACKs. Same shape in onShareCaptured (returns null →
  - gap: Silent { error } swallow on every split DB write/read inside a webhook whose only durability mechanism is a non-2xx → Stripe retry. A DB outage converts retryable events into permanently lost money-state transitions. This is exactly the CLAUDE.md Pre-PR sweep class ('every { error } handled or a com
  - fix: In split-settle.ts, destructure and THROW on `error` for every qr_cart_shares/qr_carts read+write (and make cartIdForPi throw on error, returning null only for a genuine no-row), so the webhook's existing try/catch 500s and Stripe redelivers for 72h — making the docstring true. Add a reconciler arm
- **[HIGH]** `apps/qr/lib/totals.ts (getCartTotals) + app/api/stripe/webhook/route.ts payment_intent.succeeded single-pay branch`
  - dep: qr_cart_items select + mms_promo_discount / mms_reward_discount RPCs inside getCartTotals; qr_orders `existing` + qr_carts `cartRow` reads in the webhook
  - behavior: A diner who reached the pay step BEFORE the pause holds a mounted Payment Element + clientSecret; stripe.confirmPayment talks only to Stripe, so THE CHARGE SUCCEEDS DURING THE OUTAGE. The succeeded webhook then: `existing` read error silently → null; `cartRow` read error silently → null (cross-tender guard skipped); getCartTotals does NOT throw — its three reads all destructure only { data }, so a
  - gap: getCartTotals treats an unreadable cart as an EMPTY cart (zeros) instead of failing. In the webhook that misroutes an outage into the tamper/mismatch branch (wrong triage signal, wrong refunds_needed reason). In create-intent, a PARTIAL failure is worse: if the items read succeeds but mms_promo_disc
  - fix: Surface errors in getCartTotals: destructure { data, error } on all three reads and throw (or return a discriminated failure) on any error — making the webhook's existing 'Totals lookup failed; will retry' 500 path live and making create-intent fail the mint (500 → honest client retry) instead of mi
- **[HIGH]** `apps/qr/app/cart/page.tsx (SSR entry to the whole money path)`
  - dep: getCartView → assertCartMember → serverClient(cookies).auth.getUser() (Supabase Auth, network) + service-role qr_carts/table_sessions/session_members reads; then didIPayForCart fallback (also DB)
  - behavior: getCartView throws (auth.getUser returns an AuthRetryableFetchError → AuthzError 401), the catch-all sets view=null, didIPayForCart also fails → .catch(false) → mine=false. Every diner with a live, full cart — mid-meal, about to pay — is shown: 'This order isn't available on this device. Start from the menu.' with only a door-picker link. No retry affordance, no acknowledgment that we are down. Th
  - gap: DISHONEST COPY, verbatim the class the brief names: the order IS available — our backend isn't. The catch-all can't tell 'not a member' from 'our infrastructure is unreachable', so it asserts the former. A diner may walk out believing their order vanished, or re-order at the counter (double food).
  - fix: Discriminate infrastructure failure from authz refusal (e.g. getCartView returns a typed result; or catch AuthzError separately from transport errors — a non-AuthzError throw / an AuthRetryableFetchError-driven 401 means US, not them). Outage branch: honest copy ('We're having trouble reaching your
- **[HIGH]** `apps/qr/lib/authz.ts (getCallerUid) → app/api/stripe/create-intent, create-share-intent, setup-intent → components/Checkout.tsx continueToPayment, SharePay.tsx mint effect, SecureTabButton.tsx`
  - dep: supabase auth.getUser() network verification before every payment-intent mint
  - behavior: auth.getUser() fails at the network layer → getCallerUid throws AuthzError('Not signed in', 401) → all three Stripe routes return 401 with that body. All three clients deliberately pass 4xx server copy through (res.status < 500 && data.error): the checkout CTA's live region shows 'Not signed in', SharePay shows 'Not signed in' + Try again, SecureTabButton shows 'Not signed in'. Matches the live ev
  - gap: 'Not signed in' is dishonest and alarming for an anonymous diner who has been ordering all evening — the truth is our auth service is unreachable. AuthzError conflates 'no session cookie' with 'auth backend down'; no route can currently express 'we are down, retry'.
  - fix: In getCallerUid, branch on the auth error class: a network/5xx AuthRetryableFetchError → throw a new AuthzError('We're having trouble on our end — give it a moment and try again.', 503, 'backend_unreachable') so routes answer 503 and clients fall into their generic-but-honest retry copy (or add a de
- **[MED]** `apps/qr/components/SharePay.tsx (ShareForm) + SettlementBoard.tsx`
  - dep: Post-authorize board re-sync: onAuthorized → load() → getSettlement Server Action; row flip via qr_cart_shares realtime/5s poll
  - behavior: A payer whose Element was mounted pre-outage CAN authorize (Stripe is up — a real hold lands on their card). onAuthorized fires, the board load fails, and ShareForm intentionally 'keeps the spinner — the row flips to Authorized on the next board sync' — which never comes: 'Authorizing…' spins forever with money held. The board itself degrades honestly ('Couldn't refresh — showing the last update.'
  - gap: Unbounded spinner on a money action that DID succeed at the card network — the one state the board's honesty work doesn't cover. The diner can't tell 'my authorization failed' from 'their backend is down'; some will re-tap or hand the phone to the host.
  - fix: Bound the post-confirm wait (~20–30s): if the board hasn't flipped, replace the spinner with honest copy — 'Your card is authorized (no charge yet — nothing is captured until the whole table is in). We're having trouble updating the board; it'll catch up.' Keep the form unsubmittable (the PI is alre
- **[MED]** `apps/qr/components/RewardField.tsx (apply / remove)`
  - dep: applyReward / clearReward Server Actions (throw under outage — assertCartMember rejects before any discriminated result is produced)
  - behavior: apply(): `await applyReward(...)` rejects → no try/catch → setBusy(false) and setError are skipped → busy latches TRUE: every coupon button disables forever, no error text, unhandled promise rejection. remove(): same — the Remove button shows '…' permanently. Silent dead controls on the reward money path; only a full reload recovers.
  - gap: Missing catch on both async handlers → stuck aria-busy state with no failure/recovery state, violating the repo's own 'loading AND failure state everywhere' rule. Copy gap: even the reachable 'error' reason says 'please try again' with no outage attribution.
  - fix: Wrap both awaits in try/catch/finally (finally → setBusy(false)); on catch set an honest line ('Couldn't reach us to apply that reward — your Stars are safe. Try again in a moment.').
- **[MED]** `apps/qr/components/Checkout.tsx onPromo catch (line ~537)`
  - dep: applyPromo Server Action (throws pre-result under outage: assertCartMember → getCallerUid)
  - behavior: The catch shows 'Couldn't apply that code — check your connection and try again.' — blaming the diner's connection when their radio is fine and OUR backend is down. Retry works (button not latched), live region announces.
  - gap: Dishonest attribution — the exact 'blaming the user's connection' anti-pattern the outage brief names. Same string pattern likely repeated on other catch paths (grocery page per cart-failure.ts's own comment).
  - fix: Neutral-honest transport copy: 'We couldn't reach our kitchen systems just now — the code's still good, try again in a moment.' Consider a shared outage-copy helper so every catch on the surface says the same true thing.
- **[LOW]** `apps/qr/app/api/stripe/webhook/route.ts payment_intent.payment_failed branch`
  - dep: releaseCartLock / releaseSettlement service-role updates
  - behavior: Both releases fail and are explicitly swallowed (.catch(() => {})) → the handler 200-ACKs → Stripe never redelivers → a declined payment leaves the pay-window lock / settle freeze in place until the TTL (5 min lock / 10 min settle). Moot-ish during a full outage (every diner surface is down anyway), but the event is permanently lost, so a decline landing in the outage's last seconds still costs th
  - gap: Marginal: the deliberate swallow predates the outage lens; a 5xx here would get Stripe to redeliver the release for free. Low practical impact given the TTLs.
  - fix: Optionally return 500 when BOTH releases report errors (idempotent on redelivery). Not urgent — document as accepted if kept.
- **[MED]** `apps/qr/app/track/page.tsx (paid=1 split arm) + components/OrderTracker.tsx timedOut banner`
  - dep: getSplitOrderId (SSR, .catch(null)); useOrderStatus browser reads of qr_orders (3 consecutive failures → timedOut) + getMyOrderFallback Server Action; realtime channel to qr_orders
  - behavior: Single-pay redirect (/track?...&redirect_status=succeeded): useAnonSession resolves from LOCAL storage (getSession is offline-safe) so the tracker runs; each read fails (with postgrest GET retries: ~7s per attempt, 3 attempts + 3s gaps ≈ 20–30s of 'Confirming payment…') → timedOut → the W9c banner: 'This is taking longer than usual / Your payment went through; we just can't reach the order from th
  - gap: No outage-aware degradation: the 'moment'/'refresh to check' vocabulary assumes a transient blip; after N failed refreshes the page should escalate to 'we're having trouble on our end — your payment is safe, show this screen at the counter' + the order code/PI tail as a human-verifiable receipt toke
  - fix: Track consecutive-failure count (or a shared backend-health signal) and swap to an outage state: payment-confirmed framing, a show-staff receipt affordance (amount + PI suffix from the redirect params — client-known data, no fabrication), retry with backoff.
- **[LOW]** `apps/qr/components/SettlementBoard.tsx poll + apps/qr/lib/realtime.ts (useCartRealtime / useSettlementRealtime / useGroupCart)`
  - dep: 5s setInterval → getSettlement Server Action (each = auth.getUser round-trip + reads); realtime websocket channels (qr_carts, qr_cart_items, qr_cart_shares, presence)
  - behavior: The board polls every 5s indefinitely with no backoff — each tick a full failed Server Action — while realtime-js separately reconnect-loops the socket. Checkout's useCartRealtime only console.errors CHANNEL_ERROR/TIMED_OUT and relies on the SUBSCRIBED self-heal (good design for recovery: a re-subscribe after restore triggers a refresh). UI stays honest throughout (frozen-board banner). Cost: phon
  - gap: No backoff/circuit-breaker on the poll during sustained failure; no user-facing signal distinguishing 'their backend is down' from 'my wifi'. Minor, but the pattern multiplies across staff boards too.
  - fix: Exponential backoff (5s → 30s cap) after consecutive load failures, reset on success; optionally surface 'reconnecting' state once failures exceed a threshold.
- **[LOW]** `apps/qr/app/api/cart/release-lock/route.ts + Checkout pagehide beacon`
  - dep: assertCartMember (auth + reads) before releaseCartLock
  - behavior: The beacon's authz fails → AuthzError → deliberate 204 (oracle-sealing) → the lock is never released; sendBeacon discards the response so nothing can react. TTL (5 min) is the designed backstop. Fine during a full outage (nobody else can edit either), listed for completeness.
  - gap: None material — behavior is within the designed TTL envelope.
  - fix: None needed.
- **[LOW]** `apps/qr/app/api/stripe/webhook/route.ts (setup_intent.succeeded + charge.refunded branches) — positive control`
  - dep: mms_secure_tab RPC; refunds.list (Stripe) + mms_record_refund/mms_apply_refund_reconcile RPCs
  - behavior: Both branches CHECK their RPC errors and return 500 → Stripe redelivers up to 72h → self-heal after restore. RPC calls are POSTs (no postgrest retry) so they fail fast; error objects are surfaced. These are the correct pattern the split branches should match. Residual: >72h pause loses them too (manual Stripe replay).
  - gap: None in-branch; the 72h Stripe retry ceiling vs. an indefinite pause is a platform-level residual worth a runbook note (Stripe dashboard 'resend' after restore).
  - fix: Ops runbook: after any pause/restore, replay failed webhook deliveries from the Stripe dashboard and run a shares-vs-orders sweep.

## design-primitives

- **[HIGH]** `apps/qr/app/(order)/menu/page.tsx (line ~109) + components/menu/MenuBrowser.tsx (lines 617-626)`
  - dep: RSC read of menu_items via publicClient (PostgREST) — `const { data } = await db.from("menu_items").select(...)`; the `error` half of the response is never destructured
  - behavior: supabase-js returns { data: null, error } without throwing; `data ?? []` renders MenuBrowser with items=[], which shows the search-empty state: 'Nothing matches' + 'The menu catalog is empty' — no retry, no error styling. ISR (`export const revalidate = 300`) masks this for up to 5 min by serving the cached page; the first revalidation MISS during the outage bakes the dishonest empty menu INTO the
  - gap: The flagship surface lies during an outage: 'The menu catalog is empty' reads as 'the restaurant has no menu'. RUBRIC #9 requires a designed error state with specific retry; none exists here. No branded full-surface outage state exists anywhere to branch to.
  - fix: Branch on `error` → render the new branded OutageState (full-page variant) with retry, instead of MenuBrowser. Keep ISR as deliberate graceful degradation (stale menu > no menu) but never cache the error branch (throw inside the fetch or return dynamic on error so the empty lie can't enter the ISR c
- **[HIGH]** `apps/qr/app/grocery/page.tsx:322,376 · components/grocery/GroceryBrowse.tsx:110 · components/PickupWhenChoice.tsx:66 · components/staff/PinUnlock.tsx:82`
  - dep: Client-side Server Actions / fetches (scanAdd, getGroceryLines, aisle load, ASAP switch, staff sign-out) failing at transport level
  - behavior: Every transport failure shows 'check your connection and try again' / 'Check your connection and try again' — while the user's wifi is fine and WE are down. User toggles airplane mode, moves around the store, retries, self-blames. Retry re-fails with the same blame copy forever.
  - gap: Copy asserts a cause the code cannot know. There is no health probe and zero navigator.onLine usage in the entire app (verified by grep), so no code path can distinguish 'your wifi' from 'our kitchen'. Violates the repo's own honest-microcopy bar.
  - fix: Add /api/health (edge, DB-less, sub-second) + a `useConnectionTruth` hook (navigator.onLine + probe on failure) returning you-offline | we-down | unknown; route all failure copy through a shared bilingual copy matrix — 'check your connection' allowed ONLY when navigator.onLine === false; probe-reach
- **[HIGH]** `apps/qr/app/ (no not-found.tsx anywhere — verified by find) · /track/<uuid> route shape`
  - dep: Any unmatched deep link (e.g. /track/<uuid> — track only exists as /track?payment_intent=…) and any notFound() throw during an outage
  - behavior: Default unbranded Next.js 404 — confirmed live ('/track/<uuid> 404s'). A post-payment diner who mangles/retypes the tracking link during the outage sees a cold '404 This page could not be found' and reads it as 'my order is gone', with no route back to /track, /account, or the menu.
  - gap: The only unbranded, voice-less screen in the app sits on the worst possible journey moment (post-payment). RUBRIC J-G: 'no dead ends anywhere on the path; every failure names its way out.'
  - fix: Add branded app/not-found.tsx: 🫖 medallion, bilingual 'We can't find that page', honest post-payment reassurance ('If you just paid, your order is safe — find it in Rewards & account'), links to /track, /account, menu. Zero DB reads so it renders during the outage.
- **[MED]** `apps/qr/app/error.tsx + app/global-error.tsx`
  - dep: Catches any RSC/render throw (dine-in force-dynamic occupancy read, cart split context, staff board initial reads) — during the outage these boundaries ARE the de-facto outage screen
  - behavior: Diner sees 'This screen didn't load / Something went wrong on our end. Your order is safe — try again.' Tapping Try again re-runs the same dead read → the same screen, with no cooldown, no escalation, no 'we're having trouble' acknowledgment after N failures, and no alternate exit (menu/home link).
  - gap: EN-only (the v7.2 prototype is bilingual EN/MY on every state, incl. errors); copy never distinguishes a persistent outage from a one-off bug; reset() loops with no honesty escalation and no secondary way out. 'Your order is safe' is claimed unconditionally — fine for a paused DB (data persists) but
  - fix: Rebuild both boundaries on the OutageState primitive: bilingual title, escalating copy after 2 failed resets ('Still down — our kitchen system isn't reachable. Your order and rewards are saved; please see a server'), secondary link home, retry with retrying-label swap.
- **[MED]** `apps/qr/lib/realtime.ts:160-165, 210-214 · lib/useTableSession consumers (TableCartProvider group cart, SettlementBoard)`
  - dep: Supabase Realtime websocket subscriptions (postgres_changes on qr_cart_items/qr_carts/qr_cart_shares)
  - behavior: CHANNEL_ERROR/TIMED_OUT → console.error ONLY on all diner surfaces; supabase-js keeps a background reconnect loop. The shared group cart and the live settlement board look live while frozen — a table splitting the bill watches shares never flip, with no cue anything is wrong. Staff boards got the honest strip ('Reconnecting — showing the last known queue', var(--warn), after 2 consecutive poll fai
  - gap: Silent liveness degradation on money-adjacent diner surfaces. Also QA-CHECKLIST §A P1 'Offline/connection-loss is announced' is unticked and has no implementing mechanism anywhere.
  - fix: Expose channel/refresh health from the hooks; after ≥2 consecutive failed refetches show the shared DegradedStrip through the view's ONE existing live region ('Live updates paused — showing the last known table'). Only claim what the write path proves — during a full outage writes fail too, so drive
- **[MED]** `apps/qr/components/AnonAuthGate.tsx:58-68 · lib/useAnonSession.ts · grocery/page.tsx:312 mint-window states`
  - dep: supabase.auth.signInAnonymously() (GoTrue) — the very first call before any session exists
  - behavior: Auth is down too: both sign-in attempts fail → console.error, no UI, no scheduled retry (only a pathname change re-runs the gate). useAnonSession stays null forever; downstream, scan-and-go's AddButton shows 'Still starting your basket — try again in a moment' — a promise ('in a moment') that never resolves, looping forever. Dine-in is better: GuestList renders its 'Couldn't join this table' strip
  - gap: Pre-session outage has no designed state on 2 of 3 modes; the 'still starting' microcopy is dishonest after the first failure. No shared 'session unavailable' pattern exists for AnonAuthGate to signal into.
  - fix: AnonAuthGate publishes a session-health status (context or store); a shared SessionUnavailableStrip (built on DegradedStrip) renders on any add/mint attempt when auth is provably down, with real retry — replacing the eternal 'still starting' copy.
- **[MED]** `apps/qr/app/grocery/page.tsx:784-802 · components/GuestList.tsx:83-118 · components/PickupSlotSheet.tsx:28-88 · components/SettlementBoard.tsx:30-80 · components/grocery/GroceryBrowse.tsx:66-120 · app/cart/page.tsx:104-110 · staff boards' stale strips`
  - dep: Every client read/action failure branch — 7+ surfaces
  - behavior: Each surface shows its own hand-rolled degraded strip. Behavior is right (honest staleness, retry) but visually and behaviorally divergent: different classes (.grocery-retry vs .gb-retry vs GuestList's inline retryBtn), different roles (status vs alert), different retry semantics (in-place revalidate vs window.location.reload — reload during an outage re-strands the user through a dead SSR pass),
  - gap: No @mms/ui primitive encodes the pattern, so every new surface re-derives it and drifts (W9b review already caught two independent implementations of the same retry-unmounts-itself bug). Several strips use window.location.reload() as 'retry', which is the worst recovery during an outage.
  - fix: Extract DegradedStrip + RetryButton into @mms/ui (see inventory spec); migrate the 7 call sites; ban reload-as-retry in the primitive (in-place refetch prop required).
- **[LOW]** `packages/ui/src/empty-state.tsx + packages/ui/src/icon.tsx`
  - dep: n/a (primitive-level)
  - behavior: EmptyState is composed only for genuine-empty/all-clear ('nothing here yet'); failure branches don't reuse it, so failed-vs-empty look unrelated. Icon's bounded union has alert/refresh/info but no connectivity glyph — an outage state has no honest visual anchor distinct from a generic warning triangle.
  - gap: No tone/variant ('empty' vs 'failed' vs 'outage'), no bilingual subtitle idiom baked in, no connectivity glyph (WifiOff/CloudOff) in the icon map.
  - fix: Add `tone?: "empty" | "error"` to EmptyState (error = warn-tinted icon row + composes RetryButton in the action slot) and add one `offline` glyph (lucide WifiOff) to the Icon union.
