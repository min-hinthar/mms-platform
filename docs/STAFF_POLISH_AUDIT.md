# Staff console polish — the seven-surface audit (2026-09-20)

**What this is.** The owner asked for production-grade UI/UX and quality-of-life polish across every
`/staff` screen and the `/board` TV. One auditor per surface read the source against `docs/context/RUBRIC.md`
(incl. the O-axes), QA-CHECKLIST §A, DESIGN-LANGUAGE §17/§19, SPEC-KDS and the 25 filed OPEN-ITEMS rows;
a second, adversarial agent per surface then tried to refute each finding against source. This file is
the durable digest — the raw journal lives in a session directory that does not survive the container.
**Re-verify every anchor before acting**: the kitchen auditor read `main` while the working tree already
held the fix for four of its rows, and the verifier caught that. Slice 1 (the kitchen) shipped with this
file; the remaining verified findings are the backlog for the counter, manager, `/board`, menu/tips and
sign-in slices, in that order.

Verdict key: **real** = the verifier confirmed the defect against source · **stale** = already fixed or
never true · a verifier note follows where it corrected the auditor’s mechanism or fix. The
cross-cutting verifier did not run (the model quota was exhausted mid-workflow), so that surface’s
findings are UNVERIFIED and carry no verdict.

## Kitchen — the KDS board

_Auditor’s surface line:_ Kitchen — the KDS board (/staff/kitchen · KdsBoard.tsx)

- **kitchen-1** [critical/S] (row K29) — The bar's station segment has NO visible pressed state — `.staff-seg > .kds-chip` overrides the gold cap, so a filtered board hides tickets with no signal — **stale**
  - anchor: `apps/qr/app/globals.css:10188`
  - fix: In globals.css add, immediately after the `.staff-seg > .kds-chip` block, a (0,3,0) restatement that re-asserts the cap: `.staff-seg > .kds-chip[aria-pressed="true"] { color: var(--tx); border-color: color-mix(in oklab, var(--gold) 55%, var(--bd)); background: linear-gradient(180deg, color-mix(in oklab, var(--gold) 20%, var(--cd)), var(--cd)); }` (or move the `.staff-seg > .kds-chip` block ABOVE line 7439 so source order stops deciding). Pin it the LEARNINGS #101 way: a jsdom `StaffBar.test.tsx`/new `KdsBoard.test.tsx` case that parses globals.css, resolves the cascade for a `.staff-seg > .kds-chip[aria-pressed=true]` element and asserts `background` is not `transparent`. While there, the language switch beside it (`.staff-lang-btn[aria-pressed="true"] { background: var(--ac); color: var(--oa); }`, :7343) is an ACCENT fill — a second vocabulary next to the gold cap; share the cap declarations there too (its comment already claims it does).
  - verifier: The override the auditor describes no longer decides the cascade. globals.css:7451-7460 declares `.kds-chip[aria-pressed="true"], .staff-seg > .kds-chip[aria-pressed="true"], .staff-lang-btn[aria-pressed="true"], .help-size-row[aria-pressed="true"] { border-color: transparent; background: var(--ac); color: var(--oa); box-shadow: inset 0 1px 0 var(--sheen), 0 0 14px -6px var(--glow-gold); }` — the `.staff-seg >` form is (0,3,0) and outranks `.staff-seg > .kds-chip { background: transparent; color: var(--t2) }` at globals.css:10214-10222 (0,2,0) regardless of source order. The rail track (`.kds-rail-seg` also carries `.staff-seg`, KdsBoard.tsx:703) and the language switch ride the same block, so the second-vocabulary half of the finding is closed too. KdsBoard.test.tsx:255-275 parses the sheet and asserts the four pressed selectors share exactly one fill block.
  - on the fix: The proposed fix restates the retired 20%-gold-TINT gradient (`linear-gradient(180deg, color-mix(… var(--gold) 20% …))`) — that is the second vocabulary K29 filed; the as-built §2/§17 recipe is the solid `--ac` fill with `--oa` ink, sheen and gold halo, already shared by all four selectors. Nothing to do on this tree; on `main` the correct move is to land the working-tree slice, not the tint.
- **kitchen-2** [high/M] (row K22) — "86 this dish" is one tap, no confirm, no undo — and it goes native `disabled` the moment it is tapped — **stale**
  - anchor: `apps/qr/components/staff/KdsBoard.tsx:1142`
  - fix: Reuse the existing undo bar: widen `RecallEntry` to a discriminated `UndoEntry = { kind: "bump", … } | { kind: "86", menuItemId, label }`, push an `86` entry with `expiresAt: Date.now() + UNDO_MS` after a successful flip, and have the bar's button call `setItemSoldOut({ menuItemId, soldOut: false, expectedSoldOut: true })` for that kind (the compare-and-swap already exists in `menu-availability.ts:45`). Announce through the ONE region with new keys `kds.live.86` / `kds.undo.86` ("{x} off the menu" · "Put it back"). Replace `disabled={eightySixing}` with `aria-disabled` + `if (eightySixing) return;` in `flip`, and add `.kds-line-86[aria-disabled="true"]` beside the `:disabled` rule at globals.css:7730. Pure part (which entry kind expires when, and that an 86 entry never reaches the recall rail) goes in `lib/kds-undo.ts` with a suite.
  - verifier: Both halves are built. `type UndoEntry = ({ kind: "bump" } & RecallEntry) | { kind: "eighty6"; menuItemId; label; expiresAt }` (KdsBoard.tsx:63-65); `flip` calls `onEightySixed({ menuItemId, label: dishVisible(...) })` on success (:1119), which pushes `{ kind: "eighty6", …, expiresAt: Date.now() + UNDO_MS }` and announces `kds.live.86` through the one region (:443-450); the bar's button routes to `undoEightySix` → `setItemSoldOut({ soldOut: false, expectedSoldOut: true })` (:452-474, :915). The control is `aria-disabled={eightySixing || undefined}` (:1207) with `if (eightySixing) return;` first in `flip` (:1101), and `.kds-line-86[aria-disabled="true"]` exists (globals.css:7747). Dictionary keys `kds.undo.86`, `kds.live.86`, `kds.live.86.undone`, `kds.err.86.undo` exist (i18n/staff.ts:220,236,240,259). KdsBoard.test.tsx:139-190 pins the undo round-trip and :192-210 the aria-disabled state.
  - on the fix: The fix matches what is built, except the `lib/kds-undo.ts` pure module — expiry lives in the 1s tick (`setUndo(prev => prev && prev.expiresAt <= localNow ? null : prev)`, :179) and an eighty6 entry never enters `recall` (only `onBumped` writes it, :427), so the suite-worthy rule the auditor wanted is already covered by the jsdom cases. K22 is closed in the working tree (see missed/ROW-STATUS).
  - new Burmese → K15
- **kitchen-3** [high/M] — Every server-action error is an English sentence rendered inside the region's `lang={lang}` — the `msg: string` wall §17 names, and `OutageText` is never used on the KDS — **real · med**
  - anchor: `apps/qr/components/staff/KdsBoard.tsx:552`
  - fix: Change `KitchenActionResult`'s failure arm to carry a key: `{ ok: false; key: "kds.err.already" | "kds.err.bumpFailed" | "kds.err.recallWindow" | "kds.err.fireLive" | "kds.err.invalid"; error: string }` (keep `error` for callers that only have a string), hold `err` as `StaffMsg` (key + vars OR server sentence) as `StaffBar`'s pattern does, and render via `<MsgText>` so a keyed message arrives marked and an untranslatable server sentence renders as a bare English node with NO `lang="my"` on it (move `lang={lang}` off the region onto the chrome-only branches). Six new MY strings for the kitchen sentences → K15. The gate's `"Staff sign-in required."` should never be shown at all on this board: on that arm call the same `window.location.assign("/staff/login")` the poll already does (:237), so an expired session during a bump goes to the honest surface, not to a 5-second banner.
  - verifier: Partly live. The thrown-error arms are now keyed (`tf(lang, "kds.err.bump"|"kds.err.fire"|"kds.err.line"|"kds.err.86"|"kds.err.recall"|"kds.err.86.undo")`, KdsBoard.tsx:972,986,1141,1123,493,471), but every server REFUSAL still flows raw: `if (!res.ok) onError(res.error)` at :965, :983, :1115, :1135 and `setErr(res.error)` at :463, :483, into `const [err, setErr] = useState<string | null>(null)` (:125), rendered as the first branch of the region that carries `lang={lang}` (:589-619). The sentences are English literals: kitchen.ts:350/380/407/435 "Invalid request.", :359/361 "Couldn’t update that ticket. Try again."/"That item was already updated.", :389/391, :416/418 "The recall window has passed for that ticket.", :443; staff.ts:162 "Staff sign-in required."; menu-availability.ts:44/71/122 + AVAILABILITY_OUTAGE. Under `my` the global `[lang="my"] { overflow-wrap: anywhere; line-height: var(--lh-my) }` (globals.css:64-74) re-leads that English and a Burmese TTS voice reads Latin. `StaffMsg`/`MsgText` exist (components/staff/StaffMsg, used by ApprovalsBoard.tsx:28, PinUnlock.tsx:12, LossActionSheet.tsx:12) and KdsBoard imports neither (:2-39). Downgraded from high: it fires only on a refusal (already-bumped, expired recall, expired session), the text is still legible, and the common paths are keyed.
  - on the fix: Keyed failure arm on `KitchenActionResult` (and `SetItemSoldOutResult`) keeping `error: string`, `err` held as `StaffMsg`, rendered by `<MsgText>` with `lang` moved off the region onto the chrome branches — matches the §17 pattern already in ApprovalsBoard/PinUnlock. Redirecting on the gate's `signin` arm mirrors :236. Neither kitchen.ts nor menu-availability.ts is in `verify-slice.mjs`'s mutate set (grep: no entry), so the shape change needs no mutant, but the six MY sentences are machine drafts → K15 rows. Keep the region's ONE-ness: MsgText must render inside the same `<p role="status">`.
  - new Burmese → K15
- **kitchen-4** [high/S] (row P7g) — The "+N more" rush signal and the page controls live only in the footer, below a grid that grows past the viewport — **real · med**
  - anchor: `apps/qr/components/staff/KdsBoard.tsx:784`
  - fix: Render the rush signal in `.kds-head`: move `{moreAfter > 0 && <span className="kds-more">…}` and the `.kds-dots`/sr-only "Page n of total" into `.kds-controls` beside the arrival pill (they are the board's, like the pill), keep ‹ › in the footer or put them in the head too (48px, `.kds-page-btn`). Do NOT make the footer sticky (§17: StaffBar is the only sticky element; the KDS already spends its one fixed slot on the undo bar). Optional belt for P7g: `.kds-body { overflow: auto; min-height: 0 }` so the head and the moved signal are never pushed off. `moreAfter`/`pageCount` are already pure derivations; a jsdom case asserting the count is inside `.kds-head` closes it.
  - verifier: Confirmed mechanism. The pager, dots, sr-only page and `kds-more` render only inside `<footer>` (KdsBoard.tsx:823-869); `.kds-head`/`.kds-stats` (:564-584) show Open/Oldest/Late/Avg and no page or overflow count. `.kds-root { min-height: 100dvh; display: flex; flex-direction: column }` (globals.css:7367-7371) is a MIN height, and `.kds-body { flex: 1; … min-height: 0 }` (:7468-7474) has no `overflow`, so the root grows with the grid and the footer is pushed below the fold whenever two rows (plus `gridRow: "span 2"` tickets, :997) exceed the viewport. SPEC-KDS §2:42 lists `page dots` under "Header strip (always visible)" and §2:40-41 says the count IS the rush signal. Downgraded to med because whether the overflow actually happens at 1080p is exactly the unmeasured P7g render; the head placement is the spec regardless.
  - on the fix: Moving `kds-more` + the sr-only `kds.page` into `.kds-controls` (:621-663) and keeping ‹ › in the footer respects §17 (nothing new sticky; the undo bar keeps the fixed slot). The optional `.kds-body { overflow: auto }` belt does NOT bound anything while the root is `min-height` — it would need `height: 100dvh` on `.kds-root`, which also changes how the sticky StaffBar and the fixed undo bar compose; leave the belt out and just relocate the signal. `moreAfter`/`pageCount` are pure (:355-358); a jsdom case asserting `.kds-head .kds-more` exists at 9 tickets is one file.
- **kitchen-5** [med/M] — Six controls go native `disabled` on the tap that just happened — bump, line, 86, undo, recall, and the pager button you just reached the edge with — **stale**
  - anchor: `apps/qr/components/staff/KdsBoard.tsx:1002`
  - fix: Replace each `disabled={x}` with `aria-disabled={x}` and a first-line `if (x) return;` in the handler (bumpAll, fireNow, tap, flip, doRecall, the two pager handlers); add `[aria-disabled="true"]` twins beside the existing `:disabled` rules (globals.css:7587, :7730, :7832) so the look is unchanged. Keep the label and overlay a busy state (`data-busy` + reduced opacity) instead of swapping in "…". After a successful ticket bump move focus explicitly to the undo bar's button (the recovery control) — it is the element a mis-tap wants — and after a line tap keep focus on the same row (the `<li>` stays mounted). A jsdom `KdsBoard.test.tsx` case: tap the bump, assert `document.activeElement` is never `document.body`.
  - verifier: No action control on the board is natively disabled any more. Bump `aria-disabled={pending || undefined}` (:1059) with `if (pending) return;` (:959); fire :1050/:978; line :1155/:1130; 86 :1207/:1101; recall :892 with `if (recallPending) return;` (:478); undo bar :916 with the refusal in `doRecall`/`undoEightySix` (:478, :453); ‹ :836/:831 and › :859/:853. CSS twins exist at globals.css:7604 (`.kds-line`), :7747 (`.kds-line-86`), :7853 (`.kds-page-btn`). KdsBoard.test.tsx:228 `it("no action button on the board is ever natively disabled")` and :192-226 pin focus staying on the tapped 86/bump.
  - on the fix: Already applied. Residual the auditor folded in and which is still live: `{pending ? "…" : …}` at :1052, :1064, :1214 — the fire button (:1046-1053) has NO `aria-label`, so its accessible name becomes "…" for the round trip (bump and 86 keep theirs via `al()`), and the 64px zone visibly collapses to an ellipsis. Prefer a `data-busy` overlay over the label swap, as the auditor suggested — one file, S.
- **kitchen-6** [med/M] (row K27) — Type that must read at distance is pinned in px, and the text-size dial scales only four of the six `--kfs-*` tokens — the mm:ss clock and every chip stay 24/15px at Large — **real · med**
  - anchor: `apps/qr/app/globals.css:7407`
  - fix: Add `--kfs-label: 0.8125rem` (the 13px chrome tier) on `.kds-root` and point the four px rules at it (`.kds-stat span`, `.kds-badge`, `.kds-line-tag`, `.kds-rail h3`); restate `--kfs-clock`, `--kfs-meta` and `--kfs-label` in BOTH `[data-size]` overrides (e.g. m: clock 1.75rem / meta 1.0625rem / label 0.9375rem; l: clock 2rem / meta 1.1875rem / label 1rem — keep clock under `--kfs-id` so the table number stays the largest). Extend `kds-size.test.ts`'s stylesheet parse (it already reads the `--kfs-item-my` overrides) to assert every `--kfs-*` declared on `.kds-root` is restated in both overrides, so a seventh token cannot be forgotten. Then correct the two comments.
  - verifier: Half stale, half live. STALE: `.kds-stat span { font-size: var(--kfs-meta) }` (globals.css:7405-7412, with a K27 comment) and `.kds-badge { font-size: var(--kfs-meta) }` (:7564) — the 12px stat-label claim is gone. LIVE: `.kds-line-tag { font-size: 13px }` (:7640-7647, the Bag it/Cooking tag) and `.kds-rail h3 { font-size: 13px }` (:7898-7905) are still magic px; and the dial `.kds-root[data-size="m"]` (:9956-9961) / `[data-size="l"]` (:9962-9967) restate only `--kfs-id/--kfs-item/--kfs-item-my/--kfs-mod` while `--kfs-clock: 1.5rem` and `--kfs-meta: 0.9375rem` (:7365-7366) are never restated, so at Large the mm:ss clock (:1006, `.kds-clock` :7558), the stat numerals (:7401), `+N more` (:7872), the page buttons, chips and recall buttons stay at 24/15px. The CSS comment at :9952 ("scaling every `--kfs-*` together") and KdsBoard.tsx:390 ("every `--kfs-*` tier re-sizing under the thumb") are both false statements about the dial. The O-B clock not following the distance dial is a real gap; the two px literals are house-rule defects.
  - on the fix: Add `--kfs-label` on `.kds-root`, point the two remaining px rules at it, restate clock/meta/label in both overrides (clock must stay under `--kfs-id` at each step), and extend kds-size.test.ts — it already reads `globals.css` with `readFileSync` + a per-selector regex (:40-49), so a "every `--kfs-*` on `.kds-root` is restated in both overrides" case is ~10 lines. Caveat to note: `--kfs-meta` also sizes `.staff-lang-btn` (`var(--kfs-meta, var(--fs-sm))`, :7332) and `.kds-chip` in the bar, so scaling meta grows the bar's controls too — acceptable (44px min stays), but the composite-contrast pins are unaffected only if no color changes. `.orb-root` overrides `--kfs-meta` on its own (:8033), so `/board` is untouched.
- **kitchen-7** [med/M] (row K28) — `fmtElapsed` has no ceiling and its sr-only twin is hard-coded English outside the dictionary — **stale**
  - anchor: `apps/qr/components/staff/KdsBoard.tsx:101`
  - fix: Create `lib/kds-time.ts` with pure `formatElapsed(ms)` — `m:ss` under an hour, `2h 44m` from an hour, `1d+` past a day — plus `elapsedParts(ms)` returning `{ h, m, s }` for the spoken form; suite in `kds-time.test.ts` with the three boundaries (3599s, 3600s, 86400s) and a `verify:slice` mutant on the hour threshold. KdsBoard's three call sites and the `/board` clock read it. Add dictionary keys `kds.a11y.elapsed` ({m} minutes {s} seconds elapsed / with an {h} form) and `kds.a11y.firesAt` ({t}), rendered via `tf()` with `lang={lang}` on the sr-only span — two new MY strings → K15.
  - verifier: `lib/kds-time.ts` exists (untracked, working tree) with `elapsedParts` banding at `HOUR_S = 3_600` / `DAY_S = 86_400`, `fmtElapsed` → `m:ss` / `2h 44m` / `1d+`, and `spokenElapsed(lang, ms)` through `kds.age.mmss` / `kds.age.hm` / `kds.age.days` (i18n/staff.ts:226-228) with `localizeCount` numerals. KdsBoard imports both (:32) and uses `fmtElapsed` for Oldest (:573), Avg (:581) and the strip (:1007); the sr-only twin is `<span className="sr-only" lang={lang}>{… spokenElapsed(lang, ageMs)}</span>` (:1012-1016) and the held form goes through `tf(lang, "kds.slot", …)` — no template literal remains. kds-time.test.ts covers the 3599/3600 edge (:18), the day cap (:27), negative clamp (:33), truncation (:38) and the spoken keys (:43); KdsBoard.test.tsx:234-253 pins `1d+` and the Burmese numerals in a render.
  - on the fix: Built as proposed, minus the `verify:slice` mutant: `scripts/verify-slice.mjs` has no `kds-time` entry (grep), so the hour threshold is pinned by the suite only. If a mutant is added, kds-time.ts joins the mutate set and CLAUDE.md's bucket enumeration must be updated (the 111+3+12+1 list). `/board`'s clock is another surface. K28 is closed for the KDS half in the working tree.
  - new Burmese → K15
- **kitchen-8** [med/M] — After any reload the board is silently mute — sound arming is not remembered and the only sign is a ghost chip identical to "All-day" — **real · med**
  - anchor: `apps/qr/components/staff/KdsBoard.tsx:159`
  - fix: Persist `mms.kds.sound = "1"` when `arm()` succeeds (and clear it on an explicit mute at volume 0). On mount, when that flag is set and the engine is not armed, attach a one-shot `pointerdown` capture listener on `.kds-root` that calls `chime.current.arm()` — the first tap of the shift (usually a bump) arms it without a detour to the chip. While wanted-but-unarmed, render the chip in a warn state (`.kds-chip-muted`: warn hairline + the `volume` icon with an sr-only `kds.sound.off` dictionary label) and, if unarmed for more than a minute of open tickets, say so in the status region under the notice tier (`kds.live.muted`). Keep `enableSound`'s audible confirmation tone only for the explicit chip tap, never for the silent auto-arm. Two new MY strings → K15.
  - verifier: Confirmed. `const [soundOn, setSoundOn] = useState(false)` (KdsBoard.tsx:158); the persisted-controls hydrate (:188-212) reads STATION_KEY, RAIL_KEY, KDS_SIZE_KEY and `getKdsVolume()` and no sound flag; the only arming path is the chip's own `enableSound` (:410-415, :650-652), rendered as a plain `.kds-chip` two lines under the All-day `.kds-chip` (:624). `KdsChime.play` is `if (!this.engine.armed) return;` (kds-sound.ts:59), so arrivals (:255-256) and re-chimes (:312) drop silently after any remount — the auth redirect at :236, a deploy, a crashed tab. No warn state, no region line. SPEC-KDS §3:56 "gesture-armed once at shift start" vs. once per page load.
  - on the fix: The persistence + warn-state + `kds.live.muted` parts are fine (a warn hairline is a STATE, not a selection idiom, so §2 is untouched; two K15 rows). The auto-arm as written does not work on the target device: under the HTML user-activation rules a `pointerdown` from a TOUCH pointer is not an activation-triggering event (only mouse `pointerdown`, `pointerup`, `touchend`, `keydown`, `click` are), so `AudioContext.resume()` inside a `pointerdown` capture listener stays suspended on the kitchen tablet. Use a one-shot `pointerup`/`click` capture on `.kds-root` (chime-core.ts:118 `arm()` is async and returns the resumed state, so gate `setSoundOn` on its result). Also silence `enableSound`'s confirmation tone for the auto-arm path as proposed.
  - new Burmese → K15
- **kitchen-9** [low/S] (row K29) — The bump — the biggest commit on the console — has no press feedback and no haptic while every door and segment gets both — **real · low**
  - anchor: `apps/qr/components/staff/KdsBoard.tsx:911`
  - fix: Add `staff-press` to `.kds-bump` and `.kds-bump-fire` (`className="kds-bump staff-press"`) — it is already reduced-motion escorted at globals.css:10333 — and call `haptic("commit")` as the first line of `bumpAll` and `fireNow` (the undo bar is the visible half §12 requires). Consider `haptic("pick")` on the per-line check-off (`tap`, :1068) since the row wash is its visible half.
  - verifier: Confirmed. The bump is `<button type="button" className="kds-bump" onClick={bumpAll} aria-disabled=…>` (KdsBoard.tsx:1055-1063) and the fire button `className="kds-bump kds-bump-fire"` (:1048) — neither carries `staff-press`; `bumpAll` (:958-975) and `fireNow` (:977-989) call no `haptic`. The file's haptic calls are :377, :390 (`pick`), :712, :723 (`pick`), :447, :468 (`commit` — on the 86 and its undo). So the 86 buzzes on commit while the bump beside it does not. globals.css has no `.kds-bump:active` rule (7781-7799 declares none; grep none). `.staff-press` (10225-10250) is reduced-motion escorted at 10360.
  - on the fix: `className="kds-bump staff-press"` (and on the fire button) plus `haptic("commit")` as the first line after the re-entry guard in `bumpAll`/`fireNow`; the undo bar is the visible half. `.staff-press` sets `position: relative; overflow: hidden`, harmless on the bump. `haptic("pick")` on the line tap is reasonable but optional. S.
- **kitchen-10** [low/S] — An action error survives at most one poll — a bump refusal can vanish in under a second — **real · low**
  - anchor: `apps/qr/components/staff/KdsBoard.tsx:262`
  - fix: Stamp `errSince.current = Date.now()` in `onError`, and in `refresh` clear the banner only when it is older than a dwell (`ERR_MIN_MS = 8_000`) or the next user action replaces it (every handler already calls `onError(null)` first). Pure rule `shouldClearActionError(since, now, min)` in `lib/kds-undo.ts` (or `staff-outage.ts`) with a suite; the 5-second poll is not the reader's clock.
  - verifier: Confirmed. `setSnap(queue); setErr(null); // a fresh good snapshot clears a stale action-error banner` (KdsBoard.tsx:260-261) runs on every successful `refresh`, which fires from the 5s backstop (:285) and the 400ms-debounced realtime `onChange` (:277-282), so a refusal written by `onError(res.error)` (:965, :983, :1135) with no follow-up refresh survives 0–5s. A one-shot `notice` has its own 4s dwell (:322-326); an error has none.
  - on the fix: A ref stamp in `onError` and a pure `shouldClearActionError(since, now, minMs)` in lib with a suite is the right shape; `refresh` is `useCallback(…, [])` (:275) so the stamp must be a ref, not state. Handlers already `onError(null)` first (:960, :979, :1103, :1131), so a user action still replaces it immediately.

**What the verifier found that the auditor missed:**

- ROW-STATUS: the auditor's calls are against `main` (5db02da), not the working tree — K22 CLOSED (KdsBoard.tsx:63-65 UndoEntry eighty6, :443-474, :1207 aria-disabled; KdsBoard.test.tsx:139); K28 CLOSED for the KDS half (lib/kds-time.ts:34-70 `1d+` cap + `spokenElapsed`; KdsBoard.tsx:32,:573,:581,:1007,:1012-1016; `/board` not checked); K29 KDS half CLOSED (globals.css:7451-7460 one shared pressed rule at (0,3,0) incl. the switch; the ✓ inline at KdsBoard.tsx:1068-1069 with `.kds-bump { display: flex; gap: 10px }` :7781); K27 PARTLY-CLOSED (`.kds-stat span`/`.kds-badge` on `--kfs-meta` :7405/:7564; `.kds-line-tag` :7642 and `.kds-rail h3` :7900 still 13px; dial never restates clock/meta :9956-9967). All of this is uncommitted (git status: M KdsBoard.tsx, globals.css, i18n/staff.ts, staff-labels.ts; ?? KdsBoard.test.tsx, kds-time.ts, kds-time.test.ts) — P2n (`<ul className="kds-lines" role="list">` unnamed, KdsBoard.tsx:1032; rail lists :740/:784 unnamed) and P7g remain still-live as the auditor said.
- apps/qr/components/staff/KdsBoard.tsx:1046-1053 — the held card's Fire button has no `aria-label`, so `{pending ? "…" : <Chrome lang={lang} k="kds.fire" echo="stack" />}` makes its accessible name literally "…" for the whole round trip (the bump at :1060 and the 86 at :1209 keep their names through `al()`), and the 64px `.kds-bump-fire` zone visibly collapses to an ellipsis under the thumb; same swap at :1064 and :1214. Fix: keep the label, overlay busy via `data-busy` + `.kds-bump[aria-disabled="true"]` opacity (no such rule exists in globals.css:7781-7799). Low, S.
- apps/qr/app/globals.css:7752 `.kds-line-86:hover:not(:disabled)` is keyed on `:disabled`, which the control never carries any more (KdsBoard.tsx:1207 uses `aria-disabled`), so an in-flight 86 still lights its warn hover fill — should be `:not([aria-disabled="true"])`; and neither `.kds-recall-btn` (:7973-7983) nor `.kds-undo button` (:8006-8016) has an `[aria-disabled="true"]` twin, so a recall/undo in flight (KdsBoard.tsx:892, :916) shows no busy state while the handler silently refuses the second tap (:478, :453) — the person sees a tap that did nothing. Low, S.

## Counter & tables — the counter person’s screen

_Auditor’s surface line:_ Counter & tables — the counter person's screen (POS): /staff floor branch (RegisterStart → FloorBoard merged list → ExpoBoard lane → DayCash → MoreGrid) + the doors

- **counter-1** [high/M] — "Picked up" drops a bag with one 44px tap, no undo, no recall — the KDS bump gets 6s undo + a 2-minute rail, the lane gets nothing — **real · high**
  - anchor: `apps/qr/components/staff/ExpoBoard.tsx:317`
  - fix: Deferred commit with an undo pill, the KDS pattern: on a second-stage tap flip the card to a `picked` posture instantly (optimistic, the button becomes an `Undo` control announced through the lane's ONE live region), and fire `setTogoStatus({to:"picked_up"})` only after a 6s window unless undone; the rule (`expoUndoWindow`, window + expiry) goes in `lib/expo-rules.ts` with a suite. Raise `bumpBtn.minHeight` to 60 (a token, e.g. `--tap-bump`, shared with `.kds-bump`). No status-machine change needed — the write simply waits.
  - verifier: Confirmed. ExpoBoard.tsx:317 `const to = firstStage ? "ready" : "picked_up"`, :470-472 one `onClick={bump}` with no confirm; `grep -n 'undo\|recall' ExpoBoard.tsx` is empty. expo-stage.ts:256 `minHeight: 44` vs globals.css:7781-7787 `.kds-bump { min-height: 64px }`. KdsBoard.tsx:57 `UNDO_MS = 6_000` / :428 `setUndo({kind:"bump"…})` ships the undo the lane lacks. The SQL machine (supabase/migrations/20260624000000_s4_togo_fulfillment.sql:181-188) admits only preparing→ready and ready→picked_up — there is NO reverse edge, so a mis-tapped Picked up is unrecoverable from any surface, and lib/expo.ts:39/:68 drops picked_up from the queue. O-E fails on both halves (zone size, undoability).
  - on the fix: Deferred commit is the only no-migration path (no SQL reverse edge exists), so it is the right shape. Caveats to build in: keep the `picked` posture in the LANE keyed by orderId (the card re-renders from `snap` on the 5s poll at :131 and the card's own state would not survive a re-fetch that re-keys it); announce the undo through the lane's one `role="status"` at :208 (ExpoCard already routes through `onError`, add an `onNotice`); a tab closed inside the window loses the write — acceptable, say so in the docblock; /track and /board lag 6s. `--tap-bump` does not exist in tokens.css (no `--tap*` token) — add it there, share with `.kds-bump`'s 64. Rule + expiry in lib/expo-rules.ts with a suite and a mutant.
  - new Burmese → K15
- **counter-2** [high/S] — The bump button goes native `disabled` and collapses to "…" while pending — focus drops to body and the heading catch-all yanks it to the h2 after EVERY bump, even one that keeps the card — **real · high**
  - anchor: `apps/qr/components/staff/ExpoBoard.tsx:472`
  - fix: `aria-disabled={pending}` + refuse re-entry inside `bump` (`if (pending) return`), keep the label rendered and add `aria-busy={pending}` with a `.staff-btn[aria-busy]` opacity in globals.css (RM-safe, no transform). Restrict the heading catch-all to the case where the focused card actually LEFT the list (compare the previous ticket ids), so a first-stage bump keeps focus on its own button.
  - verifier: Confirmed. ExpoBoard.tsx:472 `disabled={pending}` and :500-501 `{pending ? ("…") : …}` replace the bilingual label with an ellipsis. The catch-all at :142-146 runs on EVERY `snap` change (`useEffect(…, [snap])`), and `bump` (:368-372) awaits `onBumped()` → `refresh` → `setSnap`; whichever order `pending=false` and the new snap commit in, `document.activeElement` is already `<body>` because the browser blurred the button when it went native-disabled, so `hadRealFocus.current` (set by `onFocusCapture` at :191) sends focus to the h2 at :189 even when the card stays (first stage → ready). §17 (DESIGN-LANGUAGE.md:651-653) names exactly this.
  - on the fix: aria-disabled + `if (pending) return` in `bump` + keep the label + `aria-busy` is correct and matches KdsBoard.tsx:1155. Two adjustments: (1) globals.css:3320-3324 keys the press feedback on `.staff-btn:not(:disabled)` — add a `:not([aria-disabled="true"])` twin or a busy button keeps scaling on tap; (2) the 'compare previous ticket ids' half is moot once native disabled goes: focus never leaves the button on a first-stage bump, and on the second stage the card unmounts so the existing body-check already does the right thing. Don't add the id-diff.
- **counter-3** [med/S] — Every Start-zone control is native `disabled` during the mint, and a refused mint leaves focus nowhere — **real · med**
  - anchor: `apps/qr/components/staff/RegisterStart.tsx:74`
  - fix: Same rule as §17: `aria-disabled={pending}` on each control, a `pending` guard at the top of `mint`, and on `!r.ok` move focus to the notice (`ref` on the `<p role="status">`, `tabIndex={-1}`). The submit buttons already echo both states; keep that.
  - verifier: Confirmed. RegisterStart.tsx:74, :82, :91, :124, :161 all `disabled={pending}`; `mint` (:48-62) has no re-entry guard; on `!r.ok` (:56-59) only `setNotice` runs — nothing moves focus, and the controls re-enable with focus on `<body>`. On success `router.push` (:60) masks the drop. Same §17 rule as counter-2.
  - on the fix: `aria-disabled` on the five controls + `if (pending) return` at the top of `mint` closes it. Drop the 'move focus to the notice' half: with aria-disabled the tapped control never loses focus, and the `<p role="status">` at :171 announces on its own — focusing a live region on top of that risks a double announcement and is not one of §7's focus-move cases (remove/route/step). Note the submit buttons' `reg.going` echo (:125/:162) already keeps height stable.
- **counter-4** [med/S] (row K29) — The chosen Start arm wears an accent border, not the lit gold cap, and none of the three arms has a press affordance — **real · med**
  - anchor: `apps/qr/components/staff/register-stage.ts:155`
  - fix: Move the arm styles into globals.css as `.staff-arm` / `.staff-arm[aria-expanded="true"]` extending the gold-cap recipe (`.staff-door[aria-current="true"]`'s gradient + `--glow-gold`, `--ink` on the fill per §2), add `staff-press` to all three, and let `HelpPicture` read the same class (register-stage.ts becomes the class names, not style objects). Keep `aria-expanded` as the state carrier.
  - verifier: Confirmed. register-stage.ts:240-244 `startBtnActive = { …startBtn, borderColor: "var(--ac-strong)", color: "var(--ac-strong)" }` — an accent outline where every chosen thing on the console wears the gold cap (globals.css:9859-9865 `.staff-door[aria-current="true"]`, §2 and §17's segmented-control rule). RegisterStart.tsx:71-96: none of the three buttons carries a `className`, so neither `.staff-press` (globals.css:10225) nor `.staff-btn` (:3314) feedback fires. K29's 'no hairline' half IS closed (register-stage.ts:233 `border: 1px solid var(--bd)`); the press and the cap are not.
  - on the fix: Class-based `.staff-arm` / `.staff-arm[aria-expanded="true"]` extending the door cap recipe is right; ink on a gold fill is `--ink` per §2. It breaks HelpPicture.test.tsx:54 (`style.background === "var(--sf)"` read off the exported object) — move that assertion to the class, and keep §17's rule that the picture wears the REAL control's own declaration (the class satisfies it). `aria-expanded` stays the state carrier; Walk-up has none and so never wears the cap — correct.
- **counter-5** [med/S] — Opening the Phone / Table arm does not move focus into its input, and the previous arm's error stays on screen — **real · med**
  - anchor: `apps/qr/components/staff/RegisterStart.tsx:84`
  - fix: Focus the revealed input when `arm` changes (`useEffect` on `arm` → `inputRef.current?.focus()`; `autoFocus` also works since the form mounts fresh), `enterKeyHint="go"` on both inputs, and `setNotice(null)` in the arm toggles.
  - verifier: Confirmed. RegisterStart.tsx:84 / :93 toggle `arm` only; the inputs at :111-122 and :148-160 carry no `autoFocus`/ref, so focus stays on the pill and the keyboard does not rise. `setNotice(null)` exists only inside `mint` (:53); the table arm's local `reg.err.table` (:138) is set on submit and survives switching to Phone, so it reads under the wrong form.
  - on the fix: `autoFocus` on the mounted input (the form mounts fresh per arm, and the mount runs inside the tap's discrete-event flush so iOS raises the keyboard), `enterKeyHint="go"`, and `setNotice(null)` in both toggles. One line each; no dictionary change.
- **counter-6** [med/M] (row P2q) — The to-go card is still English inside a Burmese lane — nine visible strings, none through the dictionary — **real · med**
  - anchor: `apps/qr/components/staff/ExpoBoard.tsx:403`
  - fix: ONE pass over the card: keys `expo.tag.here`, `expo.tag.ready`, `expo.tag.verified`, `expo.pickup` ({t} slot, Latin-marked by <Chrome>), `expo.grocery.note`, `expo.dest.togo`, `expo.dest.grocery`, and render the visible call-out through `<Chrome k="floor.table" vars={{id}}>` (the aria already does at :344). The two catch strings become the branched local notice P2p describes (`{kind:"local", k}` per grocery × table, rendered through <Chrome>). Machine-draft MY → K15.
  - verifier: Confirmed, P2q still live. Visible English on the card under `my`: :333 `` `Table ${ticket.tableNumber}` `` (only the aria twin at :344 is localized), :403 `Here now`, :418 `{grocery ? "Verified" : "Ready"}`, :427 `Pickup {formatSlotLong(…)}`, :447 `Scan &amp; Go — verify the exit pass; nothing to bag.`, :540 `"Grocery" : "To-go"`, plus the two catch sentences at :378-379 (P2p). Head row, counts, empty state and the four bump verbs are through <Chrome>. No `expo.tag.*`/`expo.dest.*`/`expo.pickup` keys exist in staff.ts (grep empty).
  - on the fix: One pass over the card as P2q asks. `floor.table` (staff.ts:331) already carries the `{id}` Latin slot; `formatSlotLong` output rides `{t}`; `whoElse` (a name or `#code`) stays verbatim. The catch strings become the `Notice` shape RegisterStart already uses (`{kind:"local",k}` per grocery×table) rendered through <Chrome> — P2p's own prescription. Retire the `callOut` declaration-following exemption in scripts/check-staff-lang.mjs (~:440) once the visible call-out is dictionary text. New MY strings → K15.
  - new Burmese → K15
- **counter-7** [med/M] (row K27) — The lane's clock is paid-age, not due-ness, and it carries no urgency dimension — a noon-paid 6pm pickup reads "5h ago" in 13px grey while "Here now" is the smallest text on the card — **real · med**
  - anchor: `apps/qr/components/staff/ExpoBoard.tsx:421`
  - fix: A pure `expoAge({arrivedAt, pickupSlot, createdAt, now})` in `lib/expo-rules.ts` → `{ from: slot ?? paid, tone: ok|warn|late }` with the two thresholds a config constant (suite + mutant), age counted from the SLOT when one exists (never late before it). Render it as a header-strip tone on `cardHead` (`--okb`/`--warnb`, text never recoloured — SPEC-KDS §1) with the elapsed at `--fs-h3`; promote "Here now" to a `Badge tone="accent" bordered` beside the call-out and lift `tableLabel` to `--fs-h2` and `lineRow` to `--fs-body` (K27's ask for the counter tiers). Keep the sort untouched.
  - verifier: Confirmed. ExpoBoard.tsx:421 renders `RelativeTime iso={ticket.createdAt}` at `--fs-sm`/`--t2` (:420) — paid age, never due-ness; nothing on the card changes tone as a bag goes late (`cardHead` :563-568 has no background). `hereTag` :580-586 is `--fs-xs` (11px, tokens.css:38) uppercase — the smallest text on the card; `tableLabel` :569 is `--fs-body` (16px). lib/expo-rules.ts exports only `kitchenStateOf` and `compareExpoTickets` (:30, :55) — the sort knows the slot, the card does not. O-B/O-G as RUBRIC.md:80/85 define them; the M65 shape (a noon-paid 6pm pickup reads '5h ago').
  - on the fix: Pure `expoAge` in lib/expo-rules.ts with a suite + mutant is the house shape. `--okb`/`--warnb` exist in both themes (tokens.css:99/101 light, :460/462 Night) and SPEC-KDS §1:27-28 ('never recolor the body') supports a header-strip tone only. Using `--fs-h2`/`--fs-body` rather than `--kfs-*` is right: `--kfs-*` is declared only inside `.kds-root` (P6f). Counting age from the SLOT when one exists keeps a scheduled bag from aging red for hours (O-G). Keep `compareExpoTickets` untouched.
- **counter-8** [med/S] — `RelativeTime` renders English literals ("just now", "3m ago", "2h ago") on every table card, counter card and bag under a Burmese console — **real · med**
  - anchor: `apps/qr/components/staff/RelativeTime.tsx:28`
  - fix: Give `RelativeTime` a `lang` prop (from `useStaffLang()`), words from four keys `time.justNow`, `time.minAgo`, `time.hrAgo`, `time.dayAgo` ({n} slot via `tf`), and render the label through `<Chrome>` inside the `<time>` so the Burmese arrives marked and the numeral rule applies. Then the `table.detail.*` prefixes at staff.ts:592-596 can become one sentence with a `{t}` slot.
  - verifier: Confirmed with one corrected mechanism. RelativeTime.tsx:296-301 returns English literals; :291 `<time dateTime={iso}>{label}</time>` carries no `lang`. Mounted on this surface at TableCard.tsx:202, CounterOrderCard.tsx:93, ExpoBoard.tsx:421 (and ApprovalsBoard.tsx:425 on the rails). The auditor's 'announced as Burmese' is wrong: `<html lang="en">` stays by design (OPEN-ITEMS P2d) and TableCard.tsx:201 wraps the node in a plain span, so a MY voice reads it as English — the defect is an unmarked English tail on every Burmese card, not mispronunciation. The dictionary comment is at staff.ts:610-611 ('an English RelativeTime node … that this slice does not own'), not :594.
  - on the fix: `lang` from `useStaffLang()` (every mount sits under StaffLangProvider) + four `time.*` keys with a `{n}` slot rendered through <Chrome> inside the `<time>` is correct; `{n}` is a COUNT slot (fill.ts) so Burmese numerals under `my` follow the owner's rule. Then `table.detail.tabOpened`/`lastActivity` (staff.ts:612-613) can become `{t}` sentences. Four machine-drafted MY strings → K15.
  - new Burmese → K15
- **counter-9** [low/S] — The counter's loading skeleton is not the counter's geometry — no bar, no greeting, no `.staff-main`, and an English sr-only line — **real · low**
  - anchor: `apps/qr/app/staff/loading.tsx:10`
  - fix: Render `<main className="staff-main">` with an inert bar-shaped `Skeleton` (68px, full-bleed) then `<div className="staff-col" style={{maxWidth:1080}}>` with a greeting bar and the three zone shapes; make `loading.tsx` async, `await readStaffLang()` and announce `what.floor` through `<Chrome>` (`out.loading`-style key). Mirror the `app/staff/table/[id]/loading.tsx` shape which already uses `.staff-col`.
  - verifier: Confirmed. loading.tsx:10-11 `<main style={{ maxWidth: 1080, margin: "0 auto", padding: "var(--s6)" }}>` + `Loading the floor…` English sr-only, no `.staff-main`, no bar, no `.staff-col`, no greeting. The hydrated floor is page.tsx:207-213 `.staff-main` → `StaffBar` (globals.css:10015-10021 sticky, `min-height: 68px`, `margin: 0 0 var(--s4)`) → `.staff-col` (`padding: 0 var(--s5)`) → greeting → `.staff-zone` (`margin-top: var(--s6)`). The first heading lands ~110px lower than the skeleton drew it. RUBRIC #1 'skeletons matching component geometry; no layout shift'. table/[id]/loading.tsx:8-19 already draws the bar and column; kitchen/loading.tsx:9 shares the English sr-only.
  - on the fix: Mirror table/[id]/loading.tsx. An async loading.tsx awaiting `readStaffLang()` is cheap — it is a memoized cookie read (staff-lang-server.ts:18-19), no DB — so announcing through <Chrome> costs nothing. Use `var(--s*)` tokens for the gaps (the current 18/12 are magic numbers).
  - new Burmese → K15
- **counter-10** [low/S] (row K29) — More rows print a duplicate "→" beside the chevron and their English echo at 11px — **real · low**
  - anchor: `apps/qr/lib/i18n/staff.ts:767`
  - fix: Strip the trailing " →" from the six `floor.nav.*` values (the chevron IS the disclosure; the doors' `floor.door.*` keys already carry none) and set `.staff-row-name > .chrome-pair > .chrome-en` to `--fs-sm`. One dictionary edit, one CSS line; `StaffDoors.test.tsx`'s selector guard still holds.
  - verifier: Confirmed. staff.ts:783-797: eight `floor.nav.*` values end in a literal ` →` (kitchen, approvals, approvalsCount, menuPrices, menuAvailability, tips, pin, pinSet); MoreGrid renders the label at StaffDoors.tsx:161 beside an `aria-hidden` chevron at :163-165, so a row reads 'Kitchen → ›'. globals.css:10315-10316 `.staff-row-name > .chrome-pair > .chrome-en { font-size: var(--fs-xs) }` = 11px (tokens.css:38), under 19px Padauk (:10308-10313). K29 filed both. Correction: §6's 13px floor is stated for Burmese stacked diacritics, not the English echo — the size claim rests on K29's `--fs-sm` ask and the doors' own echo at `--fs-h3` (:9845-9846), not on §6.
  - on the fix: Strip the arrow from all EIGHT keys, not six — `floor.nav.approvals`/`approvalsCount` are the bar circle's sr-only name at page.tsx:131, and an SR reads '→' as 'right arrow'. strings.test.ts:212 `DECOR` strips arrows before comparing so it survives; StaffDoors.test.tsx:155-169 only checks selector liveness. This is the 'drop the glyph' option P2o leaves open — note it there. `--fs-sm` on the echo is one CSS line.

**What the verifier found that the auditor missed:**

- ROW-STATUS: K29 — the More-row English echo is 11px (`--fs-xs`, globals.css:10316), one step smaller than the 12px the row filed; hairline half is closed (register-stage.ts:233), press/cap/arrow halves live.
- apps/qr/components/staff/ExpoBoard.tsx:400 `gap: 8` and TableCard.tsx:99 `marginLeft: 6`, :110 `gap: 6`, :213/:223/:233/:242-243 `gap: 8/10/7/10, marginTop: 2` — hardcoded spacing on the counter's two card types where `--s2`/`--s3` (tokens.css:12-13) exist; house rule counts a magic number as a defect (RUBRIC #10).
- apps/qr/app/globals.css:3320-3324 `.staff-btn:not(:disabled):hover/:active` has no `[aria-disabled="true"]` twin (only `.checkout-pill` at :2701 does), so the moment counter-2/-3 move to the §17 idiom a busy `.staff-btn` keeps its brightness/scale feedback while refusing the tap — fix it in the same commit.

## Manager rails + the table drill-down

_Auditor’s surface line:_ Manager rails + the table drill-down (approvals · refunds-needed strip · settled today · refund sheet · loss sheet · manager PIN step-up · FloorDetailLive · add-to-table browser + mod sheet · settle/clear/merge/tab buttons)

- **manager-1** [high/M] (row M34) — Approvals zone swallows session expiry into 'Not updating right now' forever — no path to sign in (M34 still live) — **real · low**
  - anchor: `apps/qr/components/staff/ApprovalsBoard.tsx:183`
  - fix: Give `listPendingApprovals` (lib/approvals.ts) the `KitchenPoll`/`getTableDetail` shape — `{ kind: "rows", rows } | { kind: "signin" } | { kind: "outage" }` (catch `AuthzError` 401/403 → signin, 503 → outage) — with a suite that falsifies each arm. In `refresh()` branch on the kind: `signin` → `window.location.assign("/staff/login")` (the FloorDetailLive arm), `outage` → `nextDegraded(d, "outage", …)` (the side IS known now), rows → setSnap. Keep the `unknown` cause only for a thrown/timed-out transport. ApprovalsBoard.test.tsx gets one case: an expired session leaves the page, never the frozen copy.
  - verifier: The mechanism is real: ApprovalsBoard.tsx:178 `if (queue.status === "rejected") throw queue.reason;` → :183-187 counts every rejection as a miss and paints `unknown` after two, and lib/approvals.ts:316 `await requireStaff("manager")` throws AuthzError 401 (lib/staff.ts:137) on an expired session. But the stated failure — 'waits for a reconnect that can never come' — does not happen on the page this board lives on: apps/qr/app/staff/page.tsx:227/230 mount FloorBoard and ExpoBoard beside it, and FloorBoard.tsx:80 `window.location.assign("/staff/login")` / ExpoBoard.tsx:88 redirect the whole page on their own poll's signin verdict within one interval. The auditor's 'FloorDetailLive on the same page' is wrong — that is /staff/table/[id]. So M34 is still live as a code defect (the board cannot discriminate) but a manager sees the frozen copy for at most one sibling poll, not forever. Severity drops to low.
  - on the fix: Discriminated result from listPendingApprovals mirroring getTableDetail's {rows|signin|outage} is the filed M34 fix and respects the rules (lib/ rule + suite; no money change). Keep `unknown` for a thrown/timed-out raceTimeout only. Cheap; do it, but rank it as M34's low, not high.
- **manager-2** [high/M] — Native `disabled` on just-tapped controls across the whole surface — every stepper tap, confirm, refresh and add drops keyboard/AT focus to <body> — **real · med**
  - anchor: `apps/qr/components/staff/StaffLineEditor.tsx:254`
  - fix: Two layers. (1) `packages/ui/src/stepper.tsx`: swap `disabled` for `aria-disabled={disabled || undefined}` on both buttons and refuse in the handler (`if (disabled) return;`), keep the dimmed style keyed on the same flag; leave `soldOut`/`qty >= max` on the + as `aria-disabled` too so a mis-tap announces the reason instead of blurring. (2) Each staff site listed above: `disabled={X}` → `aria-disabled={X || undefined}` + an early return in the handler (most already have one: `if (!canConfirm) return`, `if (pending || locked) return`), and `.staff-btn:not(:disabled):active` in globals.css:3323 becomes `.staff-btn:not([aria-disabled="true"]):active`. Pre-tap refusals that are NOT 'just tapped' (a sold-out Add, `qty <= 1` on the mod sheet's −) may stay native. A jsdom case on StaffLineEditor: focus stays on the + after a tap while the transition is pending.
  - verifier: Confirmed at every anchor I opened: StaffLineEditor.tsx:254 `disabled={busy}` into packages/ui/src/stepper.tsx:72 `disabled={disabled}` and :94 `disabled={incDisabled}`; StaffLineEditor.tsx:224 and :283; ApprovalsBoard.tsx:506 `disabled={!canConfirm}` (canConfirm includes !pending) and :528 `disabled={pending}`; RefundActionSheet.tsx:219/:228; LossActionSheet.tsx:410/:424/:443; StaffAddButton.tsx:82 `disabled={pending || !!soldOut}`; StaffModSheet.tsx:199. globals.css:3323 `.staff-btn:not(:disabled):active` keys on the native attribute. DESIGN-LANGUAGE §17 states the rule verbatim and counts '64 native disabled sites remain across 27 other staff components (K35)' — so this IS a filed row (K35), which the auditor missed (filed_row null). Impact is keyboard/AT continuity on a mostly-touch tablet, so med rather than high; the stepper's three-taps-three-trips-to-the-heading via FloorDetailLive.tsx:166-170 is accurate.
  - on the fix: Two-layer fix is right. Notes: (1) keep `.staff-btn:not(:disabled)` AND add `:not([aria-disabled="true"])` — dropping the native selector un-styles the pre-tap refusals the auditor says may stay native; (2) the stepper is a diner primitive too — the aria-disabled swap must keep the sold-out/`qty >= max` dimming and the handler refusal, and needs a stepper suite case; (3) file under K35, not a new row.
- **manager-3** [high/M] — 'Mark refunded' is one tap with no confirm, no undo and no busy state — a mis-tap hides a stranded charge from the tablet, and a double-tap submits twice — **real · high**
  - anchor: `apps/qr/components/staff/RefundsNeededStrip.tsx:111`
  - fix: Same shape as `ClearTableButton`: tap → an inline `role="group"` confirm row (`tf(lang, "table.appr.a11y.confirmRefunded", { x: r.paymentIntent })`, focus moved into it via a ref, back to the trigger on cancel) with the question naming the amount + PI ('Refunded $12.40 in Stripe? ↦ Yes, mark it done / Cancel'); wrap the action in `useTransition` and render the confirm button `aria-disabled` + refused while pending with a `table.appr.verb.marking` label. New keys: `table.appr.confirmRefunded.q`, `table.appr.verb.markRefunded.confirm`, `table.appr.a11y.confirmRefunded` (MY drafts → K15). ApprovalsBoard.test.tsx already has 'marking a row refunded removes it once the server has confirmed' — extend it: the first tap does NOT call `resolveRefundNeeded`.
  - verifier: Confirmed: RefundsNeededStrip.tsx:91-103 `<form action={async () => { await resolveRefundNeeded(r.id); … }}>` fires on the first tap of the submit at :111; there is no confirm step, no useTransition/useFormStatus, no aria-disabled while in flight, and no undo. The row leaves the strip on the next poll (ApprovalsBoard.tsx:160-163 filters resolvedIds). Every other destructive control on this surface is two-step or PIN-gated (ClearTableButton, CashSettle, Merge, CloseSecureTab focus their confirm groups — grep at :36/:102/:56/:37). House rule 'destructive taps two-step OR undoable' is violated on the one control whose mis-tap hides a stranded charge from the console. Not a money-semantics change — the ledger marker's UI only.
  - on the fix: ClearTableButton-shaped inline confirm group with focus in/out via refs + useTransition + aria-disabled refusal is the house shape; new keys are MY drafts → K15. Extend the existing test so the FIRST tap does not call resolveRefundNeeded. Keep the row's `role="status"` failText as the strip's one region.
  - new Burmese → K15
- **manager-4** [med/S] — Approvals card: tapping Approve/Deny unmounts the button and moves focus nowhere — the PIN step opens with focus on <body> — **real · med**
  - anchor: `apps/qr/components/staff/ApprovalsBoard.tsx:347`
  - fix: Add `const formRef = useRef<HTMLFormElement>(null)` + `const openerRef = useRef<HTMLButtonElement>(null)` on the card; `<form ref={formRef} tabIndex={-1} aria-labelledby={`appr-q-${request.id}`}>` with the confirm sentence as the labelled heading; an effect keyed on `decision`: opening → `formRef.current?.focus()` (or the manager `<select>` directly, `document.getElementById(`appr-${request.id}-mgr`)`), closing via cancel → `openerRef.current?.focus()` (the Approve/Deny row remounts, so attach the ref to whichever button matches the last decision). One jsdom case in ApprovalsBoard.test.tsx: after `fireEvent.click(Approve)`, `document.activeElement` is inside the form.
  - verifier: Confirmed: ApprovalsBoard.tsx:347-350 `open()` only sets decision/msg; :451-549 `decision === null ? <div style={btnRow}>…</div> : <form …>` unmounts the tapped Approve/Deny; ManagerPinStepUp.tsx:91 ManagerPinFields has no autoFocus (only `disabled={locked || noManagers}` at :122 and `disabled={locked}` at :157); the catch-all at :215-219 is keyed on [snap] only. Focus lands on <body> after the step change; the four sibling two-steps (CashSettleButton:102-103, ClearTableButton:36-37, MergeTableButton:56-58, CloseSecureTabButton:37-38) all move focus.
  - on the fix: Effect keyed on `decision` focusing the form (tabIndex -1, aria-labelledby the confirm sentence) and returning to the matching Approve/Deny button on cancel is the sibling pattern; S effort, one jsdom case.
- **manager-5** [med/S] — Refund sheet keeps a wrong PIN in the masked field after 'wrong PIN' — the next Refund tap re-sends it and burns another attempt toward lockout — **real · med**
  - anchor: `apps/qr/components/staff/RefundActionSheet.tsx:58`
  - fix: In `submit`'s failure branch, `setPin("")` before the switch (mirror ApprovalsBoard:367), and move focus back to `#refund-pin` after a `pin_wrong`/`pin_no_pin`/generic refusal (`document.getElementById("refund-pin")?.focus()`), leaving the lockout arm alone (its field should read `readOnly`, §17). Consider hoisting the PIN field into `ManagerPinFields`-style shared code so the three step-ups cannot diverge again. A jsdom case: a `pin_wrong` answer empties the field.
  - verifier: Confirmed: RefundActionSheet.tsx has exactly one `setPin(` — the input onChange at :196; the failure switch at :56-108 never clears the field (contrast LossActionSheet.tsx:151 `setPin("")` and ApprovalsBoard.tsx:367 `setPin("")` before their switches). The field is `type="password"` (:191) and the Refund button is gated only on `pending || pin.length < 4` (:228), so a second tap re-sends the same wrong PIN and spends an attempt.
  - on the fix: `setPin("")` before the switch + focus back to #refund-pin is right. Add what the auditor left out: destructure `locked` from useLockout (ManagerPinStepUp.tsx:48 returns it) and gate submit on it, and make the field `readOnly` during lockout per §17 (see missed).
- **manager-6** [med/M] (row P2t) — Loss sheet is still English under a Burmese console — the title is a literal (behind a stale 'Sheet.title is a string' comment) and 12 refusals are `setMsg("…")` literals (P2t) — **real · med**
  - anchor: `apps/qr/components/staff/LossActionSheet.tsx:299`
  - fix: Title: `title={<Chrome lang={lang} k={action === "comp" ? "table.loss.title.comp" : "table.loss.title.void"} vars={{ x: line.name }} />}` (two keys, MY drafts → K15) and delete the stale comment. Refusals: one `table.loss.msg.*` key per arm (`reasonRequired` already exists as `table.loss.reasonRequired` — reuse it for the two 'Pick a reason first.' arms; the mid-payment / not-open / not-found sentences match `table.appr.msg.inFlight` / `notOpen` and can share keys) rendered via the existing `<MsgText>`; `setMsg({ k: … })` everywhere. ~9 new MY strings → K15.
  - verifier: Confirmed: LossActionSheet.tsx:293-299 `const titleVerb = action === "comp" ? "Comp" : "Void"` → `title={`${titleVerb} “${line.name}”`}` at :311, behind a comment (:293-297) claiming Sheet.title is `string`; packages/ui/src/sheet.tsx:37 declares `title: React.ReactNode` and RefundActionSheet.tsx:132 already passes `<Chrome …/>`. Measured `grep -c 'setMsg("'` = 12 in LossActionSheet.tsx (:171/:174/:177/:189/:197/:229/:255/:258/:261/:263/:266/:274); StaffMsg.tsx:18 accepts a string arm and :21 routes it through OutageText, so the literals reach the live region in English.
  - on the fix: Two title keys + per-arm `table.loss.msg.*` keys via the existing MsgText path; reuse `table.loss.reasonRequired`, `table.appr.msg.inFlight`/`notOpen` where the sentence already exists. New MY strings → K15. Delete the stale comment. P2t's LossActionSheet half closes with this.
  - new Burmese → K15
- **manager-7** [med/M] — Five pressed states on this surface use an accent fill or tint instead of the one lit-gold-cap vocabulary — **real · low**
  - anchor: `apps/qr/components/staff/LossActionSheet.tsx:484`
  - fix: One class in globals.css beside `.staff-seg` — `.staff-chip[aria-pressed="true"]` — whose declarations are LIFTED from `.kds-chip[aria-pressed="true"]` (gradient fill + inner sheen + `--glow-gold`, `--ink` on the gold fill per §2), plus the vellum-ghost idle state; the Void/Comp segment becomes `<div className="staff-seg">` with two `.kds-chip` buttons. Delete `segBtnOn`, `reasonBtnOn`, `chipOn`, `optBtnOn`, `tipChipOn` and set `className="staff-chip"` on those buttons (they all already carry `aria-pressed`). Run `composite-contrast.test.ts` for the ink on the gradient — it is the pinned number.
  - verifier: Partly overstated. The console's ONE cap (globals.css:7451-7460) is `background: var(--ac); color: var(--oa); inset sheen + --glow-gold` — the CSS comment at :7439-7441 says Night's --ac IS the gold. So segBtnOn (LossActionSheet.tsx:484-488), chipOn (StaffMenuBrowser.tsx:364-369) and optBtnOn (StaffModSheet.tsx:275-281) share the cap's fill and ink and only lack the sheen/halo (plus carry a visible accent border) — a partial cap, not a second idiom. Two genuinely differ: reasonBtnOn (:509-513, 10% tint on --cd — exactly the tint the same CSS comment names as 'a SECOND vocabulary (K29)') and tipChipOn (CashSettleButton.tsx:431-436 ring + 9% wash) whose own comment claims to mirror the checkout tip chips while `.checkout-tip-on` (globals.css:2906-2915) wears the full gradient cap. Real, but two vocabulary slips plus three sheen-less copies — low.
  - on the fix: Do NOT 'lift' the declarations into a new `.staff-chip[aria-pressed]` rule: the file's own comment at :7446-7448 forbids copies ('share this rule rather than carrying copies') and DESIGN-LANGUAGE §17 says a guard reddens when a pressed selector grows a second fill. Correct shape: append the new selector to the ONE rule at globals.css:7451 (or give those buttons `.kds-chip` inside `.staff-seg`), delete the five inline on-styles, and let the shared rule own the cap. Then run composite-contrast.test.ts as the auditor says.
- **manager-8** [med/S] — Table drill-down skeleton does not match the live geometry: no status-chip row, 36px guest chips vs ~30px, 38px control pills vs 44px steppers, off-token paddings, English sr-only literal — **real · low**
  - anchor: `apps/qr/app/staff/table/[id]/loading.tsx:20`
  - fix: Rebuild `loading.tsx` from the live constants: a first row of two 24px pill skeletons (chip row), then the 16px sub line; cards with `padding: "var(--s5)"`, `marginBottom: "var(--s4)"`; guest chips 80×30 radius `var(--r-full)`; order rows = a 60% bar + a 30% bar on the left and on the right two 44px circles + one 90×44 pill; keep the bar block as is (it already mirrors StaffBar). Replace the sr-only literal with `aria-busy` on the `<main>` and a `<Chrome>`-rendered `what.table`-style key via `readStaffLang()` if loading.tsx may be async, otherwise mark it `lang="en"` and file it. Effort S, one file.
  - verifier: Geometry claims check out: live header starts with a chip row (FloorDetailLive.tsx:247-266 FloorStatusChip + Badges) while loading.tsx:20 opens with one 200×16 bar; live cards use `padding: var(--s5)` / `marginBottom: var(--s4)` (:887; tokens 20px/16px) vs skeleton 16/14 (:21, :29); guest chips `padding: 4px 12px` at --fs-sm (:936-942, ≈31px) vs 88×36 (:25); order rows carry a 44px note pill + 44px stepper pair (StaffLineEditor.tsx:218-263) vs one 110×38 pill (:40). The sr-only 'Loading the table…' literal at :9 is confirmed. Visible for a fraction of a second, so low.
  - on the fix: Rebuild from the live constants with tokens, not px. For the literal: loading.tsx is a server boundary — reading the lang cookie there is fine for this dynamic route, but the safer S-sized move is `aria-busy` on <main> plus `lang="en"` on the span and a K15 note, as the auditor's fallback says.
- **manager-9** [low/M] — Every staff sheet's ✕ is named by a literal English aria-label in the primitive — under Burmese the exit announces 'Close' in English on the refund, void/comp and modifier sheets — **real · low**
  - anchor: `packages/ui/src/sheet.tsx:266`
  - fix: Add an optional `closeLabel?: React.ReactNode` (and `busyCloseLabel?`) to `Sheet`; when provided, render it as `<span className="sr-only">{closeLabel}</span>` inside `Dialog.Close` and drop the `aria-label` (the icon stays `aria-hidden`). Staff callers pass `<Chrome lang={lang} k="sheet.close" />` / `k="sheet.close.busy"`; diner callers keep the default. Two new keys (MY drafts → K15). `Sheet` tests: the name is the sr-only text when supplied.
  - verifier: Confirmed: packages/ui/src/sheet.tsx:266 `aria-label={busy ? "Close — finishing, please wait" : "Close"}` is a literal; scripts/check-staff-lang.mjs:296 `ARIA_DIRS` = components/staff, app/staff, app/board only, so the primitive is outside rule 3's reach; RefundActionSheet, LossActionSheet and StaffModSheet all render through Sheet, so the ✕ — the first tabbable element per the comment at :255-263 — announces in English under lang=my.
  - on the fix: Optional `closeLabel`/`busyCloseLabel` ReactNode rendered as sr-only text, aria-label dropped when supplied, icon stays aria-hidden; staff callers pass <Chrome>. Same widening precedent as `title: ReactNode`. Two keys → K15; a Sheet test for the name.
  - new Burmese → K15
- **manager-10** [low/M] (row M76) — Sheets still cut on close — refund, void/comp and modifier sheets slide in over a beat and vanish in one frame (M76 still live) — **real · low**
  - anchor: `packages/ui/src/sheet.tsx:88`
  - fix: Primitive-level, as filed: `<Dialog.Portal forceMount>` wrapped in framer `AnimatePresence` (already a dependency; `StaggerList` uses it) so the `m.div` gets an `exit={{ y: "100%" }}` + the overlay an `exit={{ opacity: 0 }}` over `--dur-sheet`, gated by `useAnimationPreference().shouldAnimate` (reduced motion → `exit={undefined}`, unmount instantly). Keep the CSS `up` for the entrance or move it to framer `initial/animate` so there is one motion owner. Restore-focus timing is unchanged (Radix fires `onCloseAutoFocus` on the close, not the unmount).
  - verifier: Confirmed: sheet.tsx:88 `<Dialog.Portal>` has no forceMount and no AnimatePresence anywhere in the file; globals.css:181 `.mms-sheet { animation: up var(--dur-sheet) var(--spring) }` is entrance-only; grep for `data-state="closed"` / `forceMount` over globals.css + sheet.tsx returns nothing. M76 is still live as filed.
  - on the fix: Framer AnimatePresence + forceMount works, but forceMount must go on Portal, Overlay AND Content (Radix Presence unmounts each), and the exit must be gated on `useAnimationPreference().shouldAnimate` (exported from packages/ui/src/index.ts:7) so RM gets an instant unmount. Pick one motion owner (move `up` to framer initial/animate) rather than CSS-in + framer-out.

**What the verifier found that the auditor missed:**

- apps/qr/components/staff/StaffLineEditor.tsx:286 — `{notePending ? "…" : <Chrome … k="table.line.save" />}`: while a kitchen note saves, the Save button's only content — and therefore its accessible name — collapses to an ellipsis (no aria-label, no sr-only), on top of the native `disabled={notePending}` at :283 blurring it. TeamManager.tsx:352 documents the same shape as a known defect; needs a `table.line.saving` key rendered visibly (K15 draft).
- apps/qr/components/staff/RefundActionSheet.tsx:47 + :228 — `const { setLockLeft, lockCopy } = useLockout(lang)` never takes `locked` (ManagerPinStepUp.tsx:48 returns it), so during the lockout countdown the Refund button stays tappable (`disabled={pending || pin.length < 4}`) and the PIN field is neither readOnly nor gated — unlike ApprovalsBoard.tsx:345 `canConfirm = … && !locked` and LossActionSheet.tsx:410 `pending || locked`; §17 says a lockout makes the field readOnly. A locked manager can keep re-sending into the lock.
- ROW-STATUS: M34 — still-live in ApprovalsBoard's own code (:178-187), but the practical failure is masked on /staff: FloorBoard.tsx:80 and ExpoBoard.tsx:88, mounted on the same page (app/staff/page.tsx:227-241), redirect to /staff/login on their own poll's signin verdict within one interval, so the frozen copy never persists 'forever'. The auditor's sibling citation (FloorDetailLive) is the /staff/table/[id] page, not this one. Also: manager-2 is the already-filed K35 (DESIGN-LANGUAGE §17 names it), not an unfiled finding.

## /board — the order-ready TV

_Auditor’s surface line:_ /board — the order-ready TV board (apps/qr/app/board/page.tsx · components/ReadyBoard.tsx · api/board/route.ts · lib/board-pulse.ts · lib/board-poll.ts · globals.css .orb-\*)

- **board-1** [high/M] — No rush behaviour: columns grow unbounded, the runner's 'Food up' band is pushed off the TV, nothing says '+N more' — **real · high**
  - anchor: `apps/qr/components/ReadyBoard.tsx:281`
  - fix: A pure `boardColumnFit(count, cap)` in `lib/board-fit.ts` (value-falsifiable, with a suite) returning `{ shown, more }`; render the first `shown` cards and one `.orb-more` row `+{n} more` through the EXISTING `kds.more` key (`tf(lang, "kds.more", { n })` — no new MY) at `.orb-empty` scale; give `.orb-col` `overflow: hidden` and pin the band with `.orb-pulse { flex: none }` so it can never leave the viewport. Ready stays newest-first (the guest walking up), Preparing oldest-first (the next bag up) — the cut falls on the end that matters least in each.
  - verifier: Confirmed. ReadyBoard.tsx:281–285 and 296–306 map every row of each column into a bare `<ul role="list">` with no cap; globals.css:8028 `.orb-root { min-height: 100dvh; display:flex; flex-direction:column }` (min-height, so the root GROWS), 8061 `.orb-cols { flex:1; grid; min-height:0 }` and 8097 `.orb-col ul { list-style:none; margin:0; padding:0 }` — no `overflow` on `.orb-col`, no `height` bound anywhere, no `+N more` in the component. The route (route.ts:77 `BOARD_ORDER_CAP = 60`, 170–175 warns on saturation) can send up to 60 rows and the wall renders all of them. O-D's target is 'nothing hides silently' — here the pulse band and the oldest cards leave the bottom of a screen nobody can scroll.
  - on the fix: Shape is right (pure `boardColumnFit` in lib/ with a suite, reuse `kds.more` — no new MY, `.orb-col { overflow:hidden }`). Two corrections: (1) the band cannot be pinned by `.orb-pulse { flex:none }` alone while `.orb-root` is `min-height` — the root must become `height:100dvh` (or the cols get a bounded `max-height`) or the page still grows; (2) the `cap` is a viewport fact, not a constant — derive it the way P4 measures the KDS envelope (`kds-size.test.ts`) or from a ResizeObserver on `.orb-col`, and pin the chosen number in the suite; a hand-picked 6–8 will be wrong on the next TV.
- **board-2** [high/S] — A stale board is indistinguishable from a live one at three metres — **real · med**
  - anchor: `apps/qr/components/ReadyBoard.tsx:256`
  - fix: Stamp `data-stale` on `.orb-root` when `stale`; under it dim both columns to the paper's secondary ink (`.orb-root[data-stale] .orb-card { color: var(--t2) }`, RM-safe since it is a state, not motion) and promote the existing `board.reconnecting` sentence into a full-width band at `.orb-empty` scale (`clamp(18px, 1.8vw, 30px)`) directly under the header — still the ONE `role="status"` node, just larger. One jsdom assertion in the existing stale test (`container.querySelector('.orb-root[data-stale]')`). No new copy.
  - verifier: Confirmed mechanism, severity trimmed. ReadyBoard.tsx:240 `const stale = …state.stale` reaches the cards only as `!stale` on the wait span (542) — the name and code render at `.orb-card`'s full `clamp(28px,3.6vw,54px)` / 800 (globals.css:8102–8112) with no stale class or attribute anywhere (`grep data-stale` across ReadyBoard.tsx and globals.css: none). The tells are the 20px `.orb-status` (8055–8060, `--t2`) and — which the auditor missed — the pulse band's `board.pulse.unavailable` note at `clamp(15px,1.4vw,24px)` (325 passes `pulse=null` when stale; 375–378 renders it). Two secondary-ink sentences ≤24px under 54px names is still unreadable from the room, so the defect stands; it is `med` because the board already blanks every age and says so twice in text.
  - on the fix: `data-stale` on `.orb-root` + `.orb-root[data-stale] .orb-card { color: var(--t2) }` is token-pure and RM-irrelevant; `--t2` on `--pg` is an asserted pair (contrast-audit.test.ts, cited at globals.css:8161). Keep the ONE `role="status"` node; the enlarged sentence must stay that node, not a second region. One jsdom assertion in the existing 'stale' describe (ReadyBoard.test.tsx:330) fits.
- **board-3** [med/S] (row K27) — The kitchen-pulse band — the only 'act now' text for a runner — is phone-sized on a wall TV — **real · med**
  - anchor: `apps/qr/app/globals.css:8230`
  - fix: Lift the band onto the same viewport scale the columns use: `.orb-table { font-size: clamp(22px, 2.1vw, 36px) }` with the state at `0.8em` and `--tx` (not `--t2`) on the `up` chip; `.orb-stat span`, `.orb-rail h3`, `.orb-rail-more` to `clamp(14px, 1.2vw, 20px)`; `.orb-rail-row` to `clamp(18px, 1.7vw, 30px)`. Same token pairs (`--tx`/`--t2`/`--gold` on `--pg`) so contrast coverage is unchanged. Keep the band static (its no-motion rule is right).
  - verifier: Confirmed against the current file (lines shifted): globals.css:8243–8251 `.orb-table { … font-size: clamp(15px, 1.4vw, 24px) }`, 8256–8262 `.orb-table-state { font-size: 0.68em; … color: var(--t2) }` → 16.3px at 1920; 8228–8234 `.orb-stat span { font-size: clamp(11px, 0.85vw, 14px) }`; 8274–8285 `.orb-rail h3 { font-size: clamp(11px, 0.85vw, 14px) }`; 8323–8328 `.orb-rail-more { font-size: clamp(11px, 0.9vw, 15px) }`. Beside 54px column cards the only act-now text (`Table 3 · Food up`, ReadyBoard.tsx:451–458) is ~16px. K27's row names `/board` explicitly; O-A's bar is ≥28px at 1–2m.
  - on the fix: Viewport clamps are the block's own idiom (every .orb-\* size is one), so no token rule is broken. If board-9's lit cap is adopted on `.orb-table-up`, the up-state ink is `--oa` (ink follows the FILL, §2), not `--tx` — reconcile the two fixes in one edit.
- **board-4** [med/M] — The sound control unmounts on the tap that arms it — focus drops to body and there is no mute or volume afterwards — **real · med**
  - anchor: `apps/qr/components/ReadyBoard.tsx:263`
  - fix: Keep the chip mounted as a toggle: `aria-pressed={soundOn}` wearing the gold cap when on (the ONE selection vocabulary, `.kds-chip[aria-pressed="true"]` already declared), name through `al()` with two keys — the existing `board.sound` and a new `board.sound.on` ("Sound on" / MY draft → K15) — and a second tap mutes (`setKdsVolume(0)` or a local `muted` guard in `play`). Focus never leaves the element. Optionally the KDS's `.kds-vol` slider beside it. Pair with board-10 so the chip finally has a resolvable size.
  - verifier: Confirmed. ReadyBoard.tsx:263 `{!soundOn && (<button … className="kds-chip" onClick={enableSound}>` — `enableSound` (191–196) sets `soundOn=true` on a successful arm, which unmounts the only control on the page mid-tap, dropping focus to `<body>` (the §17 rule against native `disabled` exists for exactly this). After that there is no mute, no volume and no visible 'sound on' state; `KdsChime.play` reads `getKdsVolume()` → `KDS_DEFAULT_VOLUME = 0.8` (kds-sound.ts:41, 59) and nothing on the board writes it. §7: 'controls stay rendered-and-disabled with a reason, never vanish'.
  - on the fix: Keep it mounted as an `aria-pressed` toggle wearing the shared cap (`.kds-chip[aria-pressed="true"]`, globals.css:7451) — correct. But NOT 'name through `al()` with two keys': `al()` takes a `StaffControl` discriminated union (staff-labels.ts:80, 168), not dictionary keys, and rule 3 refuses an aria-label on a control with children anyway. The name is the VISIBLE dictionary text: swap `board.sound` ↔ new `board.sound.on` (MY draft → K15). Mute via a local `muted` guard in the handler, not `setKdsVolume(0)` — that persists 0 to the device, so the next reload arms 'sound on' at volume zero. Also handle `arm()` returning false (see missed).
  - new Burmese → K15
- **board-5** [med/S] — A hardcoded English sentence and the brand name bypass the dictionary on the unlinked screen; two `board.*` keys are dead — **real · low**
  - anchor: `apps/qr/components/ReadyBoard.tsx:213`
  - fix: `board.signin` key with a `{path}` slot rendered through `tf()` with the Latin path in a `<Chrome>`/`lang="en"` span (MY draft → K15); `<h1>` reads `BRAND_NAME` from `lib/brand.ts` (the W22r singleton rule: adopt as touched); delete `board.unlinked` and `board.unavailable` or wire the latter as the `retry`-path sentence. One assertion in the suite: the unlinked branch has no unmarked Latin outside the path span.
  - verifier: Confirmed. ReadyBoard.tsx:213–215 `<p className="orb-empty">A manager can sign in on this screen at <strong>/staff/login?next=/board</strong>.</p>` — a literal English sentence with no `lang`, directly under the localized refusal (210–212), so a Burmese screen shows a Burmese refusal followed by unmarked English. Brand name is a literal at 202, 228, 251 while `lib/brand.ts:14` exports `BRAND_NAME` (adopted by ReceiptCard, StaffLogin, SignedInCard). `board.unlinked` (staff.ts:1597) and `board.unavailable` (1617) have zero call sites outside staff.ts across apps/scripts/docs. `check-staff-lang` rule 3 scans JsxText only under a control (365) and rule 5 checks Burmese marking, so neither sees an unmarked English sentence — the auditor's 'cannot see it' is right. Low: it is a manager-only setup screen and the actionable part (the path) is Latin regardless.
  - on the fix: `<Chrome lang={lang} k="board.signin" vars={{ path: "/staff/login?next=/board" }}/>` — `Chrome` already wraps Latin vars in `lang="en"` (ReadyBoard.tsx:445 docblock, the table-number case). `BRAND_NAME` for the three `<h1>`s (and page.tsx:5 metadata title). Deleting the two dead keys is safe (no ratchet entry references them). The existing 'no English inside lang=my' test (ReadyBoard.test.tsx:140) does not cover this branch — add the assertion the auditor names.
  - new Burmese → K15
- **board-6** [med/S] — Always-on posture stops at Night: fixed gold rules and static chrome for 12 h/day with no drift or idle dim — **real · low**
  - anchor: `apps/qr/app/globals.css:8067`
  - fix: An RM-gated `orbDrift` keyframe on `.orb-root` — `transform: translate(±2px, ±2px)` over ~600s, `animation-play-state` paused under `prefers-reduced-motion` (transform only, no layout); drop the 2px solid gold rule to a `--bd` hairline and keep gold on the heading TEXT only (the flash and `.orb-table-up` remain the gold that moves); optionally an idle dim to `--t2` on the static chrome after N minutes with an empty Ready column. No copy.
  - verifier: Mechanism confirmed, standard partly met. globals.css:8093–8096 `.orb-col-ready h2 { color: var(--gold); border-bottom-color: var(--gold); }` over 8068–8075 `border-bottom: 2px solid …`; `.orb-root` is Night-forced (ReadyBoard.tsx:200/226/248 `orb-root dark`) and nothing in the block or component drifts, dims or schedules (`grep -i burn` → only the KDS comment). SPEC-KDS:71 already credits Night as the burn-in choice, so the O-F 'burn-in-aware theme' bar is met at the theme level; a fixed 2px full-brightness gold rule and a static serif title for 12h/day is the residual risk on OLED. Polish, not a live defect a person sees this week — low.
  - on the fix: An RM-gated ±2px `transform` drift is fine in principle, but NOT on `.orb-root`: it is the element painting `--pg` (8035) and `<main>` in page.tsx:30 is unthemed, so translating the root exposes a 2px LIGHT-paper strip of the body at the edge. Drift an inner wrapper (or head/cols/pulse) instead. Dropping the READY rule to `--bd` is acceptable since the heading text keeps `--gold`; the idle-dim after N minutes is a second timer with no suite — skip it.
- **board-7** [med/M] (row K28) — The Ready-shelf wait has no ceiling — an untapped bag reads '1440 min' to the room — **real · low**
  - anchor: `apps/qr/components/ReadyBoard.tsx:544`
  - fix: The K28 formatter in `lib/kds-time.ts` shared with the KDS header/strip: `{mins} min` under 60, `{h} h` after, and for this GUEST surface a ceiling (`60+ min`) past which a bigger number carries no information; pure rule + suite + a mutant on the ceiling; the Burmese hour form is one new K15 draft. `BoardCard` renders the formatter's key/vars, never its own arithmetic.
  - verifier: Confirmed on the code, wrong on the premise. route.ts:300–303 `readyMinutes: … Math.max(0, Math.floor((dbNowMs - Date.parse(o.togo_ready_at)) / 60_000))` floors at 0 and has no ceiling; the 24h `created_at` floor (route.ts:121, 133) bounds it at ~1440. ReadyBoard.tsx:544–546 renders `tf(lang, "board.card.wait", { mins: wait })` for any integer. BUT `apps/qr/lib/kds-time.ts` EXISTS (K28's formatter: `elapsedParts`/`fmtElapsed`/`spokenElapsed`, bands mmss·hm·days) and is wired in KdsBoard.tsx:32 — the auditor's 'No lib/kds-time.ts exists' is false. The /board half of K28 is open; the lib half is done. Low: the number is honest (the bag IS untapped) and the route comment says the cure is tapping; the auditor's own K28 row files it low.
  - on the fix: Reuse `kds-time.ts` rather than a parallel rule — add a minutes-in variant (`readyMinutes` arrives as whole minutes, not ms; do not re-derive from timestamps on the TV) returning a key + vars, a value-falsifying case per band edge in kds-time.test.ts, and one MY draft for the hour form → K15. `BoardCard` renders the key only.
  - new Burmese → K15
- **board-8** [low/S] — Adding the wait made the card a three-item space-between row: the code drifts to mid-row, away from the name it identifies — **real · low**
  - anchor: `apps/qr/app/globals.css:8076`
  - fix: Group identity: wrap name + code in a leading `<span class="orb-id">` with `gap: .35em` (or `.orb-code { margin-right: auto }`), leaving `.orb-wait` alone at the trailing edge; keep `align-items: baseline`. One line of CSS, no copy, no suite change (the existing `.orb-wait` assertions still hold).
  - verifier: Confirmed. globals.css:8102–8106 `.orb-card { display:flex; align-items:baseline; justify-content:space-between; gap:20px; …}` and ReadyBoard.tsx:540–548 render up to three siblings (name, `.orb-code`, `.orb-wait`), so with a wait present the code is centred in the column, detached from the name it identifies; a Preparing row (two children) keeps name-left/code-right — two rhythms on one wall.
  - on the fix: `.orb-code { margin-right: auto }` (or the `.orb-id` wrapper) is one line, token-free, and the suite's `.orb-wait` assertions (ReadyBoard.test.tsx:420, 425) are unaffected. Drop `justify-content: space-between` when adding the auto margin so the two-child row lays out the same way.
- **board-9** [low/S] — Two vocabulary slips: `.orb-table-up` is a gold OUTLINE, not the lit cap, and the flash paints `background` instead of opacity — **real · low**
  - anchor: `apps/qr/app/globals.css:8237`
  - fix: Lift the pressed-chip declarations onto `.orb-table-up` verbatim (the P7·1b pattern; `--tx` on gold-20%/`--cd` in `.dark` is the pair every pressed KDS chip already ships, so coverage exists) and keep `.orb-table-up .orb-table-state { color: var(--tx) }`. For the flash: `.orb-card { position: relative }` + `.orb-card-flash::before { inset: 0; background: color-mix(in srgb, var(--gold) 32%, transparent); animation: orbFlash 2s var(--ease-out) }` with the keyframes on `opacity` 1→0; the reduced-motion block stays as written.
  - verifier: Both halves confirmed. globals.css:8263–8269 `.orb-table-up { border-color: var(--gold); } .orb-table-up .orb-table-no, .orb-table-up .orb-table-state { color: var(--gold); }` — that is §2's IDLE idiom ('vellum ghosts — warm translucent fill, gold hairline'), not the lit cap the block's own comment (8166–8170) claims to extend; the console's cap is `background: var(--ac); color: var(--oa); box-shadow: inset … var(--sheen), 0 0 14px -6px var(--glow-gold)` (7451–7460). And 8130–8139 `@keyframes orbFlash { 0% { background: color-mix(…) } 100% { background: transparent } }` animates a paint property against §3's 'transform/opacity only'. RM escort exists (8147–8151).
  - on the fix: Lift the pressed-chip declarations onto `.orb-table-up` verbatim — `--oa` on solid `--ac` is asserted (contrast-audit.test.ts:303), so coverage exists; note the band docblock (8156–8164) argued flat-on-`--pg` for coverage reasons and must be rewritten with the change. Flash: `.orb-card { position: relative }` + `::before { content:""; position:absolute; inset:0; pointer-events:none; }` animating `opacity` 1→0, RM block unchanged.
- **board-10** [low/S] (row P6f) — P6f is still live: the sound chip's `--kfs-meta` never resolves outside `.kds-root` — **stale**
  - anchor: `apps/qr/app/globals.css:7432`
  - fix: Give the board its own `.orb-chip` (extends `.kds-chip`'s box + the gold cap when pressed) sized `clamp(16px, 1.3vw, 22px)`, OR add the fallback `var(--kfs-meta, var(--fs-md))` at 7432. Add `.staff-press` for the release sheen. Do it inside board-4's toggle rewrite so the chip is touched once.
  - verifier: REFUTED — P6f is already closed. globals.css:8028–8033 `.orb-root { /* P6f — `.kds-chip`reads`--kfs-meta`, declared only on `.kds-root`; … */ --kfs-meta: clamp(15px, 1.2vw, 22px); … }` declares the token on the board's own root, so the chip at ReadyBoard.tsx:266 resolves `font-size` to a viewport clamp, not an inherited body size. The auditor read a stale copy (its `.orb-root` at 8007 is 21 lines off and predates this declaration). The secondary claim is also mostly wrong: a global `:focus-visible` rule exists at globals.css:100, so the chip has a focus state; only the `.staff-press` release sheen is absent, which is a nit that belongs inside board-4's rewrite.
  - on the fix: No fix needed for the token. Add `.staff-press` to the chip when board-4 touches it; do not create an `.orb-chip` — a second chip class is the drift the shared `.kds-chip[aria-pressed]` rule exists to prevent.

**What the verifier found that the auditor missed:**

- ROW-STATUS: P6f is CLOSED, not still-live — globals.css:8028–8033 `.orb-root { --kfs-meta: clamp(15px, 1.2vw, 22px); }` with the P6f comment; the auditor's line numbers (8007/7432) are a stale read. Retire the row.
- ROW-STATUS: K28 is PARTLY-CLOSED, not still-live — apps/qr/lib/kds-time.ts exists (`elapsedParts`/`fmtElapsed`/`spokenElapsed`, suite kds-time.test.ts) and KdsBoard.tsx:32 imports it; the KDS header/strip half is done. Only the `/board` half (ReadyBoard.tsx:544–546 rendering `board.card.wait` uncapped) remains open.
- apps/qr/components/ReadyBoard.tsx:191–196 — `enableSound` swallows a failed arm: `const ok = await chime.current.arm(); setSoundOn(ok);` — on a TV whose browser refuses/lacks audio, `ok=false` leaves the same `Enable sound` chip with no message, no `aria-disabled`, no live-region sentence; a manager with a remote taps it repeatedly and nothing changes (Error/recovery rule: never strand the user). Fix inside board-4's toggle: on `false` announce a dictionary sentence through the ONE `role="status"` node (new key → K15) and mark the chip `aria-disabled` with the refusal in the handler.

## Menu · Tips · Glossary

_Auditor’s surface line:_ Menu + Tips + Glossary (manager screens): apps/qr/app/staff/menu/page.tsx · components/staff/MenuPriceEditor.tsx · StaffPromoControl.tsx · app/staff/tips/page.tsx · PilotNightSheet.tsx · app/staff/glossary/page.tsx · PrintSheetButton.tsx · lib/staff-more.ts · globals.css .pgl*/.pns*/.staff-\*

- **menu-1** [high/S] — The 86 pill and both confirm buttons go native `disabled` the instant they are tapped — focus drops to <body> — **real · med**
  - anchor: `apps/qr/components/staff/MenuPriceEditor.tsx:339`
  - fix: Mirror StaffPromoControl: `aria-disabled={flipping.has(i.id) || undefined}` with a `flippingRef` Set that refuses re-entry inside `toggleSoldOut`; `aria-disabled={busy || undefined}` on Keep/Set with the refusal at the top of `save()` (`if (busyRef.current) return`); keep `disabled={!validDraft}` only on Save-while-typing (focus is in the field) or convert it too for uniformity. Add `className="staff-btn staff-press"` to the 86/Put back, Edit, Keep and Set buttons. Extend a jsdom suite (`MenuPriceEditor.test.tsx`, none exists) with the StaffPromoControl `querySelectorAll("[disabled]")` sweep + the triple-click re-entry assertion.
  - verifier: Confirmed at apps/qr/components/staff/MenuPriceEditor.tsx:340 `disabled={flipping.has(i.id)}` on the 86/Put-back pill, :437 and :447 `disabled={busy}` on Keep/Set. The pill half is fully real: nothing re-homes focus after `flipping` clears (:125-129) — the only focus effects are the confirm-group pair (:183-187) and the post-save `searchRef.current?.focus()` (:247). The confirm half is TRANSIENT: after the in-flight window `setConfirming(false)` fires the :185 branch (`saveRef.current?.focus()`) or `closeEdit()`+search focus, so focus is dropped only for the duration of the save. Neither button carries `.staff-press`/`.staff-btn` (inline `eightySixBtn`/`restoreBtn`/`ghostBtn` at :536-586, no className). This is NOT unfiled: OPEN-ITEMS K35 (docs/OPEN-ITEMS.md:378) names "MenuPriceEditor 4" explicitly and K35's own note carves out the invalid-form `disabled={!validDraft}` (:487) as a different rule. Downgraded to med: the 86 pill is the worst K35 instance on this surface (a 115-row list), but a touch user never sees it; the row is filed low.
  - on the fix: aria-disabled + a `flippingRef` refusal mirrors StaffPromoControl.tsx:107-138/:215 and §17. Two additions: (1) `.staff-btn:not(:disabled):hover/:active` (globals.css:3320-3323) keys on NATIVE `:disabled`, so once converted the busy pill keeps its hover brightness and press scale unless the rule also excludes `[aria-disabled="true"]`; (2) the inline styles need an aria-disabled variant (opacity/cursor) since there is no attribute-keyed CSS for these buttons. File it as the MenuPriceEditor entry of K35, not a new row.
- **menu-2** [high/M] (row K24) — Every outcome and refusal renders at the TOP of the page, above a 115-row list — off-screen for any row past the first viewport, and the region's srOnly→visible swap shifts layout — **real · high**
  - anchor: `apps/qr/components/staff/MenuPriceEditor.tsx:259`
  - fix: Keep the ONE `role="status"` region permanently sr-only (no geometry change, no layout shift) and render a VISIBLE `aria-hidden` echo of the same `Msg` inside the row it concerns (the `<li>` whose id matches `msg.x`/the acted id — store the item id on `Msg`), styled with the existing `okLine`/`errLine` tokens; a server sentence stays `<OutageText>`. The row is the thing that changed, so the verdict appears where the eye already is. Alternative that avoids duplicate text: `scrollIntoView({ block: "nearest" })` on the region — but §17 forbids a second sticky element, and a scroll jump under a thumb is worse than an inline echo.
  - verifier: Confirmed: the ONE `role="status"` `<p>` sits at MenuPriceEditor.tsx:271, before the `<ul>` at :294, and its style flips from `srOnly` (:629, `position:absolute`, 1×1) to `okLine`/`errLine` (:619-628, block with `margin: var(--s3) 0 0` + a body line) the moment `msg` is set — so every outcome/refusal lands above the list and pushes it ~30px down under the thumb. The row itself carries no verdict; it only re-labels after `router.refresh()` (:145). The concurrency refusal (`menu-availability.ts:75-78` English sentence) is exactly the case where the cook needs the reason and cannot see it.
  - on the fix: Keeping the live region permanently sr-only (no geometry change) and echoing the same `Msg` aria-hidden inside the acted row (store the item id on `Msg`; the `plain` saveUnknown arm has `current.id`) preserves ONE live region and removes the layout shift. Because the row's pill also re-labels only after the refresh, pair this with menu-3 so the echo and the pill agree. Reject the scrollIntoView alternative as the auditor did.
- **menu-3** [med/M] — The 86 flip is not optimistic: between the response and `router.refresh()` the row shows the STALE label, so a second tap sends a stale `expectedSoldOut` and is refused — **real · med**
  - anchor: `apps/qr/components/staff/MenuPriceEditor.tsx:112`
  - fix: Hold a `Map<id, boolean>` of confirmed overrides: on `r.ok` set `override.set(i.id, r.soldOut)` BEFORE clearing `flipping`, read `soldOut = override.get(i.id) ?? i.soldOut` for the row's label/style/`expectedSoldOut`, and drop the entry in a `useEffect` on `items` once the prop agrees (the refresh landed). On `!r.ok` clear nothing (revert-to-confirmed is the prop). Same shape for the saved price (`res.priceCents`) so the row shows the new amount before the refresh. Pin it in the new jsdom suite: after the mocked action resolves `{ ok: true, soldOut: true }`, the row reads "Put back" before any rerender with new props.
  - verifier: Confirmed: `setFlipping` delete runs in `finally` (:124-130) BEFORE `router.refresh()` (:145) resolves, and the pill's label/style/`expectedSoldOut` all read the prop `i.soldOut` (:115-117, :339, :365-371). So between the response and the refreshed RSC payload the pill re-enables with the STALE verb; a second tap posts the stale `expectedSoldOut` and `menu-availability.ts:75-78` refuses it with the English 'Someone else already marked …' sentence. §4's doctrine is flip-at-tap + revert-to-confirmed; this control shows '…' then the old label.
  - on the fix: A confirmed-override Map read for label/style/expectedSoldOut and dropped once the `items` prop agrees closes the failure and honours §4 rule 3/4 (record every confirmed value, revert to the prop on refusal). It is a half-step short of §4 rule 1 (flip at TAP) — a true optimistic flip with revert-on-refusal is also compliant here since 86 is not an amount; either is acceptable. Same shape for `res.priceCents` (:241) is fine — a dollar figure returned by the server is confirmed, not optimistic.
- **menu-4** [med/M] — "sold out since {t}" prints a clock with no day, so a flag from yesterday is indistinguishable from one set this shift — the exact case the stamp exists to expose — **real · med**
  - anchor: `apps/qr/components/staff/MenuPriceEditor.tsx:558`
  - fix: A pure rule in `lib/menu-availability.ts` (or a tiny `lib/sold-out-since.ts`): `soldOutSinceParts(iso, now, tz)` → `{ sameDay: true, t } | { sameDay: false, d, t }` (compare LA calendar dates via `Intl.DateTimeFormat` parts), value-falsifiable with a suite (23:59 vs 00:01 across the LA midnight). Render `browse.price.soldOutSince` for same-day and a new key `browse.price.soldOutSinceDay` ("· sold out since {d} {t}", e.g. "Tue 6:40 PM") otherwise, and tint the older tag `--warn` so an overdue flag is pre-attentive. One new MY string → K15.
  - verifier: Confirmed: `clockFmt` at MenuPriceEditor.tsx:530-534 is `hour: "numeric", minute: "2-digit"` only, rendered through `browse.price.soldOutSince` (staff.ts:1120-1122, ' · sold out since {t}'). The type's docblock (:17-18) and `browse.price.leadServer` (staff.ts:1108) both promise this stamp is the only thing exposing a flag that outlived its shift; a 6:40 PM from yesterday reads identically to one from this shift.
  - on the fix: A pure `soldOutSinceParts(iso, now, tz)` in lib with a suite across the LA midnight is the right shape (decision in lib, value-falsifiable). New key `browse.price.soldOutSinceDay` → K15. Tinting the older tag `--warn` is token-compliant. Put the shared LA formatter in the same `lib/staff-clock.ts` tips-1 proposes so the zone is named once.
  - new Burmese → K15
- **menu-5** [med/M] — Price entry has no inline validation — an out-of-range or unchanged amount just greys Save with no reason — **real · low**
  - anchor: `apps/qr/components/staff/MenuPriceEditor.tsx:148`
  - fix: A pure `priceDraftVerdict(draft, currentCents)` in `lib/menu-price.ts` returning `"ok" | "empty" | "nan" | "below" | "above" | "unchanged"`, exporting `PRICE_MIN_CENTS`/`PRICE_MAX_CENTS` once (the schema reads the same constants — name it ONCE). Suite + a mutant on the bound. Render a `--warn` `--fs-xs` line under the field for every non-ok verdict via four new keys (`browse.price.draft.below` "Lowest price is $0.25", `.above` "Highest price is $5,000.00", `.nan` "Numbers only — 14.50", `.unchanged` "That's the current price"), `aria-describedby` from the input; Save becomes `aria-disabled` (menu-1). Four new MY strings → K15.
  - verifier: Confirmed: `validDraft` at MenuPriceEditor.tsx:162-167 duplicates the bounds 25/500000 from packages/db/src/schemas.ts:574 (`z.number().int().min(25).max(500000)`), Save is `disabled={!validDraft}` (:487) with `saveBtnOff` opacity .5 (:594) and no sentence, no `aria-describedby`; the server's own bound sentence (menu-price.ts:64 'Enter a price between $0.25 and $5,000.00.') is unreachable from the UI because the client pre-filters. Opening a row (`openEdit` :189-194 seeds the current price) starts with Save dead and nothing saying why. Low, not med: a manager types a price a few times a month; the dead-button reason is guessable and the confirm step still names both amounts.
  - on the fix: Shape is right (pure verdict + shared constants + four keys → K15 + describedby), but the location as written breaks the build twice: (1) `apps/qr/lib/menu-price.ts` is a `"use server"` module (menu-price.ts:1) — Next refuses non-async exports from it, so neither `priceDraftVerdict` nor `PRICE_MIN_CENTS` can live there; (2) 'the schema reads the same constants' inverts the one-way dep rule — `packages/db` cannot import from `apps/qr`. Export the two constants from `@mms/db` (beside `setMenuPriceInput`, schemas.ts:572) and put the pure verdict in a new non-server `lib/menu-price-draft.ts` that imports them. Keep Save native-disabled-on-invalid or convert — K35 says the invalid-form case is not the focus-drop rule.
  - new Burmese → K15
- **menu-6** [med/S] — Menu, Tips and Glossary borrow the FLOOR's loading skeleton — wrong geometry, no bar, and an English literal sr-only string — **real · low**
  - anchor: `apps/qr/app/staff/loading.tsx:10`
  - fix: Add `menu/loading.tsx` and `tips/loading.tsx` (glossary can share tips') that draw the bar's footprint (a 44px circle, a two-line title, two 44px circles right) plus the page's own shapes at 640px (a 48px field + 6 × 85px rows for Menu; a total card + 3 rows for Tips). Make the sr-only text a dictionary key rendered through `<Chrome>` (`readStaffLang()` is async and a loading component may be async) — e.g. reuse `what.menuPrices`/`what.tips` inside a new `shell.loading` "Loading {x}…" key. One new MY string → K15.
  - verifier: Confirmed: only `app/staff/loading.tsx`, `kitchen/loading.tsx` and `table/[id]/loading.tsx` exist (find), so `/staff/menu`, `/staff/tips`, `/staff/glossary` fall back to `app/staff/loading.tsx`, which draws a 1080px counter-era layout (three 132×48 buttons + two card grids, :10-42), no StaffBar, and the English literal `Loading the floor…` (:11). It is stale even for `/staff` itself — the home is now doors under a StaffBar in a 1080 `.staff-col` (staff/page.tsx:143-168). `check-staff-lang.mjs:169` exempts `loading` files, so the literal is unguarded. Low: a sub-second flash; but it fails RUBRIC #1 (geometry-matched skeleton, no shift) on every arrival at three screens.
  - on the fix: Per-route `menu/loading.tsx` and `tips/loading.tsx` drawing the bar footprint + the page's 640px shapes is right. For the sr-only text, do NOT make loading.tsx async on `readStaffLang()` (an async fallback defers the very thing it exists to show); `app/staff/layout.tsx:31` already wraps children in `StaffLangProvider`, so a `"use client"` loading component can read `useStaffLang()` and render `<Chrome k="shell.loading" vars={{x}}>` synchronously. One new MY string → K15. Also refresh `staff/loading.tsx` itself for the doors (out of this surface, note it).
  - new Burmese → K15
- **gloss-1** [med/S] (row K26) — K26 still live on the glossary: `<main className="pgl">` puts the StaffBar inside a 940px padded column on a flat ground, with no `.staff-main` LINES and no back leading — **real · low**
  - anchor: `apps/qr/app/staff/glossary/page.tsx:70`
  - fix: `<main className="staff-main">` → `<StaffBar className="print-hide" leading={{ kind: "back", href: "/staff/menu", k: "pilot.gloss.back" }} …/>` → `<div className="staff-col pgl">` carrying the sheet; move `.pgl`'s `max-width`/`margin` to that column (drop its horizontal padding — `.staff-col` already has it) and delete `.pgl-bar/.pgl-bar-right/.pgl-back`. The `@media print` block's `main { padding:12px !important; max-width:none !important }` still applies (the element is still `<main>`), so the paper is untouched; verify `.staff-main`'s `background-image` is dropped by the existing `body { background:#fff !important }` rule or add `.staff-main { background-image:none }` to the print block. New key `pilot.gloss.back` "← Menu" → K15.
  - verifier: Confirmed: glossary/page.tsx:70 `<main className="pgl">` with the StaffBar as its child (:71-77); `.pgl` at globals.css:9404-9408 is `max-width: 940px; margin: 0 auto; padding: var(--s5) var(--s5) var(--s8)` — so the bar sits inside a padded, centred column on a flat ground with none of `.staff-main`'s 28px lines (globals.css:9998-10005), and `leading` defaults to the Screens circle (StaffBar.tsx:50) on a page menu/page.tsx:38-41 calls 'an ACTION of this screen'. `.pgl-bar`/`.pgl-bar-right`/`.pgl-back` (globals.css:9409-9429) have no TSX reference (grep across apps/qr: only `pgl-print` is used). K26 files this at low and I agree.
  - on the fix: `.staff-main` → StaffBar with `{ kind: "back", href: "/staff/menu", k: "pilot.gloss.back" }` → `.staff-col pgl` is the §17 shape; new key → K15. Two print-block caveats the auditor half-saw: the W7a `main { max-width: none !important }` (globals.css:9578-9581 comment) resets MAIN, so once `.pgl`'s 940px moves to the inner div add `.pgl { max-width: none }` under `@media print` (inert on Letter portrait ≈816px, visible on landscape), and add `.staff-main { background-image: none }` there so the lines never reach paper.
  - new Burmese → K15
- **gloss-2** [low/S] (row K26) — Two vocabularies for one Print action: a text pill in the glossary bar, a 44px circle for the same sheet on the Menu bar — **real · low**
  - anchor: `apps/qr/components/staff/PrintSheetButton.tsx:23`
  - fix: Render PrintSheetButton as `<button type="button" className="staff-circ staff-press print-hide"><Icon name="print" size={20} /><span className="sr-only"><Chrome lang={lang} k="pilot.gloss.print" /></span></button>` (the circle idiom already used for the same action) and delete `.pgl-print`. If the owner wants the word visible for discoverability, put a `.staff-back`-style pill in the MIDDLE slot instead — never a second trailing vocabulary.
  - verifier: Confirmed: PrintSheetButton.tsx:25 renders `className="pgl-print print-hide"` — a text pill (globals.css:9430-9439: 44px, `padding: 0 18px`, `--cd` fill, no `.staff-press`, no glyph) in the bar's trailing slot, while the same sheet is reached from the Menu bar via a `.staff-circ staff-press` print-glyph circle (menu/page.tsx:86-91). §17: trailing = circles in one order. The component docblock (:8-11) argues for a visible bilingual label, which is why it was built this way; sr-only `<Chrome>` still names it in the device language for AT, so the argument only covers sighted discoverability.
  - on the fix: The circle + sr-only `<Chrome k="pilot.gloss.print">` matches StaffBar's own pattern (StaffBar.tsx:84-91) and rule 3 of check-staff-lang; delete `.pgl-print` with the dead `.pgl-bar*` rules from gloss-1. If the owner wants the word visible, the middle slot is the only §17-legal place. Bundle with gloss-1 (one file each, same PR).
- **tips-1** [low/S] (row K13) — Feedback timestamps use the DEVICE clock and locale while every other time on the same screen is pinned to the restaurant's (America/Los_Angeles) — **real · med**
  - anchor: `apps/qr/app/staff/tips/page.tsx:312`
  - fix: One formatter in `lib/staff-clock.ts` (`staffDateTime(iso)` / `staffClock(iso)`, both `en-US` + `America/Los_Angeles`, Latin numerals per fill.ts's clock rule) used by tips/page.tsx, PilotNightSheet's `laDate` and MenuPriceEditor's `clockFmt` — name it ONCE. A three-line suite pins the zone with a UTC fixture.
  - verifier: Real, and the mechanism is WORSE than the auditor's: `tips/page.tsx` is an async Server Component (no `"use client"`, :46), so `toLocaleString(undefined, …)` at :312-317 runs on the server, not the device — on Vercel's Node runtime that is UTC (no `TZ` in apps/qr/vercel.json, which holds only `ignoreCommand`), so EVERY feedback row on prod prints UTC for every manager (a 7:05 PM PDT review reads 'Sep 18, 2:05 AM'), while `PilotNightSheet.tsx:340-347` `laDate` and `MenuPriceEditor.tsx:530-534` `clockFmt` beside it are pinned to America/Los_Angeles. The 'device locale/third script' half of the auditor's story does not apply (server locale), but the zone defect is unconditional, hence med not low. K13 is not the right row — this is not the triage-state gap; it is unfiled.
  - on the fix: One `lib/staff-clock.ts` (`en-US` + `America/Los_Angeles`, Latin numerals) used by tips, PilotNightSheet and MenuPriceEditor, with a UTC-fixture suite, is right and cheap. Note three more device-clock sites outside this surface for the same sweep: KdsBoard.tsx:108 `toLocaleTimeString([])`, HelpButton.tsx:273 and :613 `toLocaleDateString()` (client components, so genuinely device zone there).
- **tips-2** [med/M] (row K13) — A "Needs follow-up" row cannot be traced to a table or an order — the read drops `order_id`, so the badge asks for a recovery nobody can start — **real · med**
  - anchor: `apps/qr/lib/feedback.ts:121`
  - fix: Read-side only, no migration: extend the select to `id,rating,comment,created_at,order_id,qr_orders(order_code,table_label|session mode)` (whatever `TRACK_ORDER_SELECT`'s identity fields are called — reuse that shape's naming), add `orderRef` to `StaffFeedbackRow`, and print it on the row as the identity the console already uses elsewhere (table number for dine-in, first name + short code for pickup) through existing `STAFF_CHANNEL_KEY` vocabulary; link it to `/staff/orders` if that page grows per-order addressing (it has none today — `orders/page.tsx` takes no params), otherwise plain text. Replace the magic numbers with tokens in the same edit. The handled/assign STATE half of K13 stays a write path and its own slice.
  - verifier: Confirmed: `getStaffFeedback` selects `id,rating,comment,created_at` (lib/feedback.ts:121) and `StaffFeedbackRow` (:95-100) carries no order reference, while `mms_feedback.order_id` is `uuid not null unique references qr_orders(id)` (migration 20260623090000_m4_feedback.sql:8). The row renders stars + a `floor.fb.followUp` badge (`<span>`, tips/page.tsx:304-308) + time + comment — nothing a manager can look up. The identity fields the console already uses are on `qr_orders` per `TRACK_ORDER_SELECT` (track-order.ts:23: `table_number`, `customer_name`, `pickup_slot`), not `order_code`/`table_label` as the auditor guessed (they hedged). Magic numbers confirmed: tips/page.tsx:264 `margin: "16px 0 0" … gap: 10`, :279 `gap: 10`, :367 `padding: "12px 14px"`, :374-375 `borderRadius: 999; padding: "2px 8px"`. K13 read-only is by pilot decision (PilotNightSheet.tsx:19-20), but a lookup is a READ and the pilot window (filed 2026-09-05) has elapsed.
  - on the fix: Read-side embed `order_id,qr_orders(table_number,customer_name,pickup_slot)` through the service client is fine (FK exists; not a money change). Two constraints: `apps/qr/lib/feedback.ts` is in the verify:slice mutate set (scripts/verify-slice.mjs:4529-4531, suite `lib/feedback-read.test.ts`) so the select change must keep that mutant red and extend the suite's fixture; and print the identity through existing `STAFF_CHANNEL_KEY` vocabulary as the auditor says, plain text (orders/page.tsx has no per-order addressing). Replace the five magic numbers with `--s2/--s3/--s4` and `--r-full` in the same edit. The handled/assign state stays K13.

**What the verifier found that the auditor missed:**

- ROW-STATUS: K35 (not in the auditor's list, docs/OPEN-ITEMS.md:378) already files menu-1 — it names "MenuPriceEditor 4" (the four `disabled={` sites at MenuPriceEditor.tsx:340, :437, :447, :487) and carves out :487's invalid-form disable as a different rule; menu-1 should be triaged as K35's MenuPriceEditor entry, not a new row. K24: agree still-live for THIS surface (menu/page.tsx:109 and tips/page.tsx:341 both `maxWidth: 640`, one 115-row column), but the row's core — seven divergent widths — is mostly converged (640 everywhere, /staff 1080 per staff/page.tsx:267), so the row as a whole is partly-closed.
- MenuPriceEditor.tsx:469-479 — the price field has no `<form>`, no `onKeyDown`, no `enterKeyHint` (grep across the file: zero hits), so the tablet keyboard's Return key does nothing after typing 14.50; the manager must find and tap Save. RUBRIC #6/#9 (effort at the floor, inline flow). Fix S: `onKeyDown` Enter → `validDraft && setConfirming(true)`, `enterKeyHint="done"`.
- MenuPriceEditor.tsx:148-157 — `shown` filters only by name/category text; there is no 'sold out only' filter or sort, and sold-out rows are not grouped (menu/page.tsx:63 orders by `name_en`; the flag is a `--t3` weight-400 suffix, :528). The lead copy (staff.ts:1108) tells a server to watch for flags that outlived their shift, but finding them means reading ~115 rows top to bottom. RUBRIC #6/#9, O-D. Fix S–M: a pressed chip (lit-cap vocabulary) 'Sold out (n)' or float sold-out rows first when the search is empty; pure predicate in lib with a suite.

## Sign-in + shared chrome

_Auditor’s surface line:_ Sign-in (login · lock · signed-in card · roster) + shared chrome (StaffBar · doors · Help · Lock · language switch · outage shell · error/loading)

- **signin-1** [med/M] — The /staff loading skeleton is the OLD counter's geometry with no bar, so every console navigation lands the sticky bar and the card on top of a different layout — **real · med**
  - anchor: `apps/qr/app/staff/loading.tsx:10`
  - fix: Make the skeleton wear the chrome the real pages wear: `<main className="staff-main">` → a `.staff-bar`-shaped band (a 44px circle + a 30px/13px title block, min-height 68px, `env(safe-area-inset-top)`) → a `.staff-col` with ONE textured card skeleton (the login/lock/me shape, 440px) and, beneath, two door-shaped 240px cards for the /staff arrival. Route the sr-only line through the dictionary (a `Chrome` render of a `shell.loading` key is impossible in a non-async server file, so read the cookie with `readStaffLang` — loading.tsx may be async in App Router — or drop the English and let the bar band carry `aria-busy`). Sizes from `--s*`/`--fs-*` tokens, never literals. A jsdom render test that asserts the skeleton's first child is `.staff-bar`-shaped so it cannot drift from `StaffBar` again.
  - verifier: Confirmed. apps/qr/app/staff/loading.tsx:10-44 is `<main style={{ maxWidth: 1080, margin: "0 auto", padding: "var(--s6)" }}>` with a 26px heading skeleton + three 132×48 buttons + two card grids and magic gaps (18/12) — no `.staff-main`, no bar band. Only kitchen/ and table/[id]/ override it (find: 3 loading.tsx), so /staff/login, /staff/lock and the doors all fall back to it. The real login page is `.staff-main` → `<StaffBar>` → `.staff-col entry-col` (login/page.tsx:84-91, 118-125); the doors page is `.staff-main` → bar → 1080 `.staff-col` → `StaffDoors` (staff/page.tsx:164-168). Geometry mismatch is real; RUBRIC #1 fails.
  - on the fix: Bar-shaped band + one card skeleton is right and token-only. Do NOT make loading.tsx async to read the cookie — a Suspense fallback that itself awaits defeats the instant paint; take the auditor's second option (drop the literal, `aria-busy` on the band, or a language-neutral sr-only via the layout). Keep the jsdom assertion.
- **signin-2** [med/M] — The roster's three controls are natively `disabled` the moment they are tapped — the one pattern §17 forbids on the console — **real · med**
  - anchor: `apps/qr/components/staff/TeamManager.tsx:230`
  - fix: Mirror `SignedInCard`: `aria-disabled={busy || undefined}` + `if (busy) return;` at the top of `add()`, `changeRole()`, `toggleActive()`; move the short-input refusal into the handler with a `floor.team.err.*` key in the ONE live region and focus to the field at fault; keep the select/toggle rendered-and-focusable with `aria-disabled`/`aria-busy`. Style the greyed state through the existing `[aria-disabled="true"]` rules (`.entry-primary[aria-disabled="true"]`) rather than a fresh inline one. Extend `TeamManager.test.tsx` with 'focus stays on the tapped control while busy' and 'second tap while busy does not post'.
  - verifier: Confirmed at TeamManager.tsx:230 `disabled={busy || name.trim().length < 1 || email.trim().length < 3}`, :324 `disabled={rolePendingUid === row.userId}`, :347 `disabled={pendingUid === row.userId}`. `add()` (:80-105), `changeRole()` (:107), `toggleActive()` (:129) have no re-entry guard. §17 forbids exactly this. BUT it is already filed: OPEN-ITEMS K35 (line 378) names TeamManager's three sites explicitly — the auditor's `filed_row: null` is wrong.
  - on the fix: Mirror SignedInCard: `aria-disabled` + `if (busy) return` in all three handlers; the `.entry-primary[aria-disabled="true"]` rule (globals.css:10684) exists but the roster's buttons are inline-styled, so either adopt `.entry-primary` or add an `[aria-disabled]` arm to the inline style. `floor.team.err.*` keys ARE new MY strings → K15 (auditor marked new_burmese false). File under K35, not a new row.
- **signin-3** [med/M] — A failed Lock renders a raw English sentence INSIDE the bar's tail — untranslated, and it reflows the circles a person is mid-tap on — **real · med**
  - anchor: `apps/qr/components/staff/LockButton.tsx:57`
  - fix: Have `lockConsole` answer reason codes like `setPin`/`removePin` do (`{ ok:false, reason: "outage" | "no_pin" | "auth" }`); in `LockButton` map them to dictionary keys (`shell.lock.err.outage`, `shell.lock.err.noPin` — new MY strings → K15) and render through `<MsgText>`. Take the message OUT of the tail: a `.staff-bar-msg` element with `flex-basis:100%; order:9` beneath the row (still `role="alert"`, the only assertive channel besides the switch's), or hand it to the page's polite region via a callback. `LockButton` has no suite — add `LockButton.test.tsx` (jsdom) asserting the busy name, the refused re-entry, and that a `no_pin` answer renders the Burmese key.
  - verifier: Confirmed. LockButton.tsx:31 `setErr(res.error)` stores the server sentence; :56-60 renders `<span role="alert" style={{ fontSize: "var(--fs-sm)", color: "var(--warn)" }}>{err}</span>` as a fragment sibling of the circle, i.e. INSIDE `.staff-bar-tail` (StaffBar.tsx:114-119), which is `display:flex; flex-wrap:wrap` (globals.css:10084-10091). `lockConsole` answers strings only (staff-pin-actions.ts:83-89: `PIN_OUTAGE` and `"Set a PIN before locking."`); Chrome.tsx:115-120 `OUTAGE_TWINS` holds only STAFF_WRITE_OUTAGE and AUTHORITY_UNCONFIRMED, and LockButton does not route through `<OutageText>`/`<MsgText>` anyway. The pin-actions docblock (:29-31) even records the sentence shape as deliberate — it is the P2m defect kept alive on the bar.
  - on the fix: Reason codes + `shell.lock.err.*` keys through `<MsgText>` is the §17 shape (new MY → K15). Moving the alert out of the tail is right; keep it `role="alert"` (assertive channel, same as the switch's `.staff-lang-err`). Note `no_pin` is near-unreachable (the circle only mounts when hasPin) — the outage arm is the one that matters.
  - new Burmese → K15
- **signin-4** [med/S] — The roster's role `<select>` is 13px, so iOS Safari zooms the whole page when a manager opens it and never zooms back — **real · low**
  - anchor: `apps/qr/components/staff/TeamManager.tsx:461`
  - fix: `fontSize: "var(--fs-body)"` on `roleSelect` (16px) — or, the M78 shape, introduce `--fs-field: max(1rem, var(--fs-body))` in tokens.css and point `.entry-input`, `.help-report-field` and both selects at it so the floor is a rule rather than three coincidences. One line; refresh the roster's inline styles into `.entry-*`/`.staff-*` classes while there (RUBRIC #10 — `marginBottom: 6`, `marginTop: 2`, `minHeight: 20` are magic).
  - verifier: Confirmed: TeamManager.tsx:454-464 `roleSelect` sets `fontSize: "var(--fs-sm)"` = 0.8125rem/13px (tokens.css:37) on the per-row `<select>` (:336), while the add-form select uses `input` at `--fs-body` (:216, :414). iOS Safari auto-zooms on focus of any form control under 16px, `<select>` included, and the repo's own comments size `.entry-input` (globals.css:10648) and `.help-report-field` (:10945) for exactly this. Severity low, not med: iPadOS Safari does not auto-zoom on focus — the failure is on a manager's PHONE, changing a role, a rare task.
  - on the fix: One-line `--fs-body` on `roleSelect` closes it. The `--fs-field` token is M78's shape and is the better fix if M78 is taken in the same slice; do not leave `.help-report-field`'s hardcoded 1.0625rem behind if so.
- **signin-5** [low/M] — The outage shell and the error boundary drop the bar: the takeover changes the chrome, and the language switch floats at a position it has nowhere else — **real · low**
  - anchor: `apps/qr/components/staff/StaffOutageShell.tsx:46`
  - fix: `StaffBar` is plain JSX with no data need: render `<main className="staff-main"><StaffBar lang={lang} title="out.shell.title" leading={{ kind: "here", icon: "alert" }} /><div className="staff-col entry-col">…</div></main>` in both takeovers (the bar carries the switch in its fixed slot; no Lock, no Screens — the 'here' mark, exactly the front-door rule), and pass `headingLevel="h2"` to `OutageState`/`OutageRefresh` so the bar's h1 stays the h1 (`focusOnMount` still lands on the card heading). Delete `wrap`/`switchRow` from both files. Assert in `Chrome.test`/a new `StaffOutageShell.test.tsx` that the switch renders inside `.staff-bar-tail`.
  - verifier: Confirmed. StaffOutageShell.tsx:46-49 `<main style={wrap}>` (wrap = maxWidth 640, :66-70 — no `.staff-main`, so no page LINES) with the switch in a hand-placed `switchRow` (:73-77); error.tsx:55-61 does use `.staff-main` + `.staff-col` but again no `StaffBar` and a bespoke `switchRow` (:90-94). §17 'a parent learns WHERE once' is broken on both takeovers. `OutageState`/`OutageRefresh` take `headingLevel` (fallback.tsx:111,138) so demoting to h2 under a bar h1 is supported.
  - on the fix: Render `<StaffBar leading={{ kind: "here", icon: "alert" }} title="out.shell.title" lang={lang}/>` in both, `headingLevel="h2"` on the card, delete `wrap`/`switchRow`. Check `alert` is an ICONS name (packages/ui/src/icon.tsx) before using it. `focusOnMount` still lands on the card heading — fine.
- **doors-1** [low/S] (row K29) — The doors' titles sit at different heights (K29) and a tapped door shows nothing while its cookie write is awaited — **real · low**
  - anchor: `apps/qr/app/globals.css:9797`
  - fix: Top-anchor: `.staff-door { justify-content: flex-start }` and move the slack to the sub/here block (`.staff-door-sub { margin-top: auto }` or give `.staff-door-name` `flex: 1` with the title first) so both titles share a baseline and only the note takes the remainder. Busy: `.staff-door[aria-busy="true"] { opacity: .7; cursor: progress }` plus a `busy` word in the door's `.staff-door-here` slot (`floor.door.opening`, an existing-idiom key like `shell.locking`) so the state is spoken and seen; RM-neutral (no motion). `StaffDoors.test.tsx` already holds selectors to the DOM — add the busy one.
  - verifier: Confirmed both halves. globals.css:9794-9799 `.staff-door { display:flex; flex-direction:column; justify-content: space-between; … min-height:240px }` with two children (icon, `.staff-door-name`); the `.staff-door-here` line renders INSIDE `.staff-door-name` only on the `aria-current` door (StaffDoors.tsx:100-104, 123-127), so that door's name block is taller and its 38px title sits one line higher — K29's doors half is still live. `aria-busy` is set for the whole `await setStaffDoor` (StaffDoors.tsx:63-77) and `grep aria-busy globals.css` returns nothing, so nothing is drawn or said for it beyond the 240ms press.
  - on the fix: `justify-content: flex-start` + `margin-top:auto` on the sub/here block is right and RM-neutral. The busy word is a new MY key → K15. StaffDoors.test.tsx:44/91 already asserts the attribute, so add the visible/spoken busy assertion there.
  - new Burmese → K15
- **help-1** [low/S] — Two Help pictures are drawings, not declarations: the HELD card and the BUMP replica carry sizes and borders the real controls do not — **real · low**
  - anchor: `apps/qr/components/staff/HelpPicture.tsx:59`
  - fix: Card 4: wrap the fire button in a real `.kds-ticket.kds-ticket-held` shell (the ticket's own class, `data-size` inherited) and delete `.help-pic-held`. Card 1: drop the size/font overrides from `.help-pic-bump` and let the replica sit inside a `.kds-root`-scoped wrapper so `--kfs-clock` resolves (P6f's lesson: `--kfs-*` is declared only on `.kds-root`), keeping only `margin: 0`. Extend `HelpPicture.test.tsx` to assert each picture's class list is a subset of the live control's class list plus `help-pic-*` layout-only classes whose declarations contain no color/size.
  - verifier: Confirmed. globals.css:10860-10866 `.help-pic-held { … border: 2px dashed color-mix(in oklab, var(--tx) 30%, transparent); … opacity: 0.85 }` is a new drawing; the real held ticket is `.kds-ticket-held { opacity: var(--kds-held-op); border-style: dashed }` (:7504-7507) on `.kds-ticket`'s own 1px `--bd` border. `.help-pic-bump` (:10841-10849) overrides `.kds-bump`'s `min-height: 64px … font-size: var(--kfs-clock)` (:7787,7792) to `min-height: 56px; padding: 0 22px; font-size: var(--fs-h3)`. §17/P7·3 forbids a copy-class. The font override exists because `--kfs-clock` is declared only on `.kds-root` (:7365, P6f) — so it is a symptom of the token scoping, not a whim.
  - on the fix: The held-shell half is fine (`--kds-held-op` lives in tokens.css:30 `:root`, so it resolves in the sheet). The bump half is NOT: wrapping the picture in a `.kds-root` drags `min-height:100dvh; background:var(--pg); padding; display:flex` (globals.css:7355-7375) into a help card. Instead lift the `--kfs-*` scale to `:root`/tokens.css (which also closes P6f) or declare it on `.help-pic`, then drop the size/font overrides. Keep the class-subset test.
- **doors-2** [low/S] — The More list has no accessible name — the docblock says the heading names it, but `aria-labelledby` is on the section, not the list — **real · low**
  - anchor: `apps/qr/components/staff/StaffDoors.tsx:153`
  - fix: `aria-labelledby="staff-more-h"` on the `<ul>` (the heading is already dictionary text through `<Chrome>`, so no new string and rule 3 is untouched). One attribute; add the assertion to `StaffDoors.test.tsx`.
  - verifier: Confirmed. StaffDoors.tsx:149-153 `<section aria-labelledby="staff-more-h"> <h2 id="staff-more-h" …> <ul className="staff-inset" role="list">` — the `<ul>` carries no name; `.staff-inset` is `list-style: none` (globals.css:10261) so `role="list"` is load-bearing. The component docblock (:142) and §17 both claim the list is 'named by the visible heading', which is only true of the section. The roster (TeamManager.tsx:257) and the Help rows name theirs.
  - on the fix: `aria-labelledby="staff-more-h"` on the `<ul>` — dictionary text via `<Chrome>`, no literal aria-label, rule 3 untouched. One line + a StaffDoors.test assertion.
- **chrome-1** [low/S] — The Lock circle is the only bar circle with no press feedback and no haptic — **real · low**
  - anchor: `apps/qr/components/staff/LockButton.tsx:45`
  - fix: `className="staff-circ staff-press"` and `haptic("commit")` at the top of `lock()` (a lock is a commit — the tablet is being handed off; the visible half is the press and the lock screen that follows, per the doors' rule). Two lines; covered by the new `LockButton.test.tsx` from signin-3.
  - verifier: Confirmed. LockButton.tsx:43-49 `className="staff-circ"` with no `staff-press` and no `haptic()` import; the Screens circle is `staff-circ staff-press` (StaffBar.tsx:84), Help is `staff-circ staff-circ-gold staff-press` + `haptic("pick")` (HelpButton.tsx:279-283), the KDS wall link is `staff-circ staff-press` (KdsBoard.tsx:596). §17 'Press = you committed' names `.staff-press` as the idiom.
  - on the fix: `staff-circ staff-press` + `haptic("commit")` after the `if (busy) return` guard. `.staff-press` adds `position:relative; overflow:hidden` — harmless on a 44px circle whose only child is the glyph + sr-only span (the error span is a fragment sibling, not inside).
- **doors-3** [low/S] (row K29) — The More rows' English echo is 11px — below the `--fs-sm` floor K29 named, on a 62px row read at arm's length — **real · low**
  - anchor: `apps/qr/app/globals.css:10315`
  - fix: `font-size: var(--fs-sm)` on `.staff-row-name > .chrome-pair > .chrome-en` (the bar title's echo already uses `--fs-sm` at globals.css:10073 — same idiom, same floor). The Help sheet's rows share `.staff-row-name` and inherit the fix. `StaffDoors.test.tsx` holds every `.staff-row-name` selector to the DOM, so no new selector risk.
  - verifier: Confirmed. globals.css:10315-10319 `.staff-row-name > .chrome-pair > .chrome-en { font-size: var(--fs-xs); … }` and tokens.css:38 `--fs-xs: 0.6875rem; /* 11 */`, under a 19px Padauk line (:10311). K29 named 12px-under-18px as too small with `--fs-sm` (13px) as the floor; the bar title's echo uses `--fs-sm` (:10073). Applies only in Burmese mode (English renders a bare text node at `--fs-h3`) — which is the console's primary mode. The Help sheet's rows share `.staff-row-name` (HelpButton.tsx:321/343/374) and inherit both the defect and the fix.
  - on the fix: `var(--fs-sm)` on that rule. Row height is 62px so 13px fits without changing the row's min-height; the selector is already held to the DOM by StaffDoors.test.tsx.

**What the verifier found that the auditor missed:**

- apps/qr/components/staff/TeamManager.tsx:244 + apps/qr/components/staff/SignedInCard.tsx:286 — TWO polite live regions in one view. The signed-in /staff/login renders `SignedInCard` (`<p id="me-msg" role="status">`) and, for a manager, `TeamManager` (`<p role="status" style={{ minHeight: 20 … }}>`) inside the same `.staff-col entry-col` (login/page.tsx:125-143). Each file's comment claims to be 'the one region for this view' — true before A4·4 folded the profile into the sign-in screen, false now; house rule + §7 say ONE polite region per view. Also the roster's region carries magic `minHeight: 20` (RUBRIC #10) where `.entry-msg` (globals.css:10719-10724) already declares the same 20px floor in tokens.
- apps/qr/components/staff/TeamManager.tsx:272 — `style={{ ...rowCard, opacity: row.active ? 1 : 0.6 }}` dims a whole inactive row, including its text and the 44px Reactivate control. Computed against the light tokens (tokens.css:72-76): `--t2 #6e6358` on `--cd #fffdf8` is ~5.8:1, but at 0.6 opacity the composited ink (~#a8a198) drops to ~2.5:1 — the email line, '(you)', the `--warn` 'Inactive' tag and the `--ac` Reactivate label all fall below WCAG AA 4.5:1 on exactly the row a manager needs to read to reactivate someone (RUBRIC #8). Fix: dim only decorative parts or use `--t3` ink at full opacity; keep the control at 1.
- ROW-STATUS: K35 (OPEN-ITEMS line 378) is STALE on this surface — its per-file list says 'SignedInCard · PinUnlock · 3 each' and 'StaffLogin 5', but `grep 'disabled={'` in those three files returns ONLY `aria-disabled` (SignedInCard.tsx:246/261/277, PinUnlock.tsx:138/145/155, StaffLogin.tsx:281/294/352/390/404) — zero native sites; TeamManager's 3 (:230/:324/:347) are the only native ones left on the sign-in surface. Also signin-2's `filed_row` should read K35, not null. Auditor status calls checked and agreed: M76 still-live (sheet.tsx:88 `<Dialog.Portal>` with no forceMount/data-state; globals.css:180 entrance-only `animation: up`), M78 still-live (no `--fs-field` anywhere; .help-report-field hardcodes 1.0625rem at :10956), M35 still-live (staff-actions.ts:148/259/274/349/362 return STAFF_WRITE_OUTAGE), K29 partly-closed (shared pressed rule globals.css:7455-7460 closes the segment slip; doors-1 and doors-3 halves live), P7c still-live (staff-door.ts:115 `if (!referer || !host) return true;`).

## Cross-cutting — CSS, tokens, Night, motion

_Auditor’s surface line:_ Cross-cutting: the staff CSS, tokens, Night, motion and reduced-motion (apps/qr/app/globals.css .staff-_/.kds-_/.expo-_/.floor-_/.entry-_/.help-_/.orb-\*, packages/ui/src/tokens.css, composite-contrast.test.ts, check-theme-parity.mjs)

- **xcut-1** [high/S] — The working-tree rename .kds-line:disabled → [aria-disabled] leaves composite-contrast.test.ts unable to load — every Night pin (glass floor, room worst pixel, print head, held-card echo) is now un-run — _unverified_
  - anchor: `packages/ui/src/__tests__/composite-contrast.test.ts:626`
  - fix: In composite-contrast.test.ts read the selector the CSS actually ships: `declared('.kds-line[aria-disabled="true"]', 'opacity')` (the `block()` escaper already handles `[`/`]`). Better: make the guard refuse silently-wrong selectors by asserting the rule is the one KdsBoard.tsx:1155 targets (`aria-disabled={pending || held || undefined}`) — a comment naming the component, and red-first: rename the selector, watch it throw, restore. Land it in the same commit as the CSS rename.
- **xcut-2** [high/S] — Focus rings are clipped on the language switch, every KDS per-line check-off and every More row — three overflow:hidden containers eat the global 2px-offset outline — _unverified_
  - anchor: `apps/qr/app/globals.css:100`
  - fix: Add one rule beside the global one: `.staff-lang-btn:focus-visible, .kds-line:focus-visible, .staff-row:focus-visible { outline-offset: -3px; }` (an inset ring survives the clip; `.kds-line`'s 56px min-height leaves room). Verify by tabbing in the StaffBar.test.tsx/KdsBoard.test.tsx jsdom render is not enough — this needs a browser; note it in the PR.
- **xcut-3** [med/S] — .staff-circ:disabled styles a state no control ever enters — a busy Lock circle looks idle, and the 86 button's hover guard checks the wrong attribute — _unverified_
  - anchor: `apps/qr/app/globals.css:10169`
  - fix: Replace with `.staff-circ[aria-disabled="true"] { cursor: default; opacity: 0.6; }` and `.kds-line-86:hover:not([aria-disabled="true"])`. Grep the staff families for any other `:disabled` (`.staff-btn:not(:disabled)` at 3320/3323 is the same class — CashSettleButton/ExpoBoard use `.staff-btn` with `aria-disabled`; convert it too).
- **xcut-4** [med/M] (row K29) — K29 is half-closed: the station segment and the bump ✓ are fixed, but the register's mode pills and the cash tip chips still wear an accent-outline ‘on’ state, the cash confirm is still inline, the More echo dropped to 11px, and the two doors' titles still float at different heights — _unverified_
  - anchor: `apps/qr/components/staff/register-stage.ts:20`
  - fix: (b) give the three register/tip pills `aria-pressed` and add `.staff-chip` to the shared cap list at 7451 (`.kds-chip[aria-pressed="true"], …`), delete `startBtnActive`/`tipChipOn`. (c) mount the confirm in `<Sheet className={…}>` (§16 exits, focus handled by the primitive). (d) `.chrome-en` there → `var(--fs-sm)`. (e) `.staff-door { justify-content: flex-start; }` and `.staff-door-sub { margin-top: auto }` is wrong (sub is inside the name block) — instead put `gap: var(--s4)` and let `.staff-door-name` sit at the top, giving the here-line `margin-top: auto`. Update HelpPicture's pictures, which read the same style objects.
- **xcut-5** [med/S] (row K27) — K27 remnants: two KDS labels are a hard 13px outside the --kfs-\* tier (they ignore the Aa size sheet), and /board's stat/rail labels cap at 14–15px on a 1080p TV — _unverified_
  - anchor: `apps/qr/app/globals.css:7640`
  - fix: `.kds-line-tag` and `.kds-rail h3` → `font-size: var(--kfs-meta)`. On the board reuse the tier `.orb-root` already declares — `--kfs-meta: clamp(15px, 1.2vw, 22px)` (8033) — for `.orb-stat span`, `.orb-rail h3`, `.orb-rail-more` (and `.orb-status` 8055-8060, whose ‘Connecting…’ caps at 20px). Add the three selectors to kds-size.test.ts's ‘reads the tier’ assertion so a literal cannot return.
- **xcut-6** [med/M] (row M127) — Six staff color-mix grounds are pinned by nothing (the M127 class), and one already fails AA: the ‘triaged’ report chip in light is 3.81:1 — _unverified_
  - anchor: `apps/qr/app/globals.css:11071`
  - fix: Extend the existing `declared()`/`block()` helpers in composite-contrast.test.ts to compute each mix from the declaration it ships (as the held-card block does) and assert AA (or the 3:1 large floor where the type qualifies). Darken the triaged ink to `--ac-strong` (light #8f5009) and re-measure before asserting — never paste 3.8148 into an expectation. Red-first each pin.
- **xcut-7** [low/S] — The late-ticket pulse animates `filter` infinitely — a repaint every frame per red ticket, against the transform/opacity-only rule — _unverified_
  - anchor: `apps/qr/app/globals.css:7522`
  - fix: Pulse an overlay instead: `.kds-strip { position: relative }` + `.kds-strip-pulse::after { content:""; position:absolute; inset:0; background: var(--sheen); opacity:0; animation: kdsRedPulse … }` with keyframes on `opacity` (0 → .35 → 0), keep the RM block. Drop the `filter` transition from `.staff-btn` (hover already has transform).
- **xcut-8** [low/S] — Five dead rules, one shadowed declaration, one duplicated recipe and three copied literals in the staff families — _unverified_
  - anchor: `apps/qr/app/globals.css:9892`
  - fix: Delete the five dead rules and the shadowed shadow; fold the glyph squares into one `.staff-glyph-sq` with a `--sq` size var; have the sheet read the tier — declare the three `--kfs-*` size blocks on `.kds-root[data-size], .help-size-sample[data-size]` so the sample reads `var(--kfs-item-my)`. Add the class-usage cross-check to StaffDoors.test.tsx's selector-to-render guard so a dead staff rule is a red test.
- **xcut-9** [low/M] — Off-grid magic numbers, off-token radii, five unnamed Burmese sizes and px breakpoints — the staff families break the sheet's own token/em rules — _unverified_
  - anchor: `apps/qr/app/globals.css:10276`
  - fix: Snap: 62→64, 14/12→`--s3`/`--s4`, 7→`--s2`, 28→`--s6`+4 or `--s8`; radii → `--r-sm`/`--r-card` (add `--r-xs: 8px` and `--r-md: 16px` to tokens.css for the nested chip/note/icon-square tiers and use them on the diner side too). `.entry-col` → `var(--w-content)`, the field → `var(--fs-h3)`. Add `--fs-my-door/-bar/-bar-sm/-row/-lede/-sub` in tokens.css and read them. Breakpoints → 45em / 75em. Extend `kds-size.test.ts`'s literal-ban to the `.staff-*` block.
- **xcut-10** [low/S] — The 6s undo pill — the mis-bump recovery — is fixed 18px from the screen edge with no safe-area inset — _unverified_
  - anchor: `apps/qr/app/globals.css:7987`
  - fix: `bottom: calc(var(--s4) + env(safe-area-inset-bottom, 0px));` — one line; `.help-pic-undo` already resets `position: static` for the sheet replica.
